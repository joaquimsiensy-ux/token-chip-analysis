# 复核 W2 r1: 退回

修复方向正确，能补上盲审 P1 的前缀与来源目录名约束；保留整版成本指标也符合 CHANGELOG 规则。但工单存在错误定位、测试前置条件缺失和范围冲突，需修订后施工。

1. **候选集合严格同构，但生产者行号错误。**
   `sqd_gap_repair.py:595` 为空行，`:596` 是 `_plan` 定义；并集排序实际在 `:607-608`，写入 `plan["candidate_slots"]` 在 `:626`，两类候选在 `:627-629`。
   深验 `:1518-1519` 对同两类候选取并集，故合法输入下 `sorted(all_candidates)` 与生产者候选列表严格一致，包含去重及 beta-only slot。
   工单 §0.3 明定行号不符即停工，§2 引用必须修正。

2. **深验收集前缀的位置可行，应明确初始化与插入点。**
   实际循环从 `solana_exact_validate.py:1369` 开始；`:1370-1371` 核对文件行序与连续 seq，`:1387` 已有 `adopted_valid and expected_seq < adopted["rows"]` 分支。
   可在循环前初始化局部列表，在该分支内追加 `row.get("slot")`，随后于 `:1520-1529` 比较候选前缀。
   保留 `:1404-1407` 的连续性、唯一性拒绝逻辑；不要按 slot 排序台账再比较，否则会掩盖乱序。无需新增遍历。

3. **§3 的 gen 可复用，现有 pending 不满足“两行交换”条件。**
   `test_sqd_gap_repair.py:935-951` 的正向 gen 有两条数据行、`adopted.rows=1`，适合深验向量。
   但发布通过 `sqd_gap_repair.py:337` 将 pending 重命名为 gen，原 pending 已不存在。
   现有 after-commit 场景 `:1089-1094` 只认领一行，剩余唯一 slot 遇 quota，pending 仍只有一条数据行。
   工单须指定构造“两条成功数据行、认领一行”的独立 pending；例如从正向 gen 复制台账和证据，调用三参数续跑接口。
   深验向量可加入 `:972-993` 现有篡改循环，每次从原始字节出发、同步 size/sha256，并恢复 ledger 与 bundle。
   恢复后再次断言深验 PASS；续跑向量同样恢复原件，避免破坏 `:1100` 快照及 `:1110` 已提交前缀断言。

4. **文档白名单自相矛盾。**
   工单 §0.2 仅允许修改 `header.adopted.source` 一行，§4 又要求修改 `rows` 行。
   实际位置为 `references/scan-schemas.md:1007-1008`；白名单应明确允许这两行。

5. **P2 消化方向正确，但成本示例不完整。**
   `CHANGELOG.md:7` 要求成本指标与质量指标，因此不能照盲审建议直接删除成本记录。
   工单示例仅给轮数、施工段数和网络调用数，未体现质量指标，也未交代 Bash 调用数、交付用时；“施工 3 段”还保留了拟删除的施工分段叙述。
   应改为有证据的整版成本与复核结果；缺失数据明确写未统计，不保留 `n` 占位或无依据数字。

其余定位已用 `nl -ba`／`grep -n` 亲核：
- 生产者 `:783-814`、`:800`、`:807`、`:860-861` 均匹配；`:829`、`:835-836` 与 `:880` 证明合法 source 恒为前代摘要目录名。
- 深验 `:1341-1353`、`:1387-1392`、`:1518-1527` 及所列锚文本均匹配；source 等式受前置 digest 格式检查保护。
- E27(d) `:775` 是调用入口，函数正文从 `:820` 开始，结束锚在 `:1161`。
- CHANGELOG `:101-107` 匹配；producer_history `:235-262` 包含四条记录的 sha/commit，第四条完整记录结束于 `:266`。

本次为静态工单复核，未执行会写临时文件的测试，不将其记为回归通过。全程未改文件、未 commit、未读取禁读目录；前后工作树均为空。

**v2 修订清单**

1. 修正候选构造行号，明确深验列表初始化及收集位置。
2. 明确两行 pending 的构造、篡改隔离、原件恢复和恢复后正向断言。
3. 将 schema 白名单扩为 `rows`、`source` 两行。
4. 重写整版成本与质量指标示例，去掉施工分段及占位数字。
