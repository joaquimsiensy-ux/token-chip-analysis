# 只读复核任务 r4：工单 W1 v4

纪律：**只读**，不改文件、不 commit；禁读 `~/.codex`（启动自动披露除外）、`~/Documents`、`~/Desktop`。分支 `fix/solana-txv1` 克隆。

材料：`maintenance/repair-20260923-solana-txv1/workorder_W1_v4.md`（施工工单）、`review_W1_r3_report.md`（你上一轮 r3 报告，含 5 条 v4 修订清单）。

任务：核对 v4 是否逐条正确吸收 r3 的 5 条修订，以及有无引入新问题。**报告作为最终回复文本直接输出**（不要经命令打印），首行 `# 复核 W1 r4: 通过 / 退回`。若仅剩措辞级问题，判「通过」并把措辞建议列在末尾供施工时一并处理；只有会导致施工做错或做不出来的问题才「退回」。结尾给「v5 修订清单」（通过则写“无”）。
