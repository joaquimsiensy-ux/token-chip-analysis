# 复核 W2 r2: 退回

4 条修订中，3 条已吸收，第 2 条仍缺续跑向量的明确隔离、恢复要求；相关源码锚点与实况一致。

1. **候选构造与深验位置：通过。** §2 已修正为生产者 `:607-608`、`:626-629`；深验在 `:1369` 前初始化、`:1387` 分支收集、`:1520` 块内比较，均与源码一致，并明确保留文件行序及连续性、唯一性检查。

2. **测试前置条件与恢复：部分吸收。** §3 已正确指定独立两行 pending、三参数接口及两个 completed slot 的正向断言；深验向量也明确逐次恢复 ledger、bundle，并在恢复后断言 PASS。但 `workorder_W2_v2.md:27` 只说“同样两种篡改”和“未篡改的复制品”，没有明确续跑的每个向量从原始字节独立开始、结束后恢复，以及恢复后再做正向断言。若第二个向量沿用第一个的乱序状态，可能因前缀错误而通过断言，未实际验证 source 检查。

3. **schema 白名单：通过。** §0.2 与 §4 已统一允许 `rows`、`source` 两行说明列；实际位置确为 `references/scan-schemas.md:1007-1008`。

4. **整版成本与质量指标：通过。** §4 已去除施工分段和占位数字，补入复核、盲审结果、用时口径及 Bash“未统计”。盲审首轮分布、05:13Z 入库时间有对应记录；3 次调用／约 30 credits 明示为调度方提供，本次未独立核验。收官终点及第二轮盲审结果仍须按工单在 commit 前补齐。

本次仅静态核对上述修订及锚点，未运行测试、未改文件、未 commit、未读取禁读目录；两次工作树检查均为空。

**v3 修订清单**

1. 在 §3 续跑路径明确：每个 mutation 均从复制品的原始台账字节开始；断言拒绝后恢复原始字节；恢复后调用 `load_resume_slots(pending, repair._ledger_header(plan), plan)`，断言不抛异常且 completed 等于原两个 slot 的集合；再进行下一向量。
