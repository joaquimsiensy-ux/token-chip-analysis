# 工单 B 消化轮 2 盲审复核报告（第三轮，对 6d49b04）——判定 CLOSED

> 入档件。盲审员（opus 子代理）约 70 攻击点。判定：**CLOSED**——B-01~19 全部实测闭合，每处新闸配会咬的负向锚；穷举/边界外码位/伪造收据/异形文件三路线均未打穿。三条残留均缺陷/观察级非本轮引入，建议随版本收口轮登记处置（不烧升格裁决额度）。

## 逐项判定

- **B-16（P0 回归）CLOSED**：四组直验 35 场景全对——二轮击穿 9 场景（≥/≤、↑/↓、≈/≠、俄文/阿拉伯语义反转、📈/📉、Δ/σ、‰/%、中文升降）全部正确判不同；零渲染 15 场景全归一；NFC/NFD 边角 6 场景（café/越南 tiếng/日文が/韩文한/U+3000/NBSP）全正确；未移除类别 5 场景（Co/Cn/Sk/Lm/So）全保留＝fail-closed 方向落地。负向锚削除转 4 红。
- **B-17 CLOSED**：重排版副本拒（finalize＋消费侧独立双拒）；同 entrypoint 跑两次（三 sha 全不同）冒充 2 路拒——语义锚正中设计目标；同 sha 改名拒（按内容不按路径）；真 2 路绿例不误伤。负向锚双侧生效。
- **B-18 CLOSED**：remove_any 六形态（FIFO/socket/断链 symlink/指向目录 symlink/非空目录/嵌套树）全零残留；只删链不删目标（victim 保留）。
- **B-19 CLOSED**：协议语种覆盖面/俄文阿拉伯文纯 emoji 会拒/外语变通/id 无空格/存量迁移动作全在位。docstring 豁免按裁决评估。
- **一轮全部发现保持 CLOSED**：主击穿三点全拒；wave1 38＋wave2 26 全量重放零回归；四原始负向锚仍咬中；"接受"项全部为已登记不修项无新增。

## 残留（登记版本收口轮，不构成 REOPEN）

- **R-1** 对账键 Mn/Me 全类无差别移除的窄口：泰文 กิน(吃)/กัน(互相)、阿拉伯 كَتَبَ(他写)/كُتِبَ(被写)、天城/藏/希伯来 niqqud 对、RLO 反转、键帽 1⃣/1 共 7 组撞键——组合符在这些文种承载语义且可见。项目实际语料（中英日韩拉丁含越南语）NFC 后全保留零撞键；语种边界已进协议。收口轮两处文档修正：代码注释改口（"only known zero-rendering" 与事实不符——Mn/Me 全类含可见组合符，属刻意取舍）＋协议加"命题文本不得靠泰/阿/印/藏/希伯来附加符承载语义"。
- **R-2** completeness_critic 未挂 entrypoint 语义锚：同一 critic entrypoint 跑 3 次 → reviews=4 双侧全绿——注水通道从 skeptic 挪到 critic。不削弱实质校验（覆盖只由 skeptic 贡献、blocker 须结清、artifact 仍去重），缺陷级。一行修：锚扩全角色或 critic 恰好 1 路（协议本写"＋1 完整性批评"）。
- **R-3** 只读目录（0o500）staging 残留且原始 BLOCK 理由被 OSError 掩盖：需 entrypoint 主动 chmod 才触发。修法 `rmtree(onexc=先补权限)` 或残留二次校验。

## 攻击清单

对账键 35＋边界外 9；语义锚 6 式＋critic 探测；remove_any 7 形态＋误删验证；破坏性注入 7 组各有专属锚；一轮 64＋二轮 26 全量重放；run_all 全绿。零 git 写命令，快照隔离。

## 盲审员结语（原文要义）

B-16 的修法方向纠正是对的：**实义判定用白名单、对账键用黑名单**——两个方向相反的安全需求分开处理，此元教训建议进 casebook。
