# W3 施工工单：`stage2_closeout.py reseal`＋手册净减＋CHANGELOG/VERSION 7.1.0

> 版本 **v16**（2026-09-16，融合 codex 对 v15 的十五复两条必改与两处订正，原文 `scratchpad/cx_reply_w3v15.txt`；v15 融合十四复 `cx_reply_w3v14.txt`；v14 融合十三复 `cx_reply_w3v13.txt`；v13 融合十二复 `cx_reply_w3v12.txt`；v12 融合十一复 `cx_reply_w3v11.txt`；v11 融合十复 `cx_reply_w3v10.txt`；v10 融合九复 `cx_reply_w3v9.txt`；v9 融合八复 `cx_reply_w3v8.txt`；v8 融合七复 `cx_reply_w3v7.txt`；v7 融合六复 `cx_reply_w3v6.txt`；v6 融合五复 `cx_reply_w3v5.txt`；v5 融合四复 `cx_reply_w3v4.txt`；v4 融合三复 `cx_reply_w3v3.txt`；v3 融合二复 `cx_reply_w3v2.txt`。**W2 已合入 82bb26c，行号已校**：`a4_gate.py` 本单引用行未变（W2 新增全在 :574 之后）；`stage2_closeout.py` 实物 `build_closeout_case`（`test_stage2_closeout.py:81`）／`--receipt-only`／`skill_commit`（`:68-73`）已核）。W1 落地事实：reopen 回执是案根 `distribution_reopen.json`（schema `distribution-reopen/v1`，`cycles[]` 严格递增），`reopen-cycle` 会把 `a4_seal.json` 与其 registry **复制**进 `data/stage2/dist_cycle<N>/a4_snapshot/`，拒绝 `charts/final` 含终态图以外的文件、拒绝台账引用 `dist_rounds/` 之外的文件、拒绝 `terminal.final_chart_path` 非终态图路径；成功后打印 NORMAL/LOW_SAMPLE 与 ABNORMAL 两条分支提示（`holder_distribution_scan.py:1309`）。W2 契约（v6）：`stage2_closeout.py check|fill-workorder|amend`、`a4_gate.py downstream-check`（exit 3 有过期项）、收据 `stage2_closeout_receipt.json`。依据已批计划 `plan_v2_approved.md` §2.3、§2.8、§4、§5。行号以 W2 合入后的实物为准，本单只给 764b60c 基线锚，施工方开工先 `grep -n` 锚文本重定位。
>
> v3 相对 v2 的改动（codex 复核逐条）：①迁移检测提到 A0 第 0 步（拒绝后全案不变）；②分布裁决改调 `distribution-validate`（freeze 同款 `handoff_manifest.py:1480-1482`）；③补 final-source（ABNORMAL）分支＝先建非终态轮再 finalize，旧 seal 引用已归档路径即停；④轮号一律 `len(rounds)+1`（`:909` 错轮号会把输出写成 data_broken `:1005`）；⑤verdict 停点给出恢复路径；⑥`casebook_note` 为 null 时省略参数；⑦canon 写成固定序列化；⑧解释失败原因从 stdout＋stderr 一并透传，waiver 须 ≥ 第 2 轮（`:1354`）；⑨手册 :136 替换边界收窄、保留 `holder_distribution_current.png`（`CT-DISTRIBUTION-15`）与 `a4-seal/v4`（`CT-DISTRIBUTION-16`）、sealed 读取申报仍人工必填、:117 悬空引用同步、:180 补 `amend`、:94/:154"无 validator"同步；⑩白名单加 `pyproject.toml`；⑪A1 用例改用 `test_handoff_manifest` 可 freeze 夹具，用例 1 不覆盖真实 adversarial_review.json；⑫算法绑定改读 provenance 台账；⑬`--verdicts-file` 须案内持久；⑭APU 备份改案外同级目录、dry-run 验收改路径→sha 映射＋目录清单。**v3 头部"存疑"已撤回**：`validate_scan` 虽不单独比 `algorithm.sha256`，但 `:1060` 用当前代码 `build_scan` 重算、`:719-739 semantic_payload` 保留整个 `input_binding`（含 algorithm，只剔 `upstream_receipts` 与 `labels_manifest.path`）、`:1073-1074` 整体比较不等即"scan 语义与独立重算不一致"→ validate exit 2 → `a4_gate finalize` 拒（`:294-300`）。APU initial scan 记扫描器 `44e73d52…`/62,804 B，HEAD `8abcdd16…`/76,337 B，已证漂移；且 APU `provenance_ledger.json` 记 `entity_source_trace.py` `ff4b640a…`/44,747 B，HEAD `e85acee4…`/46,034 B，**reseal 在 A0.1 即 exit 2**（见 §E）。
>
> v4 相对 v3（codex 三复）：⑮A0.1 补顶层 `algorithm.script_sha256` 与 freeze 同源（`handoff_manifest.py:1179-1182`）；⑯子进程一律 `cwd=案根绝对路径`、`--case-dir <绝对路径>`；⑰A0.6 改为**只读分支规划**，final-source 需求按"当前待登记 claims 的 dist-* 集合 vs 案根 initial scan 异常簇集合"判定（`a4_gate.py:278-309` 真实规则），不看旧 seal 标签；预建轮次挪到 A1 之后、A2 之前（新增 A1.5），只对 `--from freeze|a4`，`--from rounds` 只走 A3；⑱解释生成条件改为"当前 scan ABNORMAL 且无新簇"就尝试，旧 `explanation_path` 只作信息；⑲seal-files 缺文件先按 reopen 回执 `archived[]` 映射到归档路径核 sha，claim 文件引用已归档路径＝claims 变更走恢复路径，无法映射才 exit 3；⑳§B 夹具名改 W2 契约的 `build_closeout_case`，A2+ 用例加 `add_reseal_prereqs` 前置（provenance 算法绑定），用例 6 改为 A1 助手函数单测四变体，用例 8 改 `test_distribution_gate.py:337-355` 范式，新增 13；㉑§C :165 保留刀 1 公告申报、:184 保留"少画图仍可能过闸"边界；㉒§E APU 预期改为 A0.1 exit 2，完整 closeout 前提改写。
>
> v5 相对 v4（codex 四复，九组全部采纳）：㉓A0.2 失效表 a4 层补 final scan（它绑 a4_seal sha/revision，`holder_distribution_scan.py:685-709`）；㉔A0.6 分支规划改三态——非终态有轮次时 finalize **一律**选最新 final（`a4_gate.py:288-290`）与 `D==I` 无关，`--from freeze` 且 revision 增加须 A1.5 追加一轮；`--from rounds` 遇 terminal 且 seal 封入轮次文件 → 归档前 exit 3 改走 `--from a4`；㉕A1.5 先判终态再比 F/D；㉖新增 A2.0：claims 引用已归档文件的识别提到 canon 比较之前（`validate_claim_rows` 对缺失路径直接抛错，`case_paths.safe_case_file` 默认 `must_exist=True`），旧 claims 无法 validate 时视为须 register；㉗A2.3 只扣除本次 finalize 会自动并入的文件，`claim_registry.json` 仅 independent-audit 才自动并入（`:391-392`），new-analysis 下不得剔除；㉘A3 `new_clusters` 与 record-round 同源计算（`:1336-1339`），scan 文件无此字段；㉙§B `add_reseal_prereqs` 须写完整合法 NO_FLIPS 台账，7b/5 显式预置三图，12b 保留＋12c 独立，13 拆 13a/13b 并直接调 finalize 断言 stage；㉚§C :176-177 补日期消费句（W2 v6:46）；㉛§E FORGGIE 改为钉版下 PASS／HEAD 下预期 BLOCK，APU 恢复顺序精确化（initial→handoff→provenance→freeze→A4/rounds→旁车→closeout）；㉜实现约束：`--claims-file/--verdicts-file` 主/子进程同一绝对路径基准，`skill_commit` 用 W2 实物在仓库解析。

> v6 相对 v5（codex 五复，四条必改全部采纳，源码逐条核实）：㉝A0.4 `--from rounds` 前置改为"seal **全部封口依赖**（`sealed_files[].path` ∪ `registry.path` ∪ `verdicts.path` ∪ `distribution_claim_source.path`）∩ reopen **实际搬运集合**"，不按目录前缀猜——verdicts 允许放案内任意位置含 `dist_rounds/` 且不进 `sealed_files`（`a4_gate.py:407`），完整性检查含 registry/verdicts 专用字段（`:594-595`），reopen 搬走 `dist_rounds/` 全部文件（`holder_distribution_scan.py:1240-1247`）；A2.2 旧 verdicts 已归档时按回执映射取回；㉞A2.3 扣除改为**本次选中的来源路径**（`:393` 只加入本次 `distribution_source["path"]`），旧来源不同则作附加封入保留走映射，不另设淘汰规则；㉟A2.4 台账非终态时 `terminal_row()` 抛"指针不唯一"（`:663-668`）使 `rounds.a4_seal_sha`/`rounds.entity_freeze_revision` 必 stale → 原文"仍有过期项即 exit 3"会把 A1.5 预建轮与合法续跑一律截断；改为 reseal 编排层调 `a4_gate.downstream_stale()` 函数（`:627`，不改 `a4_gate.py`），台账非终态时这两项降为"待 A3"不计停点，其余 stale 仍 exit 3，A3 终态后由 A4 收尾 `stage2_closeout check` 完整核验；㊱13b 命令补必填 `--seal-files`（`:722` required=True，缺则 argparse exit 2）与覆盖全 id 的 verdicts；新增 13c 贯通例"已有非终态轮 → finalize → 不在 A2.4 停 → A3"；A2.1 补 register 本身失败 exit 2 透传、不得打印"registry 已变"。

> v7 相对 v6（codex 六复，三条必改全部采纳，源码逐条核实）：㊲A0.4 交集检查**前移到 A0.3 之前**（A0.3 已先搬走 `charts/final/` 其他图，之后再判交集不可能"全案不变"），搬运集合 M 并入 A0.3 自身搬走的 `charts/final/` 非终态图文件；措辞改为 `:594-595` 遍历 sealed_files＋registry＋verdicts、来源路径 `:613-616` 须在封口集合内、另有 `validate_revision_chain()`；㊳A0.6(b) 补**最新 final 有效性只读验证**：final scan 的 `input_binding.final_bindings["a4_seal.json"].sha256` 绑生成时的 seal，此前若已 finalize 换过 seal，即使 `D==F_old` 且 revision 未变，finalize 的独立重验（`a4_gate.py:294-300` → `holder_distribution_scan.py:1060-1074` 语义比较）也必拒 → 失效即 A1.5 追加一轮，不得直接"进 A2 不预建"；㊴13c 起点重构：从 13b 的 register 停点分叉（**不做** 13b 的直接 finalize，否则 round 1 已绑旧 seal，reseal 的 finalize 必 exit 2），只 finalize 一次；解释失败前提改为"解释 CLI 会生成文件，但 claim `dist-X` 无有效 `distribution_explanation` 致五判据 BLOCK"；scan 绑定字段位置改为 `input_binding.final_bindings["a4_seal.json"].sha256`；A2.4 措辞改"延期到 A3 终态验证"并补 §B 14a/14b 负例（终态 seal sha 错／freeze revision 错 → `stage2_closeout check` BLOCK，W2 实物 `:514-519`）。
> v8 相对 v7（codex 七复，三条必改全部采纳，源码逐条核实）：㊵A0.6(b) 两判据（seal sha／freeze revision）只是必要条件——final 绑定八项文件与快照/阈值/规则全进 `semantic_payload`（`holder_distribution_scan.py:685-688`、`:719`），改为**直接调 `validate --expected-stage final` 完整重验**（`python -B`）；A0 只做规划与当前有效性预判，**终判挪到新增的 A1.5(0)**（A1 后执行，`--from a4` 亦做）；dry-run 不得调 freeze 试探（no-op 前仍跑 provenance 重放写临时文件，`handoff_manifest.py:1308`）；㊶A0.3 交集检查的 M 并入 **A0.5 预计搬走的 `distribution_adjudications.json`**（其绑定 scan ∈ M 即必搬；它可作附加封入件）＋子目录展开为文件；㊷A2.2 verdicts 比对改**仅共有 id**（新增 id 须属于已登记 claims，全集由 finalize 校验），否则 13c 会在 A2.2 提前 exit 3；小订正：13c 的 round 1 状态是 REQUIRES_A4_REFLOW；14a 改 `rounds[]` 对应行而非顶层 terminal，14a/14b 断言具体 stale 项名；§B 标题计数改十四组。
> v9 相对 v8（codex 八复，两条必改全部采纳，源码逐条核实）：㊸A0.3 前置改为**所有 `--from rounds` 都执行**（非终态时本步仍搬 `charts/final/` 其他图，seal 附加封入件/专用 verdicts 可在其中，`a4_gate.py:406`），M 拆 **M3**（本步搬图）∪ **M4**（终态 reopen 搬运，非终态 ∅）∪ **M5**（adjudications 仅当 `source_scan.path ∈ M4`，`adjudication_validator.py:255`）——v8 的"绑定 scan ∈ M 即必搬"与 A0.5 实际条件不等价（反例：`source_scan.path` 指向 `charts/final/` 下文件，被 M3 搬走但 A0.5 不搬、走 exit 2）；A0.5 改按**本次** A0.4 moved 回执判断；㊹A2.2 共有 id 旧值直接读 `a4_seal.json.claims[]`（`a4_gate.py:472-476`），回执映射只用于"未给"分支复制，措辞改"相对旧 seal 新增、当前 registry 已登记"；订正头部版本行（v8 时漏改）。
> v10 相对 v9（codex 九复，两条必改＋两订正全部采纳）：㊺A0.3 新增**裁决前置三分**（所有 `--from` 都做、不以是否属于 S 为条件）：`source_scan.path` 解析得 p 并**保存**供 A0.5 用（`case_paths.py:9` 默认 `must_exist=True`，搬走后再解析必抛错）；p ∈ M4 → M5；p ∈ M3∖M4 → **exit 3 全案不变**（v9 会先搬图再在 A0.5 失败）；p ∉ M → 只读预验 `distribution-validate`，不过 exit 2 全案不变（非终态不跳过校验：不需要归档裁决≠不需要验证裁决）；㊻A0.5 改用保存的 p，p ∈ 本次 reopen 回执 moved 集合直接归档、不读历史回执补位，p ∉ M 复验作断言；㊼dry-run 退出码改**同因同码**三态（0／2／3，v9 的"exit 0，前置不过仍 exit 2"与 A0.3 的 exit 3 自相矛盾）；㊽行号订正 `adjudication_validator.py:255`（schema 错误行）→ `:256` 读 `source_scan.path`、`:257` 调候选校验；§B 新增第 15 组（15a–15e）。
> v11 相对 v10（codex 十复，两条必改＋三订正＋一表达订正全部采纳，源码逐条核实）：㊾A0.3 三分改按**台账终态/非终态**划分：终态下 M4 外的任何 final scan（含案内逐字节副本 `evidence/final_scan_copy.json` 这类 p ∉ M 路径）在 A0.4 reopen 后**必**失效——validate 从**当前** `distribution_reopen.json` 重建 `reopened_from_cycle`（`holder_distribution_scan.py:1066-1068`，A0.4 写入 `:1294-1295`）、台账搬走后 `validate_round_binding` 不再相邻（`:949-958`），v10 的"(iii) 预验通过后 A0.5 复验失败属不可预判"是错的，这是 reseal 自己计划动作造成的可预判失效，改为**终态且 p ∉ M4 → 前置 exit 3 全案不变**（并入原 (ii)）；(iii) 只读预验只剩**非终态且 p ∉ M3**；恢复提示删"作附加 `--seal-files` 封入 seal"（不改变 p 的归属，重试仍被拒）改为人工处置；第三分支写 p ∉ (M3 ∪ M4) 不用 M5 自指；㊿15d 重构：先按 `test_reopen_cycle.py` 范式造真实历史终态并真 reopen，再生成当前周期非终态 round 1（绑定该回执），再 template 并按 `test_distribution_gate.py:367-380` 填 `adjudicated_at`＋全部候选 `excluded_members`（非空 reason、`accepted_members=[]`），先断言 `distribution-validate` 通过再测不搬——v10"只填 `candidate_verdict`"过不了 validator `:272-274/:293-309`，"生成当前 scan 后再塞历史回执"会让当前 scan 失效（validator 按回执重建 `reopened_from_cycle`）；新增 15f 覆盖十复反例（终态、裁决绑案内 scan 副本、预验本可通过 → 仍前置 exit 3）；第 15 组明确纳入 `add_reseal_prereqs` 适用清单；零写入覆盖 Python 子进程链：编排层子进程统一 `-B`＋`PYTHONDONTWRITEBYTECODE=1` 环境变量继承（validator `:197` 再起的扫描器不带 `-B`）；dry-run 零写入验收加仓库 `git status --short` 为空（案根映射抓不到仓库内 `__pycache__`）。
> v12 相对 v11（codex 十一复，两条必改＋四订正全部采纳，源码逐条核实）：51 A0.3(iii) 新增**依赖交集检查**：裁决校验依赖不止 scan 路径 p——`distribution-validate` 内部调 `validate --expected-stage final`，后者重验 scan `input_binding` 全部带 `path` 的绑定条目（快照可显式指定为案内任意已登记路径，`holder_distribution_scan.py:196-197/:224-233` 不禁止 `charts/final/`；`:1028-1032/:1036-1047` 重验）、八项 final 绑定（`:685-688`）、initial scan、台账、reopen 回执，`--entity-file` 传入时另读；定义 D(p) 并要求 D(p) ∩ M3＝∅，命中 exit 3 全案不变（十一复反例：快照登记为 `charts/final/owners.json`、裁决绑 `dist_rounds/` 下 scan，v11 只查 p 会预验通过→搬快照→A0.5 必失败，v11 的"失败只能来自外部改动"不成立）；A0.5 失败文案改"预验后依赖失效"、不归因；(ii) 依据删"台账搬走必不相邻"（`:949-952/:981-985` 对无台账且原前置台账为空的 round 1 放行，不普遍成立），只留 reopen 绑定重建这一条。52 dry-run 零写入补**主进程**：`sys.dont_write_bytecode = True` 置于本地模块导入之前（`stage2_closeout.py:25-31` 在命令分派前导入，`-B`＋环境变量追溯不到主进程）；`:31` 顶层 `import figures_from_facts` 延后到使用函数内（`figures_from_facts.py:56`→`standard_charts.py:37-39` 顶层拉 matplotlib，缓存缺失时会建配置目录并写字体缓存）；明列避开 `check` 收据（`:798`）、`_atomic_json` 临时文件（`:94-103`）、`reseal_log`/`_history/`、各落盘子命令；验收改为**含忽略项**（`.gitignore:4-5` 忽略 `__pycache__/`、`*.pyc`，`git status` 看不见）的案根＋仓库 `scripts/` 路径→sha 映射＋目录清单前后比较，加 `MPLCONFIGDIR` 指向空目录、跑后仍空——v11 头部写的"仓库 `git status --short` 为空"既不可靠也未落进正文，撤回。53 已知边界改写：A1 改 freeze **及 A2 finalize 改 seal** 后旧裁决不处理 → 下次 freeze 必被前置拦（`handoff_manifest.py:1479-1486` 裁决在场先校验、失败直接返回不进 no-op 判断；`:1581/:1590-1596` 裁决哈希纳入 freeze）、下次 reseal 可能被 A0.3 预验拦；本次后续层可继续（`a4_gate.py:274-308`、`a5_report_seal.py:270-283` 不要求旧裁决重过 `distribution-validate`）；写明恢复步骤，不承诺"template 后 freeze"是长期稳定态。54 15d 重写：删"NORMAL 记为不带解释 UNEXPLAINED"选项（`holder_distribution_scan.py:1341-1345` 先判终态，normal→reopen→record 仍 NORMAL 终态）；主路线用 `prepare_explanation_case` ABNORMAL＋`test_reopen_cycle.reopen_abnormal_round1_unexplained_then_round2` 前半段（`:258-264`）得真正非终态已登记轮；备选"不 record"须明确断言范围（A3 会生成 round 1 覆盖现有 scan，不断言裁决最终有效）；禁对同一案根叠加 `normal()` 与 closeout 夹具（`add_final_inputs` 覆盖 facts/state/freeze/三账/简化 seal，`test_distribution_gate.py:87-105`）；15f 用 `build_closeout_case`＋`add_reseal_prereqs`；成员断言固定"等于 scan 对应候选全部 `members[].owner`"（`adjudication_validator.py:209/:238`），禁缺字段默认 `[]`；新增 **15g** 覆盖十一复快照反例（含去掉交集检查后必失败的反向对照；造案不 reopen 不 record——`reopen-cycle` `:1236-1239`/终态物化 `:1381-1382` 拒 `charts/final` 含其他文件）。

> v13 相对 v12（codex 十二复，五条必改全部采纳，源码逐条核实）：55 A0.3(iii) D(p) 改为"validate 实际打开的全部案内文件"：`input_binding` 下 `path` **递归提取**（不限五类字段，`_walk_entries` `:213` 同款）并区分案内/仓库路径；**新增当前台账匹配轮的 `recorded.final_scan_path`**（`validate_round_binding` 另行打开算哈希 `:956-960`、不要求等于 p——十二复反例：登记 scan 在 `charts/final/`、裁决绑其案内副本，v12 会预验通过→搬走登记件→A0.5 失败）；补最新 reopen 回执 `archived[].to`/`a4_snapshot`（`:1123-1172`，正常不命中 M3）与裁决文件自身（`adjudication_validator.py:253`）以求完整。56 dry-run 验收：`MPLCONFIGDIR` 空目录只作独立验收、**不作"未加载 matplotlib"证明**；用例 7 加 (d) `-c` 包装 `sys.modules['matplotlib'] = None` 导入阻断＋事后 `sys.modules` 断言，子进程链改静态 AST 断言顶层无 matplotlib 导入（`holder_distribution_scan.py:809` 在函数内）。57 A0.0 补缺件策略：`accounting_mode.json` 不在场 → 跳过迁移检测并打一行说明，不造 `{}` 假收据（正式 EVM 收据要求 observation bundle 真实绑定 `shared_release_receipt.py:1740-1762`）；S 提取缺项按空集、明写不是 seal 完整性校验（`a4_gate.py:594-617`）；15d 最小夹具的 A0.0/A0.1/A0.2/S 前置逐项写定。58 15g 明写 initial 须显式 `--snapshot charts/final/owners.json`（默认发现只认四个候选名 `:188-189/:204-210`，照搬 `prepare_explanation_case` 的 `run_scan(root)` 必失败 `test_distribution_gate.py:110`）；final 自动从 initial 绑定取 `:198-203`。59 15d 删"最终在 ABNORMAL 分支 exit 3 属预期"：该夹具解释仍完整有效（`:114-133`），不带解释 record 不动 claims 解释（`:1348-1364`），A3 重跑解释器（`distribution_explanation_check.py:95-146`）预期 EXPLAINED；断言范围限定 A0.3–A0.5，之后不断言。

> v14 相对 v13（codex 十三复，四条必改全部采纳，源码逐条核实）：60 A0.3(iii) D(p) 的路径分类改为**按字段解析基准**：`algorithm.files[].path` 是仓库相对路径（`rel_entry` `:132-134`、`:664`），`labels_manifest.path` 是宿主 checkout 绝对路径且语义比较剔除（`:632-633`、`:728-732`），两者不入 D 不查存在；其余 path 按 `safe_file` `:120-128` 规则入 D，案根相对串比较不额外 resolve；台账/回执合法缺席不拦（v13 "相对路径入 D、仓库外绝对路径 exit 2"会把算法相对路径当假案内依赖、把旧 checkout 的 manifest 路径误拒）。61 用例 7(d) 改为 `PYTHONPATH` 注入 `sitecustomize` 的 meta_path finder（记录尝试并抛 ImportError），主进程与全部子进程同一机制（已实测父子均拦），删 `-c`/`runpy` 包装与 AST 静态断言（入口顶层无 matplotlib 不覆盖导入链，`handoff_manifest.py:43-51`）；新增 7(e) 干净 checkout 验收（`git status` 干净≠无 `__pycache__`）。62 A0.0 `accounting_mode.json` 缺失或非法 → exit 2 全案不变（缺件跳过让删掉该件的复制案绕过写入前拦截；`check` 本就必需 `audit_release_gate.py:29`）。63 15d/15g 不经 CLI，直接调 A0.3–A0.5 可导入函数；15d 成功判据加"确实到达 A0.5"（`stage_done`＋返回列表含 p）。64 dry-run 节措辞改"dry-run 主进程及全部子进程不得加载 matplotlib；正式 reseal 编排主进程不加载，A3 scan 子进程生成分布图允许"（`:1001`→`write_png` `:808-809`）。

> v15 相对 v14（codex 十四复，三条必改与五处订正全部采纳，源码逐条核实）：65 A0.3(iii) D(p) 比较表示改定：`safe_file` 实际只拒绝绝对路径、`..` 与**文件自身**为符号链接，随后 `resolve()` 并确认仍在案根、**不逐层拒祖先目录符号链接**（`:120-128`），与 `case_paths.safe_case_file` 逐层检查（`:16-35`）是两套规则、不得混称；比较改为"先按各字段消费者校验原始值，再把校验返回的已解析文件路径转案根相对 POSIX 串"，M3 用同一文件身份表示，校验前不归一化非法输入（十四复反例：`recorded.final_scan_path` 由扫描器 `:958` 解析，案内目录别名会让 D 记别名、M3 记真实路径而漏交集）；"算法绑定由 A0.1 核"改为"仓库路径，由 scan 重算比较核验"（A0.1 核的是 provenance 三份算法文件）；补十四复核过的各字段基准表（全案根相对、无第三类）。66 A0.3 归档执行改为：M3 仍展开为文件集合做依赖检查，实际归档把 `charts/final/` 下终态图之外的**文件与子目录整体** `shutil.move`（含空目录，不 rm）——只搬文件会留空子目录，`reopen-cycle` `:1236-1239` 见任何其他条目即拒、且此时图已搬走；补用例 7(f)（含子目录与空目录）。67 用例 7(e)/§E APU 改为：基准 worktree 叠加完整 W3 待交付修改与新增测试并核文件哈希后验收，要求"无缓存、无非预期文件"而非 git 全净（v14 从 HEAD 建 worktree 与 §0"不 commit"冲突，验收不到施工件：HEAD 的 `stage2_closeout.py:761` 尚无 `reseal`）。68 用例 7(d)：`PYTHONPATH` 追加原有值，普通组与阻断组基础环境一致、仅阻断组多专用目录；父子阻断实测固定为验收自检 7(d0)；新增 7(g) 真正进入 `distribution-validate → validate` 链的非终态含裁决件 dry-run（7b 无裁决、APU 在 A0.1 提前退出都覆盖不到这条链）；dry-run 调 `downstream-check` 必须省略 `--json-out`（`a4_gate.py:706-708` 有它就写文件）。69 15d "按 A0.4 规则自算"→A0.3（A0.4 对应 M4）；返回列表命名 `prevalidated_scan_paths`；停点"先输出同一诊断再 `raise SystemExit(code)`，不把字符串作参数"；15d/15g 表述为"不经过 A0.0"；完整夹具能过 A0.0 的依据加引 `test_stage2_closeout.py:46` 重绑与 `test_a4_gate.py:170-178`。

> v16 相对 v15（codex 十五复，两条必改与两处订正全部采纳，源码逐条核实）：70 用例 7(d0) 自检与正式验收的标记文件隔离：d0 用独立标记文件并保留证据，7(b)/7(g)/§E APU 每次阻断运行换新的 `MPL_BLOCK_MARK` 路径、测量开始前断言不存在或为空（v15 同一文件"恰两行"与"为空"互斥必败）。71 用例 7(e)：验收 checkout 的 `os.walk` 文件清单允许根目录 `.git` 管理文件（`git worktree add` 必生成），目录清单按允许文件父目录另核；worktree 内直接启动其自身测试与 CLI 生成夹具，`add_reseal_prereqs` 绑定验收 worktree 自身的三份算法路径（`handoff_manifest.py:818-820` 先比 realpath，主工作树同字节文件不能替代）。72 订正 `safe_file` 引用 `:120-128`→`:120-129`（返回在 `:129`，返回 resolve 后路径）；D 与 M3 统一以 `case_dir=Path(args.case_dir).resolve()` 为基取相对串，解析落案外 → exit 2；`charts/final` 搬空只保证 `:1238-1239` 过，`:1236-1237`（`checked_dir` `:1185-1193` 逐层拒目录符号链接）不由此证明，7(f) 只证消除残留条目。73 §E APU：A0.1 诊断保留真实路径比较（绝对路径不一致可能先于 sha 触发），不得包装成只做内容比较。

## §0 开工纪律
同 W1/W2：只改白名单——`scripts/report/stage2_closeout.py`（加 `reseal` 子命令）、`references/split-run.md`、`references/analyze-workflow.md`、`references/report-template.md`（本单**不改**它，列出只为 `wc -c` 验收）、`CHANGELOG.md`、`VERSION`、`pyproject.toml`（仅 `project.version` 行 `7.0.4`→`7.1.0`，`test_version_consistency.py:14` 读它）、`SKILL.md`（仅版本标记行 `<!-- skill-version-source: VERSION; skill-version: 7.0.4 -->` → 7.1.0，`test_version_consistency.py:22`）、`scripts/tests/run_all.py`、`scripts/tests/invariant_manifest.json`（仅登记 reseal 新增的 `os.replace` 写入函数 `overwrite_single`，格式与去重规则同 W2 §0.2；不得删改既有行）、新建 `scripts/tests/test_stage2_reseal.py`。禁改 `handoff_manifest.py`、`a4_gate.py` 既有函数、`holder_distribution_scan.py`、`a5_report_seal.py`、`distribution_explanation_check.py`、`adjudication_validator.py`（只调用）。顺序 A（reseal）→ B（测试）→ C（手册）→ D（CHANGELOG/VERSION/SKILL/pyproject）。先红后绿证据 `w3_red_evidence.txt`，完工 `w3_done.md`，不 commit。开工前工作树须干净（`git status --short` 空），否则停工写 done 请示；全程不读 `~/.codex`。

## §A `reseal` 子命令

用法：`stage2_closeout.py reseal --case-dir <案根> --report 报告.md --from freeze|a4|rounds [--claims-file <json>] [--verdicts-file <案内 json>] [--seal-files a,b,c] [--dry-run]`。退出：0 完成并已跑 `check`；3 停点（列原因与下一步）；2 前置不过；1 脚本错。全程 stdout 打印"层｜动作｜将失效下游件"表（dry-run 只打印不执行）。案根 `case_dir = Path(args.case_dir).resolve()`；所有子步骤用 `subprocess.run([sys.executable, "-B", <脚本绝对路径>, ..., "--case-dir", str(case_dir), ...], cwd=str(case_dir), env={**os.environ, "PYTHONDONTWRITEBYTECODE": "1"}, capture_output=True, text=True)（`-B` 只管直接子进程，`adjudication_validator.py:197` 再起的扫描器不带 `-B`，靠环境变量继承禁写 bytecode，dry-run 零写入才覆盖整条 Python 子进程链；主进程自身的 `sys.dont_write_bytecode` 与 matplotlib 延后导入见 dry-run 节）` 调现有 CLI（下文 `--case-dir .` 一律指此绝对路径，禁止裸 `.`），**不复制其逻辑**；子命令非 0 时把它的 **stdout＋stderr 一并**原样透传（`holder_distribution_scan.py` 的 BLOCK 原因打在 stdout，`:1006/:1355/:1360`）。`--claims-file`/`--verdicts-file` 在主进程 `Path(x).resolve()` 后以**绝对路径**既供自身读取又传给子进程（主/子同一解析基准，禁一处按启动目录一处按案根）；`skill_commit` 直接用 W2 实物 `stage2_closeout.skill_commit()`（`cwd=REPO` 在 skill 仓库解析，`:68-73`），reseal 不得另写，不得因子进程 `cwd=案根` 记成案目录 HEAD 或 null。

### A0 预检与归档（所有 `--from`；第 0-2 步只读，任一失败全案不变）
0. **案根迁移检测**（最先）：读 `accounting_mode.json` 的 observation bundle 路径（`shared_release_receipt.py:367/:1755` 案根包含检查同款），任一绝对路径不在当前案根内 → exit 2"案根已迁移：observation bundle 绝对路径指向原案，reseal 须在原路径执行；未迁移重绑的复制案无法通过发布闸"。**缺件策略**（十三复④改定）：`accounting_mode.json` 不在场、或在场但读不出 observation bundle 路径字段 → **exit 2 全案不变**（本单要求不合格复制案在任何修改前停住；缺件跳过会让删掉该件的复制案绕过 A0.0 的写入前拦截，搬图、reopen、重封后才在最终 `check` 被拒；`check` 本就把它列为必需件 `audit_release_gate.py:29`，正式案没有合法缺席；不得为此给 CLI 留缺件豁免或跳过开关、不造 `{}` 假收据——正式 EVM 收据还要求 observation bundle 及真实绑定 `shared_release_receipt.py:1740-1762`；最小扫描夹具无此件，§B 15d/15g 因此不经 CLI、直接调 A0.3–A0.5 函数；完整 closeout 夹具自带案内绝对路径 bundle `test_audit_release_gate.py:276-281`，正例不受影响）。
1. 算法绑定硬约束（与 freeze 前置完全同源，`handoff_manifest.py:1178-1191`）：读 `provenance_ledger.json.input_binding.algorithm`；①顶层 `script_sha256 == full_sha256_file(entity_source_trace.py)[0]`；②`files` 为按文件名键入的字典，对 `entity_source_trace.py`／`wave_scan.py`／`sqd_cache_identity.py` 三项各调 `handoff_manifest.check_algorithm_file(files[name], 当前仓库该文件绝对路径)`（记录形状 `{path 绝对路径, bytes, sha256}`，`:806-829`）；缺 `input_binding.algorithm`、任一不等或缺项 → exit 2"算法文件 <名> 漂移：须在与 freeze 记录同一 checkout 下运行，或在当前代码下重跑 provenance/freeze 链"，并打印记录 sha/bytes 与当前 sha/bytes。
2. 失效表（按 `--from`）：freeze 层＝final scan（各轮）、rounds 全部行、a4（若 freeze revision 变）、adversarial_review/shared_release_receipt（按 registry sha 判，不按 seal revision，`shared_release_receipt.py:50` 输入不含 seal）、a5 seal、工单 bindings；a4 层＝同上（**final scan 同样失效**：它绑 `a4_seal.json` 的 sha 与 revision，`holder_distribution_scan.py:685-709`，A4 重封后旧 final 必被独立重验拒）；rounds 层＝rounds 行、终态图、a5 seal、工单 bindings.rounds/终态图。
3. **前置（在任何归档之前；`S ∩ M` 交集检查仅 `--from rounds`，裁决前置三分所有 `--from` 都做；台账终态与非终态都执行——本步搬 `charts/final/` 其他图的动作非终态时照样发生，seal 的附加封入件或专用 verdicts 可能就在其中，`--from rounds` 不经 A2 修 seal）**：取当前 seal **全部封口依赖** S＝`sealed_files[].path` ∪ `registry.path` ∪ `verdicts.path` ∪ `distribution_claim_source.path`（缺项按空集：S 提取只是路径集合提取，**不是** seal 完整性校验——简化 seal 无 `verdicts`/`distribution_claim_source`，完整校验 `a4_gate.py:594-617` 会因缺项报错，本步不调它；seal 完整性检查 `a4_gate.py:594-595` 遍历前三者，来源路径 `:613-616` 要求在封口集合内，另有 `validate_revision_chain()` 只涉 `a4_seals/` 历史件、不在搬运集合；verdicts 只要求案内合法文件、可在 `dist_rounds/` 下且不进 `sealed_files`，`:407`），与本单**全部归档动作的实际搬运集合** M＝M3 ∪ M4 ∪ M5：**M3**＝本步将搬走的 `charts/final/` 非终态图文件（含子目录时展开为其中全部文件，不登记目录名）；**M4**＝台账终态时 reopen-cycle 将搬走的 `distribution_rounds.json` ∪ `dist_rounds/` 下全部普通文件 ∪ `terminal.final_chart_path`（`holder_distribution_scan.py:1240-1247`，与其 `--dry-run` 列出的 moved 项同源；复制的 seal/registry 快照不算搬走），非终态时 M4＝∅；**M5**＝A0.5 预计搬走项，由**裁决前置三分**（所有 `--from` 都做，不以是否属于 S 为条件）决定：`distribution_adjudications.json` 在场时读 `source_scan.path`（`adjudication_validator.py:256`；`:257` 才调候选校验），经 `safe_case_file` 解析得规范相对路径 p（解析失败 → exit 2 全案不变），**保存 p 供 A0.5 使用**（`case_paths.py:9` 默认 `must_exist=True`，搬走后再解析必抛错）；(i) p ∈ M4 → M5＝{该裁决文件}（scan 随周期归档后 `distribution-validate` 必不通过，A0.5 必搬）；(ii) **台账终态且 p ∉ M4**（含 p ∈ M3、p 为案内 final scan 逐字节副本等一切 M4 外路径）**或非终态且 p ∈ M3** → exit 3"裁决文件绑定的 scan <p> 不随周期归档/将被本步搬走，归档后其绑定必失效且 A0.5 不承接：人工处置（把裁决件移出案根或改绑；终态后由 −2 重新承接裁决）"（尚未搬任何文件，全案不变）。终态下 M4 外的任何 final scan（含预验本可通过的副本）在 A0.4 reopen 后**必**失效：validate 从**当前** `distribution_reopen.json` 重建 `reopened_from_cycle`（`holder_distribution_scan.py:1066-1068`，A0.4 写入 `:1294-1295`）与旧 scan 自报值语义不一致（普遍成立的依据**只有这一条**；`validate_round_binding` 对无台账且 `rounds_before_append` 为 None 的 round 1 放行，`:949-952/:981-985`，"台账搬走必不相邻"不适用于所有 round 1，不作依据）——这是本单自己计划动作造成的**可预判**失效，所以终态**不做预验直接拒**；"作附加 `--seal-files` 封入 seal"不改变 p 的归属（反而会命中下面的 `S ∩ M`），**不作恢复提示**；(iii) **非终态且 p ∉ M3**（此时 M4＝∅，即 p ∉ (M3 ∪ M4)）→ 先做**依赖交集检查**再预验：裁决校验依赖的不止 p 自身——`distribution-validate` 内部调 `validate --expected-stage final`（`adjudication_validator.py:193-201`），validate 重验 scan `input_binding` 里全部带 `path` 的绑定条目（`snapshot`/`data_map`/`supply_truth`/`exclusion_sources[]`/`upstream_receipts[]`，`holder_distribution_scan.py:1028-1032/:1036-1047`；快照可显式指定为案内任意已登记路径，`:196-197/:224-233` 只要求 data_map 唯一登记＋sha 一致、**不禁止 `charts/final/`**），再按 `build_scan` 重算时读八项 final 绑定文件（`:685-688`）、`distribution_scan.json`（initial，`:700`）、`distribution_rounds.json`（`validate_round_binding`）与 `distribution_reopen.json`（`:1066-1068`）；`--entity-file` 传入时 validator 另读该文件。令 **D(p)**＝validate 该裁决时**实际会打开的全部案内文件**（十二复②：不是五类字段清单，按实际读取集合列）＝{p} ∪ **递归提取** p 的 scan 实物 `input_binding` 下任意深度所有 `path` 键值（扫描器自身 `_walk_entries` `:213` 同款遍历；含 `snapshot`/`data_map`/`supply_truth`/`exclusion_sources[]`/`upstream_receipts[]`，也含 `mint_closure_anchor.replay_stats`（`:657-660` 写入）与 `round_binding.previous_final_scan`（`:977-979`）等嵌套项）中的案内绑定条目（十三复②：**按字段的解析基准分类，不按绝对/相对分类**——`algorithm.files[].path` 是**仓库相对路径**（`rel_entry` `:132-134` 以仓库根为基 `:664`，形如 `scripts/report/holder_distribution_scan.py`），不入 D、不查存在（它是仓库路径：扫描器自身的算法绑定由 scan 重算比较核验，A0.1 核的是 provenance 的三份算法文件，两者不混）；`labels_manifest.path` 是**宿主 checkout 的绝对路径**、纯记录信息（validate 读的是当前仓库的 manifest `:632-633`，语义比较剔除该 path `:728-732`，指向旧 checkout 也合法），不入 D、不查存在、不因不在当前案根/仓库而拒；除这两个字段外的 path 条目**先按各自消费者的规则校验原始值**——scan 绑定项用扫描器 `safe_file`（`:120-129` 实际行为：拒绝绝对路径与 `..`、拒绝**文件自身**为符号链接，随后 `resolve()` 并确认仍在案根、**返回 resolve 后的路径**（`:126/:129`，不是原始串）；**不逐层拒祖先目录符号链接**，与 `case_paths.safe_case_file` `:16-35` 的逐层检查是两套规则，不得混称、不得比消费者更严）；台账 `recorded.final_scan_path` 按扫描器 `:958` 的解析、reopen 回执项按 `:1123-1172`、裁决件路径按 `safe_case_file`——校验不通过 → exit 2 全案不变（校验前**不归一化**非法输入）；通过后把**校验返回的已解析文件路径**转为案根相对 POSIX 串入 D（十四复：正常 `rel_entry` 产物直接用原始串没问题，但 `recorded.final_scan_path` 是另行读取的台账字段，案内目录别名会让原始串是别名、真实路径在 M3，按原始串比较会漏交集；按"不经符号链接"全面拒绝又比现有消费者更严）；M3 用**同一文件身份表示**（`charts/final/` 实物同样 resolve 后取案根相对 POSIX 串）比较；两边统一以本单已定义的 `case_dir = Path(args.case_dir).resolve()` 为基做 `relative_to(case_dir).as_posix()`（`safe_case_file` `:32-41` 同样返回 realpath 后的目标），案根自身或祖先为符号链接也不会两边基准不同；"文件身份"指解析后的路径身份、不是 inode，不加硬链接合并规则；解析后落在案外 → 各消费者的包含检查本就失败 → exit 2，不得当作"无交集"（十五复②）。scan 没有名为 `labels_file` 的绑定字段，不照搬；十四复核过的其余字段基准全是案根相对、无第三类：`upstream_receipts[]` `:672-680`（已记录条目另受两个文件名白名单 `:1043-1048`）、`exclusion_sources[]` `:391-410`、`mint_closure_anchor.replay_stats.path` `:345`（scan 内保存的表示；上游 `supply_truth.inputs.replay_stats.path` 可为绝对路径但那不是 scan 内的值 `:279-295`）、`final_bindings`/`handoff_manifest` `:685-707`、`round_binding.previous_final_scan` `:977-979`、`reopened_from_cycle.receipt` `:1175`）∪ `final_bindings` 八项 ∪ {`distribution_scan.json`, `distribution_rounds.json`, `distribution_reopen.json`} ∪ **{当前 `distribution_rounds.json` 中 `round_n == scan.round` 那一行的 `recorded.final_scan_path`}**（`validate_round_binding` 另行打开它算哈希 `:956-960`，**不要求它等于 p**——十二复反例：非终态 round 1 台账登记路径在 `charts/final/registered_scan.json`（record 记的是传入的 `args.scan`，`:1370`），裁决绑其案内逐字节副本 p，v12 的 D(p) 不读台账 → 预验通过→搬走登记 scan→A0.5 失败；台账无匹配行则此项为空）∪ {`distribution_reopen.json` 在场时最新 cycle 的 `archived[].to` 全部实物与 `a4_snapshot/a4_seal.json`}（`:1123-1172`；合法路径受 `data/stage2/dist_cycle<N>/` 前缀限制，正常不命中 M3，纳入是为了 D 是完整读取集合而非猜测）∪ {裁决文件自身}（`adjudication_validator.py:253`）∪ {实际传入的 `entity_file`}（台账无匹配行、回执不在场是合法缺席，不拦；scan、在场的台账/回执读不出或结构不符 → exit 2 全案不变）。**D(p) ∩ M3 非空** → exit 3"裁决校验依赖 <路径> 将被本步搬走，归档后裁决必失效且 A0.5 不承接：人工处置（改绑或把裁决件移出案根）"，全案不变（终态分支 (ii) 已把 M4 外的 p 全拒，无需再对 M4 求交）。交集为空 → 此处**只读预验** `distribution-validate`（命令与参数同 A0.5），不通过 → exit 2 透传、全案不变（不需要归档裁决≠不需要验证裁决），通过 → M5＝∅。已知边界（非终态 `--from freeze|a4`）：A1 改写 `entity_freeze.json`、或 A2 finalize 改写 `a4_seal.json` 后，该 scan 连同裁决件随 A0.2 失效表所列 final scan 一并失效，A0.5 **不自动归档**（reseal 不自动处置裁决）；后果不止本次——freeze 前置在裁决件在场时先跑 `distribution-validate`、失败直接返回不进 no-op 判断（`handoff_manifest.py:1479-1486`），裁决哈希纳入 freeze（`:1581/:1590-1596`），所以旧裁决不处理则**下次 freeze 必被前置拦、下次 reseal 可能被本步预验拦**；本次 A1.5/A2/A3/closeout 本身可继续（它们生成绑定新 freeze/seal 的后续 scan，`a4_gate.py:274-308`、`a5_report_seal.py:270-283` 不要求旧裁决重过 `distribution-validate`；裁决件若是封入件另按 `S ∩ M` 处理）。reseal 在完成/停点输出里打印恢复步骤：**人工归档旧裁决并移出默认案根路径 → 检查它是否被 seal/freeze 封入 → 续跑对应层；重新承接裁决须从绑定新 final 的 scan 重新 `distribution-template`**。不得承诺"重新 template 后 freeze"是长期稳定态：freeze 再变仍使其来源 scan 过期。三分互斥且穷尽：终态按 p ∈ M4 / p ∉ M4，非终态按 p ∈ M3 / p ∉ M3（(iii) 内再按 D(p) ∩ M3 是否为空细分）。然后取交集；`S ∩ M` 非空 → exit 3 逐项列出"归档将搬走 seal 封入的文件 <路径>，seal 会失效：改走 `--from a4`"（此时尚未搬任何文件，全案不变）。**不按目录前缀猜，按路径集合比**。然后 `charts/final/`：把**除 `holder_distribution_current.png` 外**的**全部条目**——文件与子目录**整体**（对子目录 `shutil.move` 目录本身、不逐文件搬，空目录也搬；禁 rm）——移到 `data/stage2/_history/<UTC 时间戳>/charts_final/`（保留相对结构；M3 展开为文件集合只用于上面的依赖检查，不是搬运粒度）；只搬文件会留下空子目录，`reopen-cycle` `:1236-1239` 对 `charts/final` 里**任何**其他条目都拒、而此时图已搬走（十四复③）；分布终态图留给 `reopen-cycle`。搬空只保证 `:1238-1239`"含其他文件"检查通过；`:1236-1237` 的 `checked_dir`（`:1185-1193` 逐层拒目录符号链接）与终态图 `safe_file` 检查、更早的台账/回执/轮次检查不由此证明——`charts/final` 自身是符号链接、终态图缺失或自身是符号链接的案，reopen 照拒，本步不替它兜底（十五复③）。
4. 若 `distribution_rounds.json` 有 `terminal`：调 `holder_distribution_scan.py reopen-cycle --case-dir . --reason "reseal --from <层> <UTC>"`（它会核 `charts/final` 只剩终态图，并把 a4_seal＋registry 复制进周期目录）。非 0 → exit 2 透传。台账在场但**非终态**（续跑场景）→ 跳过本步，后续轮号按 A3.1。**此步必须先于 A2 finalize**：`a4_gate finalize` 见台账 terminal 直接拒（`a4_gate.py:285`），顺序反了就是死锁。**`--from rounds` 前置**已在 A0.3 之前执行（S ∩ M 检查覆盖 A0.3 与本步两处搬运），到本步时交集必为空。
5. `distribution_adjudications.json` 在场时，用 A0.3 保存的规范路径 p（**不再重新解析**——文件可能已被 A0.4 搬走，`safe_case_file` 默认 `must_exist=True` 会先抛错）：p ∈ **本次**第 4 步 reopen 回执的 moved 集合（`mode:"moved"` 的 `from`，即 A0.3 的 M4；非终态时本步未 reopen、集合为 ∅，**不读历史回执补位**）→ 不必再跑校验（scan 已归档必不通过），直接 move 到同一 `_history/<ts>/` 并在 `reseal_log.json`（写在 `_history/<ts>/`，不进案根）记"失效原因＝绑定的分布扫描已随周期归档；终态后由 −2 重新承接裁决"；p ∉ M（**仅非终态可达**，A0.3(iii) 已预验通过）→ 复跑 `adjudication_validator.py distribution-validate --case-dir <绝对> [--entity-file <entity_freeze.json 顶层 entity_file>]`（freeze 前置同款 `handoff_manifest.py:1480-1482`；`--entity-file` 默认 None，`adjudication_validator.py:599`，freeze 桩无该字段时省略；**不是** `validate`，那个校的是 `candidate_adjudications.json`）作断言，通过则不动，不通过 → exit 2 并注明"A0.3 预验通过后依赖失效"，把 validator 的 stdout＋stderr 原样透传，**不归因**（不写"外部改动"：A0.3(iii) 已核 D(p) ∩ M3＝∅，但 D(p) 取自 scan 自报绑定，未经核实不得断言失败来源；不宣称全案不变）。本步搬走的文件恰为第 3 步的 M5，第 3 步通过则本步搬运不触及 seal 封口依赖。
6. **分支规划（只读，仅 `--from freeze|a4`）**：`a4_gate finalize` 的真实规则（`a4_gate.py:278-309`）分两态——台账无轮次（含无台账、或 terminal 将被 A0.4 归档后的新周期）→ 用案根 initial scan；台账在场且非终态有轮次 → **一律**用最新 final scan，与 initial 是否闭合无关（`:288-290`）；再要求所选 scan 的异常簇集合 `{dist-<cluster_id>}` 与待登记 claims 的 `dist-*` id 集合严格相等。取待登记 claims（`--claims-file` 或当前 `a4_claims.json.claims`）的 `dist-*` 集合 D；案根 `distribution_scan.json.abnormal_clusters` 得 I；最新非终态 final scan（若有）得 F_old。规划：(a) **无轮次态**：`D == I` → **initial 分支**（APU 实况：两者皆空）；否则 → **final-source 分支**，A1.5 预建。(b) **非终态有轮次态**：来源必为最新 final（`rounds[-1].final_scan_path`）；finalize 选中后**必跑独立重验**（`a4_gate.py:294-300` 子进程 `validate` → `holder_distribution_scan.py:1060-1074` 用当前案根文件重建并做语义比较），而 final 绑定八项文件（`handoff_manifest/identity_snapshot_receipt/a4_seal/entity_freeze/membership_ledger/position_ledger/economic_control_ledger/distribution_scan`，`:685-688`）连同快照/data_map/供给真值/排除来源/阈值/识别规则全部进 `semantic_payload` 比较（`:719`）——只比 seal sha 与 freeze revision 两个字段是必要不充分条件，**有效性一律以完整重验为准**：子进程 `python -B holder_distribution_scan.py validate --case-dir <绝对> --scan <rounds[-1].final_scan_path> --expected-stage final`（cwd 案根；`-B` 免写 pyc），exit 0＝有效。本步（A0）只做**规划与当前有效性预判**：打印 D、F_old 与"最新 final 当前有效=是|否（validate 输出透传）"；**终判在 A1.5(0)**（`--from freeze` 时 A1 可能改 `entity_freeze.json`，A0 时的结论不代表 A1 后；`--from a4` 无 A1，A1.5(0) 再验一次即终判）。dry-run 只报当前有效性并注明"`--from freeze` 时 A1 后须重验"，**不得调 freeze 试探**（no-op 判定前仍跑 provenance 重放并在案根创建/清理 `.provenance-replay-*.json` 临时文件，`handoff_manifest.py:1308`，不是只读）。旧 seal 的 `distribution_claim_source.stage`/`cluster_claim_ids` 只打印作参考，不参与判定。dry-run 打印"分支=initial|final"。`--from rounds` 不做本步（只走 A3）。

### A1 freeze（仅 `--from freeze`）
从当前 `entity_freeze.json` 顶层回读 `members_source`、`entity_file`、`pending_items`、`casebook_note`，回传 `handoff_manifest.py freeze --case-dir . --members <members_source> --entity-file <entity_file> [--pending <";".join(pending_items)>] [--casebook-note <casebook_note>]`（参数定义 `:1639-1646`）。**`pending_items` 为空列表时省略 `--pending`；`casebook_note` 为 `null` 时省略 `--casebook-note`**（默认 `None`，`:1646`；APU 当前即 null；不得传字符串 "None" 或空串）；非空时原样传。不传就会被清空并平白新增 revision（`:1582-1583/:1590-1593`）。四道前置任一不过（`:1456-1560`）即 exit 2 透传。成功后打印 revisions 是否增加（"无需新 revision" 也算成功）。

### A1.5 预建轮次（仅 final-source 分支；在 A1 之后、A2 之前，保证 scan 绑定的是本次 freeze revision）
**(0) 非终态有轮次态的终判**（A1 之后先做；`--from a4` 亦做）：跑 A0.6(b) 所述完整 `validate --expected-stage final`。exit 0 → 比 `D == F_old`：不等 → exit 3"待登记 dist-* claims {D} 与现存非终态 final 簇 {F_old} 不闭合：人工重定 claims 或改 `--from rounds` 续跑"（不预建）；相等 → 进 A2 不预建。非 0 → 透传 validate 输出（此前 finalize 换过 seal、A1 增加了 revision、或其它绑定文件已变）→ 该 final 已失效，finalize 必拒，按下文**追加一轮**；预建自身失败 → exit 2 透传。无轮次态直接按下文预建。
按 A3.1 生成 `--stage final --round n`（`n = len(rounds)+1`），**不带解释** record-round（`:1351` 得非终态 UNEXPLAINED 或 REQUIRES_A4_REFLOW）。record-round 输出**先判终态**：该轮直接成为终态（NORMAL/LOW_SAMPLE，此时 F 常为空，不得先报"不闭合"却宣称留下非终态锚）→ exit 3"分布形态已变，dist-* claims 失去来源，须人工重定 claims；台账已终态，重封前需再 reopen-cycle"；非终态再读异常簇集合 F：`F != D` → exit 3"final scan 异常簇 {F} 与待登记 dist-* claims {D} 不闭合：人工重定 claims（`--claims-file`）后重跑 reseal；本轮 n 已记入台账为非终态锚"；`F == D` → 进 A2，finalize 将自动选此轮为来源（`:288-290`）。

### A2 A4（`--from freeze|a4`）
0. **归档引用识别（在 A2.1 canon 比较之前）**：对当前 `a4_claims.json.claims` 与待登记 claims 各自的 `files[]` 查磁盘存在；当前 claims 有缺失路径且能按案根 `distribution_reopen.json.cycles[-1].archived[]`（`from→to`，`mode:"moved"`）映射 → 判定"claims 引用已归档文件"：未给 `--claims-file` → exit 3"claim <id> 引用已归档文件 <路径>＝claims 变更：用 `--claims-file` 重定引用后重跑 reseal（`validate_claim_rows` 对缺失路径直接抛错，`a4_gate.py:259-263`，`case_paths.safe_case_file` 默认 `must_exist=True`；仅传 `--seal-files` 无效）"；给了且其 files 全部存在 → **跳过对旧 claims 的 canon**（旧 claims 无法 validate），视为"须 register"直接走 A2.1 的 register→exit 3。其余情况进 A2.1；A2.1 里 `canon(当前 claims)` 抛 ValueError 一律视为跳过条件①不成立（须 register），不得冒泡成 exit 1。
1. claims：`--claims-file` 未给 → 取当前 `a4_claims.json.claims` 作为待登记；`canon(rows) = json.dumps(a4_gate.validate_claim_rows(case_dir, rows), sort_keys=True, ensure_ascii=False, separators=(",",":"))`（`registered_at_utc` 在数组之外，不需要删键）。**跳过 register 的条件（两者同时）**：①`canon(待登记) == canon(当前 a4_claims.json.claims)`（后者抛错＝不成立，见 A2.0）；②`sha256(a4_claims.json 原始字节) == adversarial_review.json["claim_registry"]["sha256"]`（`adversarial_review_runner.py:673`；无 adversarial_review.json 时视为 ② 不成立，须 register）。不满足则 `a4_gate.py register`（`:312-330` 会重写并带时间戳），随后打印并 exit 3：`"registry 已变：按 runner 记录的实际路数重跑受影响复核 → adversarial_review_runner finalize → shared_release_receipt；完成后再跑 reseal --from a4"`（claims 变化即停，用户 §5 拍板 2）。`register` 本身非 0（待登记 claims 未过 `validate_claim_rows` 完整校验）→ exit 2 透传其输出，**不得**打印"registry 已变"。
2. verdicts：`--verdicts-file` 未给 → 从当前 `a4_seal.json` 的 verdicts 文件（`verdicts.path`）复制到案根 `verdicts_rev<新 revision>.json`（该路径若已被 A0.4 归档而磁盘不存在 → 按 A2.3 同款回执映射 `from→to` 并核 `sha256_file(to)==sha256` 后从归档副本复制；映射不到或 sha 不符 → exit 3 要求显式 `--verdicts-file`）（**案内、封口后持续保留、禁删**；finalize 会把该路径与 sha 写进 seal，案外路径被 `safe_case_file` 拒，`a4_gate.py:407`）；给了 → 须在案内（否则 exit 2），并与旧 seal **仅按共有 id** 比对 verdict——旧值直接读当前 `a4_seal.json.claims[]`（finalize 已写入规范化 `id/verdict/revision_note`，`a4_gate.py:472-476`；**不重开旧 verdicts 文件**，回执映射只用于"未给"分支的复制）（旧 seal 无、新文件有的 id＝相对旧 seal 新增、当前 registry 已登记的 claim，不算"不同"，但须属于当前 `a4_claims.json` 已登记 id，否则 exit 2"verdicts 含未登记 id"；全集覆盖由 finalize 自行校验），任一共有 id 的 verdict 不同 → exit 3"A4 verdict 与上版有差异＝判断变更：请人工按 A4 流程 `a4_gate finalize` 封新 revision、按 `downstream-check` 重跑受影响复核，然后 `reseal --from rounds`"（不自动放行；reseal 不提供"确认"开关）。
3. `--seal-files` 未给 → 取当前 seal `sealed_files[].path`，只扣除**本次 finalize 会自动并入的**：`MANDATORY_SEAL_FILES`、**本次选中的来源路径**（按 A0.6 分支：initial 分支＝案根 `distribution_scan.json`；final 分支＝最新非终态 final scan 路径，即 A1.5 预建轮或现存非终态最新轮；`a4_gate.py:387/393` 只加入本次 `distribution_source["path"]`）、待登记 claims 的 `files`（`:389-404`；并入本身去重，不怕重复）；旧 seal 的 `distribution_claim_source.path` 若与本次来源**不同**（如旧来源 round 1、本次已是 round 2）→ **不扣除**，作附加封入文件保留并走下文映射规则（已被归档则映射到归档副本，`safe_case_file` 只要求案内），这是"只扣本次自动并入件"的字面执行，不另设淘汰规则；`claim_registry.json` **仅** `--workflow-type independent-audit` 才自动并入（`:391-392`），本单固定 new-analysis，故它若在旧 seal 里就当附加封入文件保留（无条件剔除会漏封）。只保留"附加封入文件"。其中在磁盘不存在的路径：先按案根 `distribution_reopen.json.cycles[-1].archived[]`（`{from,to,sha256,mode:"moved"}`，`holder_distribution_scan.py:1259-1262`）把 `from→to` 映射到归档路径，核 `sha256_file(to) == sha256` 后替换并在 stdout 打印"seal-files 映射 <from> → <to>"；claim 文件引用已归档路径的情形已在 A2.0 处理，本步只管附加封入文件；无法映射或 sha 不符 → exit 3 列出并要求显式 `--seal-files`，不静默剔除。用户显式给的 `--seal-files` 不做映射，缺文件直接透传 finalize 的失败。APU 实况：37 个 sealed 路径无一在 `dist_rounds/`、`charts/final/` 下，不触发映射。调 `a4_gate.py finalize --case-dir . --verdicts-file <上一步文件> --seal-files <…> --workflow-type new-analysis`；非 0 → exit 2 透传（含其 `distribution_claim_source` 对 initial/最新非终态 final scan 的独立重验 `:273-300`）。
4. 随后在编排层调 `a4_gate.downstream_stale(case_dir)`（函数，`a4_gate.py:627`；**不调 CLI**、不改 `a4_gate.py`）。**台账在场且 `terminal` 为 null（A1.5 预建轮或原有非终态续跑）时**，`terminal_row()` 必抛"rounds terminal 指针不唯一"（`:663-668`）使 `rounds.a4_seal_sha` 与 `rounds.entity_freeze_revision` 两项必为 stale——这是正待 A3 完成的正常中间态，不是过期：把这两项打印为 `pending_a3` 不计停点；其余项（`adversarial_review.claim_registry`、`shared_release_receipt.adversarial_review`、`dormant_warehouse_audit.universe_ref`）任一 stale → 打印并 exit 3（例如 shared_release_receipt 需重跑）。台账终态或无台账时不降级，任一 stale 即 exit 3。这是**延期到 A3 终态验证**，不是已验证通过：A3 终态后由 A4 收尾的 `stage2_closeout check` 完整核验（W2 实物 `stage2_closeout.py:514-519` 重调 `downstream_stale()`，任一 stale 即 BLOCK；`a5_report_seal.py:279-284` 另独立重验终态 scan），负例见 §B 14a/14b。

### A3 rounds（所有 `--from`）
1. 轮号 `n = len(rounds)+1`（无台账＝1；`:909` 与 `:1334` 都拒错轮号，且 `:1005` 会把指定输出写成 data_broken 覆盖旧 scan，故不得写死 1）。`holder_distribution_scan.py --case-dir . --stage final --round n`（W1 的 `find_snapshot` 自动从 initial 绑定解析快照；不传 `--snapshot`）。非 0 → exit 2。
2. 读新 scan：`verdict == "ABNORMAL_SHAPE"` 且 `new_clusters` 为空——**新 scan 文件没有 `new_clusters` 字段**（它是 record-round 时算出写进台账的），reseal 须同源计算 `new_clusters = {f"dist-{cluster_id}" for abnormal_clusters} − {a4_seal.json.claims[].id}`（`holder_distribution_scan.py:1336-1339`），禁 `scan.get("new_clusters")`；未覆盖簇误送解释器只会得 UNEXPLAINED 且 `errors` 可能为空，停点说明会失真→ 一律尝试用**当前** scan 与 seal 生成解释（旧周期 `explanation_path` 有无只打印作参考，不作条件；首次 UNEXPLAINED、首次回流后、旧解释失败未记路径三种场景都走这里）：`distribution_explanation_check.py --case-dir . --scan dist_rounds/round_n/distribution_scan.json --a4-seal a4_seal.json --out dist_rounds/round_n/explanation.json`（`distribution_explanation_check.py:183-192`；旧解释绑旧 scan/seal 不能复用）；它 BLOCK → record-round **不带** `--explanation`（带失败解释同样是非终态 UNEXPLAINED，只是台账多记一份解释文件 `:1351`；本单选不带），并在末尾 exit 3 列"解释检查失败：<其 stdout＋stderr>"。旧轮为 WAIVED → 不继承：仍先 record 本轮（不带解释，非终态），再 exit 3"waiver 须 ≥ 第 2 轮（`:1354`）且重新绑定当前 scan/seal/台账（`:1096-1098`）：补证据/复核 → 需要则 finalize → `--stage final --round n+1` → 人工 waiver record"。
3. `holder_distribution_scan.py record-round --case-dir . --scan dist_rounds/round_n/distribution_scan.json [--explanation dist_rounds/round_n/explanation.json]`。输出含 `REQUIRES_A4_REFLOW` 或上一步同源计算的 `new_clusters` 非空 → exit 3"新簇回流 A4"。
4. 终态（NORMAL/LOW_SAMPLE/EXPLAINED）→ 继续 A4 收尾；非终态 UNEXPLAINED → exit 3 并打印 ABNORMAL 分支下一步（"补证据/复核 → finalize → `reseal --from rounds`（轮号自动 n+1，再次尝试解释）"）。A1.5 预建轮不经本节。

### A4 收尾
先调 `stage2_closeout.py fill-workorder --case-dir . --report <报告>`（W2 E4：只更新机械绑定 a4_seal_sha256／entity_freeze_revision／rounds／终态 scan 与图／distribution_terminal／report_image_refs，`bindings.report_md` 原稿锚不动），再调 `stage2_closeout.py check --case-dir . --report <报告>`，透传 check 退出码（0/2）。reseal **不**画图、不写 A5、不改工单选材、不自动重跑对抗复核、不代替首次 A4 封口。

### dry-run
`--dry-run`：执行 A0 第 0-2 步与所有只读判断（分支判定、跳过条件、verdict 差异、失效表、将归档的文件清单、将调用的命令行、seal-files 缺失清单），不写任何文件、不建任何目录、不调任何会写盘的子命令——具体避开：`check`（`stage2_closeout.py:798` 写收据，`_atomic_json` `:94-103` 先建临时文件，"临时文件最终清掉"不算零写入）、`reseal_log.json`/`_history/`、`reopen-cycle`/`scan`/`record-round`/`finalize`/`freeze`（各自落盘，`holder_distribution_scan.py:1001/:1005/:1295/:1387`）；只调只读子命令（`validate`、`distribution-validate`、`downstream-check`、`reopen-cycle --dry-run`；`downstream-check` **不传 `--json-out`**——`a4_gate.py:706-708` 有它就写文件；它本身调本地 `downstream_stale`、不起子进程）。**主进程自身**也须零写入：`stage2_closeout.py` 在 `sys.path` 设置之后、任何本地模块导入之前（`:25` `from case_paths` 之前）置 `sys.dont_write_bytecode = True`——`-B` 只管子进程，主进程导入 `a4_gate` 等仍会写 `__pycache__`；`:31` 顶层 `import figures_from_facts` 改为在使用它的函数内延迟导入（`:412-462` 所在函数），因为它 `:56` 顶层导入 `standard_charts`、后者 `:37-39` 顶层导入 matplotlib（缓存缺失时会建配置目录并写字体缓存）；**dry-run 的主进程及全部子进程**不得加载 matplotlib；正式 reseal 的**编排主进程**不加载（`chart_style.py`/`lifecycle_flow.py` 顶层导入它，主进程不得导入这两个模块），A3 的 scan 子进程生成分布图**允许**加载（`holder_distribution_scan.py:1001` 调 `write_png`，`:808-809` 函数内导入 `chart_style`/`matplotlib.pyplot`；十三复⑥：写成"全程不得"与正式流程冲突）。主进程导入链经 `audit_release_gate`→`chain_registry.py:355-359`→`formal_capability_probes.py:187-205` 只解析 callable 不执行测试函数、无绘图库；子进程链**不靠静态断言证明**（入口脚本顶层无 matplotlib 不代表间接导入安全，如 `handoff_manifest.py:43-51` 顶层导入 `shared_release_receipt` 等），一律用用例 7(d) 的运行时阻断（主进程与全部后代同一机制）。**验收不得以 `MPLCONFIGDIR` 空目录为"未加载 matplotlib"的证明**（它只证明目录最终为空）。退出码与真跑**同因同码**：计划正常 → 0；前置校验失败（A0.0 迁移、A0.1 算法漂移、A0.3 裁决路径解析失败或 (iii) 预验不过等 exit 2 类）→ 2；A0.3 的 `S ∩ M` 交集冲突与 (ii) 裁决前置拒绝 → 3；三态均零写入。

## §B 测试 `scripts/tests/test_stage2_reseal.py`（登记 `run_all.py` SUITE；十五组用例（含 6 四变体、12b/12c、13a/13b/13c、14a/14b、15a–15g 子例）全部实跑，codex 三复只审了构造链未运行）
A2–A4 用例沿 `test_stage2_closeout.py`（W2 v6 契约 `build_closeout_case(root) -> Path`，以合入实物函数名为准）的 new-analysis 全案链夹具；**该夹具无 `provenance_ledger.json` 算法绑定、freeze 是空桩（`test_a4_gate.py:278-286`），原样会在 A0.1 exit 2**，故本文件加助手 `add_reseal_prereqs(case)`：写/合并 `provenance_ledger.json.input_binding.algorithm = {script_sha256: full_sha256(entity_source_trace.py), files: {name: {path: 仓库内该文件 realpath 绝对路径, bytes, sha256}} 三项}`（形状按 `handoff_manifest.py:806-829/1179`）；该夹具原本**没有** `provenance_ledger.json`，新建时须是完整合法的 NO_FLIPS 台账：`schema:"provenance-ledger/v2"`、`total_supply_raw`、`entities: []`（`handoff_manifest.ledger_real_flips` 为空 → `a5_report_seal.py:203-207` 返回 NO_FLIPS，closeout 第 5 项放行 `{DISCLOSED,NO_LEDGER,NO_FLIPS}`，`stage2_closeout.py:523-524`）；只补 `input_binding.algorithm` 会在 `:203-204` 报 `provenance_ledger schema 非法: None`。夹具 freeze 桩无 `provenance_ledger_sha256`（`test_a4_gate.py:281`），故 `:194` 不比台账哈希，A2+ 用例（1-5、7b、8-9、11-13、15）全部先调它（15 组用原始 W2 夹具会先在 A0.1 exit 2，测不到目标分支）；**不得**用最小 `adversarial_review.json` 覆盖该夹具的真实 v4 文件（会让 shared receipt 绑定过期污染正例）。A1 用例用 `test_handoff_manifest.py` 的 `make_case`（`:73`）→ `generate` READY → `setup_freezeable`（`:275`）→ `freeze` 链构造真实可 freeze 案（`finish_distribution_normal` 的空 freeze 桩与 `test_batch15` 的三账夹具都没有完整 freeze 前置，不可用）。用例：
1. `skip_register_when_claims_same`：夹具原样（registry sha 与其 adversarial_review.json 一致）→ 不 register（`a4_claims.json` 字节不变）、finalize 后 seal revision +1。
2. `registry_sha_drift_forces_register`：改 `a4_claims.json` 的 `registered_at_utc` → canonical 相同但原始 sha 漂移 → register 被执行且 exit 3。
3. `verdict_change_stops`：`--verdicts-file`（案内）把一条 CONFIRMED 改 WEAKENED → exit 3，seal 未变；案外路径 → exit 2。
4. `terminal_ledger_triggers_reopen`：台账 terminal → reseal 后 `data/stage2/dist_cycle1/` 在场、新台账轮 1 终态、`charts/final` 只剩新终态图。
5. `other_charts_archived_not_deleted`：**预置的** `charts/final/fig1.png` 被移到 `_history/<ts>/charts_final/`，字节相同（W2 收口夹具不生成三图，须显式写占位 PNG）。
6. `freeze_readback_no_new_revision`：可 freeze 夹具（无完整 A4 案，不能当整条 reseal 正例）直接调 reseal 的 A1 助手函数 `freeze_readback(case_dir)`（import 模块），四变体：pending 非空＋note 非空；pending 空＋note null；pending 非空＋note null；pending 空＋note 非空 → `revisions` 长度均不变、`pending_items`/`casebook_note` 值不变。
7. `dry_run_touches_nothing`：(a) 迁移案 → exit 2、stdout 含"案根已迁移"；(b) 带 terminal 的完整夹具（显式预置 `charts/final/fig1.png|fig2.png|fig3.png`）→ exit 0、stdout 含"分支=initial"与将归档清单（三图、cycle1）；两者全案 **路径→sha 映射＋`os.walk` 目录清单** 前后完全相同（不是 sha 集合；`os.walk` **不跳过** `__pycache__`、`*.pyc`、隐藏文件与临时文件——`.gitignore:4-5` 忽略的项正是要抓的）；同一比较另对 **skill 仓库 `scripts/` 目录**做一次（主进程与子进程链的 bytecode 落在仓库内，案根映射抓不到；跑前先用 `find` 断言仓库 `scripts/` 下无 `__pycache__`，不用 `git status`——它受 `.gitignore` 遮蔽）；并覆盖**缓存缺失场景**：跑前把环境变量 `MPLCONFIGDIR` 指向新建的空临时目录，跑后断言该目录仍为空（**独立的目录验收，不作"未加载 matplotlib"的证明**——十二复③：它只证明目录最终为空）。(c) 15e 与 §E APU 段的零写入验收沿用同一比较函数。(d) **导入阻断（直接验收"未加载 matplotlib"，主进程与全部子进程同一机制）**：新建临时目录放一个 `sitecustomize.py`：向 `sys.meta_path` 头部插入一个 finder，其 `find_spec(name, …)` 遇到 `name == 'matplotlib'` 或以 `'matplotlib.'` 开头时先把 name 追加写入环境变量 `MPL_BLOCK_MARK` 指向的标记文件、再 `raise ImportError('matplotlib blocked by dry-run verification')`，其余返回 `None`；以 `PYTHONPATH=<该目录>[:原有 PYTHONPATH]`（**追加**、不覆盖原值）、`MPL_BLOCK_MARK=<标记文件路径>` 环境跑 (b) 同一条直接 CLI；普通组与阻断组用**同一份显式基础环境**（同解释器、cwd、参数、环境变量），阻断组只多这一个目录与标记变量（它刻意替换了 `sitecustomize` 的选择，不宣称"完全不改变解析行为"；业务模块不受影响：`stage2_closeout.py:24` 与 validator/a4/handoff 都把 `lib` 前插 `sys.path`、`formal_capability_probes.py:187-197` 临时前插后恢复）（`python -B …/stage2_closeout.py reseal … --dry-run`）。Python 启动时 `site` 自动导入 `sitecustomize`；子进程均以 `[sys.executable, script, …]` 启动、未加 `-I/-E/-S`，继承环境后同样生效（唯一显式传 env 的 `handoff_manifest.py:1350-1352` 以 `dict(os.environ)` 为底；已实测父进程与 `-B` 子进程均被拦且标记文件记下尝试）。**(d0) 自检固定进用例**（十四复：标记为空只在 finder 已装上且相关分支真执行的前提下才充分）：同一 `sitecustomize` 目录、同一环境下先跑 `python -B -c "import matplotlib"` 与 `python -B -c "import subprocess,sys; subprocess.run([sys.executable,'-B','-c','import matplotlib.pyplot'])"`，断言父/子进程 stderr 均含 `blocked`、**d0 专用**标记文件恰记两行 `matplotlib`；自检不过则本用例整体 fail、不得继续；自检证据保留不删。正式测量（(b)/(g)/§E APU 的每一次阻断运行）各用**新的**标记文件路径（只换 `MPL_BLOCK_MARK` 的值，finder 与其余基础环境不变），测量开始前先断言该路径不存在或为空（十五复必改一：v15 让 d0 与正式断言共用同一标记文件，"恰两行"与"为空"互斥必败）。断言：exit 码与 stdout 与 (b) 无阻断直跑相同、stderr 不含 `blocked`、本次专用标记文件不存在或为空（finder 先于正常导入生效，被 `try/except ImportError` 吞掉的导入尝试也会留标记，这是 `sys.modules['matplotlib']=None` 做不到的）。不用 `-c`/`runpy` 包装（与直接 CLI 只是本例行为对照、非普遍等价）、不用 AST 静态断言（只查入口顶层导入不覆盖导入链）。(e) 本用例与 §E APU dry-run 验收在**新建的验收 checkout** 执行：`git worktree add <临时目录> HEAD` 建基准，再把 W3 **全部待交付修改与新增测试**（`git -C <仓库> diff HEAD --name-only` ∪ 未跟踪的白名单新建件）逐文件复制进去、断言每个文件 sha256 与工作树一致（v14 直接用 HEAD 建 worktree 与 §0"不 commit"冲突，验收不到施工件——十四复②：HEAD 的 `stage2_closeout.py:761` 尚无 `reseal`）；要求是**无缓存、无非预期文件**（跑前 `find` 断言 `scripts/` 下无 `__pycache__`；`os.walk` **文件**清单只含 HEAD 文件（`git ls-tree -r HEAD --name-only`）＋叠加件＋根目录 `.git` 管理文件（`git worktree add` 必生成，十五复必改二：v15 写法会拒掉它），**目录**清单＝允许文件的父目录集合另行核对，缓存与前后不变检查照旧）；worktree 内**直接启动其自身**的测试与 CLI 生成夹具（`stage2_closeout.py:22-24`、`test_stage2_closeout.py:13-15` 从 `__file__` 前插本 checkout，validator `:195` 调同目录扫描器），且 `add_reseal_prereqs` 必须绑定**验收 worktree 自身**的三份算法路径（`handoff_manifest.py:818-820` 先比 realpath，主工作树同字节文件不能替代；消费者规则不改），**不要求** `git status` 干净（叠加后必不净；`git status` 干净也不保证无缓存，普通工作目录已有缓存会把清单比较污染成误报）。(f) `archives_subdirs_including_empty`：用例 5 同一夹具与同一次真跑，`charts/final/` 另预置子目录 `sub/x.png` 与空目录 `empty/` → 两者**整体**出现在 `_history/<ts>/charts_final/`（`empty/` 仍是空目录）、`charts/final/` 只剩终态图、A0.4 `reopen-cycle` 不因残留条目拒绝（`dist_cycle1` 在场即证）；dry-run 下将归档清单列出这两个目录且全案不变。(g) `dry_run_runs_validate_chain_blocked`：用 15d 备选路线的完整夹具（`build_closeout_case`＋`add_reseal_prereqs` → `reopen` → `run_scan final --round 1` 不 record → `distribution-template` 按 15d ②填合法 → 先断言 `distribution-validate` exit 0；有 `accounting_mode.json`、无台账＝非终态、M4＝∅、p ∉ M3），跑 `reseal --from rounds --dry-run` 无阻断与 (d) 阻断各一次：A0.3(iii) 必经**只读预验** `distribution-validate → validate` 子进程链（7b 无裁决件、§E APU 在 A0.1 提前退出，都覆盖不到这条链）；断言两次 exit 均 0、stdout 相同、stderr 不含 `blocked`、本次专用标记文件不存在或为空、映射＋清单不变；预验不通过 → 修夹具，不得以 exit 2 充数。
8. `new_clusters_stop`：按 `test_distribution_gate.py:337-355` 范式（initial 经 `candidate_screening.auto_excluded_candidate` 排除 `head-new`，final 纳入）让 final 出新簇 → exit 3 文案含"回流 A4"（`bump_balances` 不保证出新簇，不用）。
9. `ends_with_closeout_check`：成功路径末尾收据 `stage2_closeout_receipt.json` verdict=PASS，工单 `bindings.a4_seal_sha256` 已更新为新 seal、`bindings.report_md` 不变，新 seal `verdicts.path` 指向案内存在的文件。
10. `migrated_case_root_stops`：夹具 `accounting_mode.json` 写一个指向案外绝对路径的 observation bundle → exit 2 文案含"案根已迁移"，且映射＋清单不变（即使台账已 terminal 也不归档）。
11. `round_number_continues_nonterminal_ledger`：先造非终态轮 1（record-round 不带解释的 UNEXPLAINED）→ `--from rounds` 只追加 round 2（台账恰 2 轮、不经 A1.5/A2），`dist_rounds/round_1/distribution_scan.json` 字节不变。
12. `sealed_file_missing_stops`：夹具首次封口时 `--seal-files notes.md`（附加文件，不进任何 claim 的 files），reseal 前把它 move 到案内别处 → 无法映射 → exit 3 列出该路径，seal 未变。12b `sealed_path_remapped_from_reopen`：首次封口附加封入 `dist_rounds/round_1/notes.json`（终态后存在的额外文件），`--from a4` 经 A0.4 归档后按回执映射到 `data/stage2/dist_cycle1/dist_rounds/round_1/notes.json`，stdout 含"seal-files 映射"，finalize 成功；12b 的归档假设已核成立（扫描器 `:1240-1246` 遍历 `dist_rounds` 全部普通文件、`:1268-1269` 搬整目录，额外文件得到自己的映射项），`notes.json` 须在**首次 finalize 前**创建并封入，不得对已终态夹具再次 finalize 补封。12c **独立保留**（不替代 12b）：claim 文件引用已归档路径、不给 `--claims-file` → 经 A2.0 exit 3 文案含"claims 变更"。
13. `final_source_branch_prebuilds_round`，拆两例。**13a 不等**：`--claims-file` 含一条 `dist-<簇 X>` 而案根 initial 异常簇为空 → A1.5 按用例 8 范式让 final 出簇 Y≠X（W2 默认夹具是 LOW_SAMPLE，仅加 claim 不保证预建轮非终态，必须造 ABNORMAL final）→ exit 3 文案含"不闭合"，台账多一轮非终态。**13b 相等**：同范式让 final 恰出簇 X → A1.5 通过（台账多一轮非终态，dry-run 打印"分支=final"）→ A2.1 register 后 exit 3"registry 已变"（只有 C1 的旧夹具新增 dist claim 必经 register 停点，不能断言一次跑到 finalize）；随后测试**直接**调 `a4_gate.py finalize --case-dir <绝对> --verdicts-file <案内> --seal-files <旧 seal sealed_files[].path 按 A2.3 同款扣除后逗号拼接> --workflow-type new-analysis`（`--seal-files` 是 `required=True`，`a4_gate.py:722`，缺则 argparse 阶段 exit 2 产不出 seal；verdicts 文件须覆盖登记后**全部** id，C1 与新增 `dist-X` 均 CONFIRMED；它不查对抗复核链）断言新 seal `distribution_claim_source.stage == "final"` 且 `cluster_claim_ids == ["dist-X"]`。**13c 贯通 `nonterminal_round_then_finalize_reaches_a3`**（防 A2.4 截断复发）：从 13b 的 **register 停点**分叉（**不做** 13b 末尾那次直接 finalize——做了 round 1 final 就绑了旧 seal R，reseal 再 finalize 时独立重验必 exit 2）。此时状态：registry 含 C1＋`dist-X`、seal 仍是 R、台账非终态轮 1（簇 X，绑 seal R；状态 REQUIRES_A4_REFLOW 而非 UNEXPLAINED——seal R 尚无 `dist-X`）。测试把 `adversarial_review.json.claim_registry.sha256` 与 `shared_release_receipt.json.inputs["adversarial_review.json"].sha256` 改成当前实物（模拟复核已重跑），备案内 verdicts 覆盖全部 id（C1 与旧裁决一致、`dist-X` CONFIRMED）→ `reseal --from a4 --verdicts-file <案内>`：A0.4 非终态跳过；A1.5(0) 完整 `validate --expected-stage final` exit 0（round 1 final 仍绑当前 seal R 且八项绑定未变）、`D=={dist-X}==F_old` → 不预建；A2.1 跳 register；A2.2 verdicts 含新增 `dist-X`，按共有 id 比对 C1 一致不停；A2.3 finalize **一次**成功（来源＝round 1 final，seal → R+1）；**A2.4 不停**（断言 stdout 含 `pending_a3`、无 rounds 两项停点）→ A3 生成 round 2（扫描器只查台账连续性与前轮哈希绑定，不要求上一轮仍绑当前 seal；round 2 的 `input_binding.final_bindings["a4_seal.json"].sha256` == 新 seal sha）→ `new_clusters` 为空 → 调解释 CLI（它**会生成**文件）→ claim `dist-X` 无有效 `distribution_explanation` 致五判据 BLOCK → record 不带解释 → 非终态 UNEXPLAINED → 末尾 exit 3 文案含"解释检查失败"；断言台账恰 2 轮、seal revision 恰 +1、exit 3 来自 A3 而非 A2.4。
14. **A2.4 延期项的兜底负例**（证明 `pending_a3` 不留缺口，跑的是 W2 实物 `stage2_closeout check`）：14a `terminal_seal_sha_mismatch_blocks`：终态夹具改 `distribution_rounds.json` 中 `rounds[]` 里 terminal 指向那一行的 `a4_seal_sha` 一位（不是往顶层 `terminal` 对象加字段） → `check` 的 downstream 子检查 BLOCK（`rounds.a4_seal_sha` stale）；14b `terminal_freeze_revision_mismatch_blocks`：改终态 final scan `input_binding.entity_freeze_revision` → BLOCK（`rounds.entity_freeze_revision` stale；scan 语义也随之失配）。两例均断言 downstream 子检查 errors 含对应具体 stale 项名，不只断言总 verdict BLOCK。
15. **裁决文件归档前置**（W2 夹具无 `distribution_adjudications.json`，本组自造；15a/b/c/e/f 与 A2–A4 完整夹具能过 A0.0 的依据＝`build_closeout_case` 复制后经 `test_stage2_closeout.py:46` `rebind_case_inputs` 重绑、accounting bundle 路径按 `test_a4_gate.py:170-178` 改到最终案根，只引用原始 source 夹具 `test_audit_release_gate.py:276-281` 不足以证明最终案合法；`entity_freeze.json` 桩无 `entity_file`，校验一律省略 `--entity-file`）：15a `adjudication_bound_to_m3_only_stops`：终态夹具预置 `charts/final/extra_scan.json`（案内普通文件）并写最小裁决件 `{schema: adjudication_validator.DISTRIBUTION_SCHEMA, source_scan:{path:"charts/final/extra_scan.json", sha256:<实物>}}`，`--from rounds` 与 `--from a4` 各跑一次 → 均 exit 3 文案含"不承接"，映射＋清单不变（图未搬、无 `dist_cycle1`、无 `_history`）；15b 同 15a 但台账非终态（用例 11 范式）→ 同 exit 3、全案不变；15c `adjudication_bound_to_m4_archived`：终态夹具用 `adjudication_validator.py distribution-template` 从 `dist_rounds/round_1/distribution_scan.json` 生成裁决件（validator 自产，schema/sha 合法）→ `--from rounds` 成功路径中该文件被 move 到 `_history/<ts>/`，同目录 `reseal_log.json` 含"随周期归档"，案根无该文件，A0.5 不调 validate；15d `nonterminal_ignores_history_receipt`：**顺序不可倒**——①造真实历史终态并真 reopen，再得当前周期**真正非终态**的已登记轮。主路线复用 `test_reopen_cycle.py` `reopen_abnormal_round1_unexplained_then_round2` 前半段范式（`:258-264`）：`test_distribution_gate.prepare_explanation_case` 造 ABNORMAL final round 1 → 带解释 `record` 得 EXPLAINED 终态 → `add_reseal_prereqs` → `reopen` → `run_scan final --round 1` → 不带解释 `record` 得 **UNEXPLAINED 非终态**（`terminal is None`，`input_binding.reopened_from_cycle.cycle == 1`；历史与当前周期复用同一路径 `dist_rounds/round_1/distribution_scan.json`，正好检验误归档）。**不得**用 `normal()` 造当前轮：NORMAL 分布 reopen 后 record 仍是 NORMAL 终态（`holder_distribution_scan.py:1341-1345` 先判终态），不会因"不带解释"变成 UNEXPLAINED；也**不得**对同一案根叠加 `normal()`/`add_final_inputs` 与 closeout 夹具（前者覆盖 facts/state/freeze/三账与简化 seal，`test_distribution_gate.py:87-105`）。最小夹具无 `accounting_mode.json`，A0.0 收紧后（十三复④）本例与 15g **不经 CLI、不跑 reseal、不经过 A0.0**（不得表述为"通过了 A0.0"，验的是共用的 A0.3–A0.5）：直接调用 A0.3–A0.5 的可导入函数（施工者把 A0.3–A0.5 实现为一个函数：输入案根、M3、M4、终态标志与 `entity_file`；成功返回 dict 含 `stage_done == "A0.5"` 与 `prevalidated_scan_paths`（已预验通过的裁决绑定 scan 规范路径列表，本例含 p）；停点＝先向 stdout/stderr 输出与 CLI 完全相同的诊断文案，再 `raise SystemExit(code)`（code 是整数 2/3，**不得**把文案字符串作 `SystemExit` 参数——那会把退出码变成 1，破坏同因同码契约）；CLI 只是包装，函数名在 §B 用例里按实际名字调用）；S 提取在该函数外，本例 M3 由测试按 A0.3 规则自算（A0.4 对应的是 M4，不是 M3）（简化 seal 四件与 registry 都是 `a4_claims.json`/`facts.json`/`analysis-state.json`/`evidence.json`，`test_distribution_gate.py:99-105/:128-133`，非终态 M4＝∅，不命中 M3）；断言范围限定 A0.3–A0.5。备选路线（主路线确实造不出时才用）：`build_closeout_case`＋`add_reseal_prereqs` → `reopen` → `run_scan final --round 1` **不 record**（无台账＝非终态、M4＝∅），此时须明确断言范围：只断言 A0 阶段裁决件不动；随后 A3 按 `len(rounds)+1` 生成 round 1 会**覆盖**现有 scan，裁决件字节不变不代表最终仍有效，不断言其最终有效性；②`distribution-template --scan dist_rounds/round_1/distribution_scan.json` 生成裁决件，按 `test_distribution_gate.py:367-380` 范式填 `adjudicated_at` 非空、每行 `candidate_verdict="excluded"`、`accepted_members=[]`、`excluded_members` 覆盖该候选全部成员各带非空 `reason`；成员集合的**断言**固定为"等于 scan 对应候选的全部 `members[].owner`"（validator 成员来源 `adjudication_validator.py:209`，模板预填 `:238`），从 scan 实物取，**不得**用缺字段默认 `[]`（另一会话正在删 `_members_total` 预填改旁车 `<台账名>.members.json`，默认空会把迁移错误掩盖成通过；"以合入实物为准"只是定位提示不是断言）；③先断言 `distribution-validate` exit 0；④`--from rounds` → A0.3(iii) D(p) ∩ M3＝∅、预验通过、M4＝∅、不读历史回执 → 裁决件字节不变、未进 `_history`；主路线下 A3 追加 round 2 不覆盖 round 1；**不得预期 reseal 最终停在 ABNORMAL 分支 exit 3**（十二复⑥：该夹具的解释仍有效——`prepare_explanation_case` 造的解释成员/数量/证据/传播哈希完整 `test_distribution_gate.py:114-133`，不带解释 record 只让该轮台账记录成 UNEXPLAINED、不动 claims 里的解释 `holder_distribution_scan.py:1348-1364`，A3 重跑解释器读的是现有 registry/证据/facts/state 哈希 `distribution_explanation_check.py:95-146`，均未被破坏，预期反而 EXPLAINED）；A0.5 之后的行为**不在本例断言范围**；成功判据＝函数正常返回、`stage_done == "A0.5"` 且 `prevalidated_scan_paths` 含 p（十三复⑤：只查"裁决字节没变、没进历史目录"在 A0.3 之前就失败也满足，必须断言确实到达 A0.5）。**禁止**在生成当前 scan 后再塞入历史回执（validator 按回执重建 `reopened_from_cycle`，事后加入使当前 scan 失效，`:1066-1068`）；15e `dry_run_same_reason_same_code`：对 15a 夹具与 12b 夹具改跑 `--from rounds`（S ∩ M 命中 `dist_rounds/round_1/notes.json`）各跑 `--dry-run` 与真跑，两者 exit 码相同（均 3）、stdout 停点原因相同，dry-run 与真跑映射＋清单均不变；15f `adjudication_bound_to_copy_terminal_stops`（十复反例）：`build_closeout_case`＋`add_reseal_prereqs` 的终态夹具把 `dist_rounds/round_1/distribution_scan.json` 逐字节复制为 `evidence/final_scan_copy.json`，裁决件从该副本 `distribution-template` 生成并按 15d ②填成合法（成员断言同 15d）、先断言 `distribution-validate` exit 0（validator 不要求被校路径等于台账记录路径，预验本可通过）；`--from rounds` 与 `--from a4` 各跑 → 均 exit 3（终态 p ∉ M4，不做预验直接拒）文案含"不承接"，映射＋清单不变（无 reopen、无 `dist_cycle1`、无 `_history`）；15g `adjudication_dependency_in_charts_final_stops`（十一复反例：p ∉ M3，但校验依赖 ∈ M3）：基于 `prepare_explanation_case` 的造案逻辑（ABNORMAL final round 1，**不 record、不 reopen**；**不能原样调用它**：它的 initial 调 `run_scan(root)` 不传 `--snapshot`（`test_distribution_gate.py:110`），默认发现只认 `SNAPSHOT_CANDIDATES` 四个名字（`:188-189/:204-210`），`charts/final/owners.json` 不在其中、不传必失败——本例 initial 须 `run_scan(root, "initial", "--snapshot", "charts/final/owners.json")`，final 从 initial 绑定自动取该路径（`:198-203`）不传；十二复⑤核：显式快照走 `:196-197`、data_map 只查唯一登记＋sha `:224-233`，目录本身不被拒——`reopen-cycle` `:1236-1239` 与终态物化 `:1381-1382` 都拒绝 `charts/final` 含其他文件，含 reopen 或终态的路线在本例造不出来；无台账＝非终态、M4＝∅），把 `make_case` 写死的快照路径 `data/holders_owners.json`（`test_distribution_gate.py:31`）改为 `charts/final/owners.json` 并在 `data_map.json` 唯一登记（参数化 `make_case` 或在本文件复制其逻辑；initial 与 final 均绑定它，`:224-233` 只要求唯一登记＋sha 一致，不禁止该目录；final 快照须与 initial 同 sha，`:700-707`），裁决绑 `dist_rounds/round_1/distribution_scan.json`（p ∉ M3），先断言 `distribution-validate` exit 0（预验本可通过）；直接调 A0.3–A0.5 函数（同 15d，不经 CLI、不经过 A0.0）→ 必须在搬任何文件前先输出诊断再抛 `SystemExit(3)`（断言 `e.code == 3`，文案从捕获的 stdout/stderr 取）、文案含"裁决校验依赖 charts/final/owners.json 将被本步搬走"，映射＋清单不变（快照未搬、无 `_history`）；反向对照（施工者本地临时验证，不进仓库）：去掉依赖交集检查后同一夹具会预验通过→搬快照→A0.5 失败，证明用例真抓到十一复缺口。

## §C 手册净减（施工原则：三册 `wc -c` 合计不增；基线 split-run 28162、analyze-workflow 34301、report-template 42499；每处改完记"删 N 字节／加 M 字节"，最终合计 ≤ 104962；`docs_lint` PASS——特别是 `contract_manifest` 的 CT-WAVE-16/17/18、CT-DISTRIBUTION-15/16 五枚针都在 split-run:136/:150，替换后必须仍在）
| 文件:行 | 现文（锚，只替换引号内这一段） | 改为 |
|---|---|---|
| split-run.md:136 | `→ final 分布扫描写 \`dist_rounds/round_N/\` → 新簇回流 A4；已覆盖异常跑解释五判据，未解释进入成员或机制闭环后回流 A4 → 唯一终态物化`（`a4-seal/v4` 之后、`\`charts/final/holder_distribution_current.png\`` 之前） | `→ final 分布扫描至终态（新簇回流 A4；重封一律 \`stage2_closeout reseal\`）→ 唯一终态物化`；同行 `§3b.3 收口自查` → `§3b.3 收口`。其余（EF-3 各针、`a4-seal/v4`、终态图名、正文亲笔成稿、工单交付即停）一字不动 |
| split-run.md:117 | `改挂 −2 收口自查（§3b.3 第⑤条）` | `改挂工单 \`stage2_selfcheck\` 申报（§3b.2）` |
| split-run.md:150 | 句尾 `…facts 与 state 在场——任一缺失或漂移停下报用户，禁带病开工禁补票。` | 五项改六项：`…facts 与 state 在场／\`stage2_closeout --receipt-only\` PASS——任一缺失或漂移停下报用户，禁带病开工禁补票。` |
| split-run.md:154 | `无 schema 版本、无 validator、不进 handoff manifest` | `无 schema 版本、\`stage2_closeout check\` 校验、不进 handoff manifest` |
| split-run.md:94 | `非正式件、无 validator、不进 handoff manifest` | `非正式件、closeout 校验、不进 handoff manifest` |
| split-run.md:158 | `entity_freeze_revision、` | `entity_freeze_revision（\`stage2_closeout fill-workorder\` 填）、` |
| split-run.md:165 | `\`stage2_selfcheck\`：−2 收口自查申报（§3b.3 五条＋刀 1 公告遵守申报）；` | `\`stage2_selfcheck\`：sealed/ 禁读令与刀 1 公告（context-discipline 2b）遵守申报（全程未读；违规读取写明何时、为何）——人工必填，机器不可验；其余自查由 closeout 收据覆盖；` |
| split-run.md:168-170 | §3b.3 标题＋五条整段 | 标题 `### 3b.3 −2 收口`；正文一句：`跑 \`stage2_closeout --case-dir . --report 报告.md\` 至 PASS（收据 \`stage2_closeout_receipt.json\` 覆盖图片引用一致、固定句式、翻转披露、原稿锚与发布闸干跑）；sealed/ 读取申报仍人工写进 \`stage2_selfcheck\`。` |
| split-run.md:176-177 | 两条（whale_series 落案根／现场 Python 调 plot_*） | 一条：`- 图 2 序列＝案根 \`whale_series.json\`（\`figures_from_facts fig2-series\` 按工单 series_source 装配，旁车 provenance 核 sha；发布闸只认案根）；图 2/3 仍现场调 standard_charts 库的 plot_whale_vs_price／plot_price_events，参数全部从工单声明的源文件读取（`ts` 为 ISO 日期串，绘图前转 datetime），禁手抄数字、禁自行改选材。` |
| split-run.md:180 | `最小机械修正＋amendments 哈希链留痕；` | `最小机械修正＋amendments 哈希链留痕＋\`stage2_closeout amend\` 更新收据；` |
| split-run.md:184 | `不覆盖：图表应有基数（少画图仍可能过闸）、fig2 实体线覆盖完整性（figure2_check 只验已提供的线）、渲染输入与 check 输入的同一性、工单字段完备性——这些由工单字段约定＋−3 开工前置自检（3b.1）与交付自查申报承担，属用户已接受的残余风险（不设 validator 系用户 2026-08-18 拍板；首战后评估是否升级）。` | `不覆盖：渲染输入与 check 输入的同一性（PNG 未绑序列）、图表基数（少画／少引用图仍可能过闸，closeout 只核正文引用＝工单引用）——属用户已接受的残余风险（2026-08-18 拍板）；fig2 必画下限、工单字段完备性自 7.1.0 由 \`stage2_closeout\` 承担。` |
| analyze-workflow.md:185 | `删除台账后从非首轮继续、终态后追加或同时存在多个 terminal 都会被拒绝。` | `终态后重开只走 \`holder_distribution_scan.py reopen-cycle\`。` |
| analyze-workflow.md:173 | `＋1 完整性批评角色查 findings/结论清单缺口（必查全史极值清单）` | `＋1 完整性批评角色查 findings/结论清单缺口（局限条目取 \`a4_gate limits-extract\` 输出，禁硬编码；必查全史极值清单）` |
report-template.md 不改。codex 按 v3 表内存重算三册 104,455（split-run 27,623／analyze-workflow 34,333／report-template 42,499），docs_lint 默认与 `--all` 皆 exit 0；codex 按 v4 表重算 104,584（split-run 27,752／analyze-workflow 34,333／report-template 42,499），余 378；v5 :176-177 再加约 40 字节仍在内。若超，优先再删 :184 的括注，不得动针所在段。

## §D 版本
- `VERSION` → `7.1.0`；`pyproject.toml` `project.version` → `7.1.0`；`SKILL.md` 版本标记行同步。
- `CHANGELOG.md`：索引段最上一行 `- **7.1.0**（YYYY-MM-DD）…`；详细段 `## [7.1.0] - YYYY-MM-DD — −2 收口与重封命令化` 置于 `## [7.0.4]` 之上（活跃文件严格降序、不得与活跃或归档撞号，`changelog_lint.py:32/:54/:57`）。内容＝stage2_closeout（check/fill-workorder/amend/reseal）、`audit_release_gate --profile stage2-dryrun`、`figures_from_facts fig2-series`、`a4_gate limits-extract/downstream-check/seal_integrity_errors`、`holder_distribution_scan reopen-cycle`＋`find_snapshot` 登记优先、手册净减字节数、SUITE 增量；**标签四址订正另记 `labels vX.Y` 前缀条目**（W1-C 正式入库时由 Fable 补，本单不写）。`changelog_lint`、`test_version_consistency` PASS。
- 压缩钩子改动不属 skill，不进 CHANGELOG。

## §E 验收（Fable）
run_all 全绿（沙箱端口两项本机复跑）；FORGGIE 已发布案发布闸：在其钉版 764b60c（7.0.4）worktree 下 `audit_release_gate --profile new-analysis` PASS；HEAD 下**预期 BLOCK**"scan 语义与独立重算不一致"（W1 改扫描器 sha 使旧案钉版机制生效，不是 W3 回归，勿当失败）；三册 wc 合计 ≤ 104962 且五枚针仍在；`stage2_closeout --help` 列四子命令；MELANIA/COLLECT/LIT 原案子检查回测不回归。
**APU 0914（W2 挪来）**：副本被 A0.0 拦（accounting 绝对路径，未迁移重绑的复制案不可用），只能在**真案目录**跑。**用户 2026-09-16 裁决：先不真跑。** W3 验收对 APU 只到 `reseal --from a4 --dry-run`（只读）：**预期 A0.1 exit 2**——APU `provenance_ledger.json` 记 `entity_source_trace.py` `ff4b640a…`/44,747 B，HEAD 为 `e85acee4…`/46,034 B（7.0.3/7.0.4 改过该文件），stdout 须点名该文件并给两组 sha/bytes；在 7(e) 验收 worktree 下跑时 A0.1 可能先因**绝对路径不一致**触发（`handoff_manifest.py:818-820` 先比 realpath 再比内容），诊断须如实打印真实路径比较结果，不得包装成只做过内容比较（十五复⑥）；验收方法＝跑前跑后案根 **路径→sha 映射＋目录清单** 完全相同（不是 sha 集合；比较函数与含忽略项、仓库 `scripts/`、`MPLCONFIGDIR` 空目录、7(d) `sitecustomize` 导入阻断、7(e) 叠加施工件的验收 checkout（无缓存、无非预期文件、允许根目录 `.git` 管理文件，不要求 git 干净）五项要求同 §B 用例 7；真案 dry-run 在阻断环境下再跑一次，exit 码/stdout 同无阻断、本次专用标记文件不存在或为空）。A0.0 通过（`accounting_mode.json:190-191` 绝对路径在原案内）。到不了分支规划与失效表，这两项由 §B 用例 7b 覆盖。**完整 closeout PASS 记为待办，前提已核清、量级不小**（待用户裁决是否值得做）：①provenance 须在当前代码下重跑（算法漂移）→ 牵动 freeze 链；②initial scan 绑旧扫描器 `44e73d52…`/62,804 B，HEAD `8abcdd16…`/76,337 B，`semantic_payload` 整体比较必拒（已证，见头部）→ initial scan 须重生成，而它在 a4 `sealed_files` 内 → A4 须重封；③案根无 `whale_series.provenance.json`，须先 `fig2-series` 产旁车；④执行前整目录备份到**案外同级目录** `…/5.6筹码分析/_backup/APU分析_20260914_pre_reseal_<ts>/`（`cp -R`，禁 rm；不得放案根自身 `_history/` 内，否则递归复制）。**恢复顺序（若用户裁决做）**：initial scan 重生成 → `handoff_manifest.json` 更新并 verify（它封入 initial scan，APU `:133-136`，不能保留原 manifest）→ provenance 重放（绑 manifest，`:154-158`）→ freeze → A4／rounds／必要复核 → `fig2-series` 旁车 → closeout；重采集原始数据、重作全部判断、重跑全部复核路**不预设**，由重放差异决定。换言之 APU 在 7.1.0 下完整 closeout ≈ −2 从 initial scan 起重做，不是 reseal 单命令能覆盖的。
