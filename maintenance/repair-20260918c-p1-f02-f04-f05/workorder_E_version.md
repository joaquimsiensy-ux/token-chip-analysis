# 工单 E（v2，融合 codex 复核 r1 六条 E-R1-01～06）：版本落地 9.0.0 —— repair-20260918c-p1-f02-f04-f05 收官（F04 RPC envelope / F05 −2 价格双源收据契约 / F02 流通量通道）

> 性质：**纯文档/元数据工单**，零生产代码、零测试代码、零手册改动。**档位＝9.0.0（主）**，用户 09-18 裁决原话"版本允许升级9.0.0"：F05 把 −2 收口对价格双源收据的契约从"引用 {path,sha256} 在场"收紧为"真读收据、重算 verdict、哈希绑定主源、拒纯申报"，**存量案已绑定的旧收据（无 `price_file_sha256`）在 HEAD 下一律 BLOCK、须重跑 price_check 迁移**，属不兼容的持久化契约变更，符合 CHANGELOG 头部"主=不兼容的工作流/schema/入口边界变更"。
> v2 变更（`review_E_reply_r1.md`）：R1-01 严重级改"三条修复（P1×2、P2×1）"；R1-02 流通量既有选材分支归属改为 `stage2_closeout.flow_selection_errors`（删 state_from_facts 归因）；R1-03 F02 盲审改"9 例逐例复算：8 例 RED→GREEN、1 例 GREEN→GREEN"；R1-04 存量描述按台账 Q8 四类拒收对象重写、ARC 引用结构与所指旧收据分述、Q9 字符串分支条件保留、删无依据的全量概括；R1-05 F02 免迁移补"未声明且保留原 facts"条件与重 build 后须刷新的绑定清单；R1-06 持久化字段计数改"新增产物输出键 3（收据 1、facts.token 2）"。
> 内容基线：HEAD 以 `construct_E_prompt.md` 派工头部为准（不以具体 SHA 判定，按 §0 检查）；三段施工 commit：F04 `d11730d`、F05 `47e9efb`、F02 `3ab1874`。

## §0 纪律与白名单
- 禁读 `~/.codex/`（启动搜索若已读 memories 披露一次）；禁读 `archive/`、`blind-reviews/`、`.staging_*`、`references/attic.md`、本目录（`maintenance/repair-20260918c-p1-f02-f04-f05/`）以外的历史 maintenance 目录、`/Users/uravvv/Desktop`、`/Users/uravvv/Documents`。离线。不 commit（Fable 验收后提交）。禁 stash/checkout/reset。
- 开工检查：`git status --short` 为空；`git diff --stat 8b041842 HEAD -- references SKILL.md commands-staging VERSION pyproject.toml CHANGELOG.md` 为空（scripts 已含三段八文件属预期，不在本单议题）。不符停工写 `E_done_attempt<N>_stopped.md`。
- **只允许修改四个文件**：`CHANGELOG.md`、`VERSION`、`pyproject.toml`（仅 :15 `version =` 一行）、`SKILL.md`（仅 :23 版本注释一行）；另允许新增本目录 `E_done.md`。`references/`、`scripts/` 一字不动。
- 本单是三段的版本登记，不重新评价施工；若 §2 文案与仓库事实不符（行号、字节、命令名、测试名、计数），退回本单而不是自行改写事实。

## §1 目标
VERSION（权威）= `pyproject.toml` `version` = CHANGELOG 最新条目 = `SKILL.md:23` 注释 全部为 `9.0.0`；CHANGELOG 新增索引一行与详细条目一段，体例与 8.0.0/7.2.1 一致；`test_version_consistency` 绿、`wc -c SKILL.md` 改前后相等（8021，`8.0.0`→`9.0.0` 等长）；`changelog_lint`、`docs_lint --all` 因读取 `archive/` 由调度方本机跑（施工方不跑；调度方施工前基线"活跃 78 条"，施工后复跑确认 79）。

## §2 改法（逐文件）
1. `VERSION`：`8.0.0` → `9.0.0`（保持原有换行形态）。
2. `pyproject.toml:15`：`version = "8.0.0"` → `version = "9.0.0"`。
3. `SKILL.md:23`：`<!-- skill-version-source: VERSION; skill-version: 8.0.0 -->` → `… 9.0.0 -->`（字节数不变）。
4. `CHANGELOG.md` 两处插入（施工者不运行 `changelog_lint`）：
   - 在索引区 `:13`（锚 `- **8.0.0**（2026-09-18）codex 7.2.1 六视角 review 两条 P0 修复`，行首片段，唯一）**之前**插入一行，逐字：
     ```
     - **9.0.0**（2026-09-18）codex 8.0.0 六视角 review 三条修复（P1×2、P2×1），−2 收口价格双源收据契约收紧（旧收据/纯申报/FAIL 一律拒、存量案须重跑 price_check 迁移，故记主版本）：F04 `RpcPool._one` 对无 error 且缺 `result` 键的 JSON-RPC 响应判失败（原先记 `ok=True, result=None`，`rpc_batch getcode` 再把 None 折成 `0x` 记 EOA、摘要"失败 0"），getcode 只认 `0x`/偶数长度十六进制串，其余记 error 退出 1；F05 `stage2_closeout` 新增 `price_receipt_errors`——按 points 以 price_check 同规则重算 verdict 并要求一致、只放行 PASS/WARN（WARN 记 NOTE）、收据新字段 `price_file_sha256` 须等于 `bindings.price_source.sha256`、内联 `dual_source_check` 须带 receipt 引用（原先只核 {path,sha256} 在场，FAIL 收据绑进工单仍 PASS）；F02 `facts_inputs.circulating_supply {raw,asof,source}` 可选通道→`facts.token.circulating_supply_raw/circulating_supply_source`，扁平键 `circulating_supply_raw` 拒，closeout NOTE 记口径（原先流通量分母无任何生产者，"必画"按流通量口径永不触发）。references/SKILL/commands 零改动。F01（日线取最高价）用户裁决不修；F03 峰值 override 沿 8.0.0 裁决不修；Q13 三端点 failover 轮转、Q14 ALL_SKIP 人工回退口子登记待裁决。
     ```
   - 在 `:96`（锚 `## [8.0.0] - 2026-09-18 — codex 7.2.1 六视角 review 两条 P0 修复`，行首片段，唯一）**之前**插入下面整段（段后空一行再接原 8.0.0 标题），逐字：
     ```
     ## [9.0.0] - 2026-09-18 — codex 8.0.0 六视角 review 三条修复 P1×2、P2×1（RPC envelope 缺 result / −2 收口真读价格双源收据 / 流通量声明通道）

     - **出处与裁决**：codex 对 8.0.0（8b041842）的六视角 review 判 PASS 需修复轮（P0 0 / P1 5 / P2 2 / P3 1）。调度方对照三轮 review 归因：F02 为 7.2.0 R07 修复带入的半成品（`stage2_closeout.flow_selection_errors` 早已按流通量口径判"必画"，但 `facts_gate` 从未产出该分母）；F05 为 7.0.4 引入时即如此；F04 为 3.16.0 起历史漏审。用户 09-18 裁决：①F01（日线取最高价当收盘）影响面有限不修；②修 F02/F04/F05；③原则＝skill 上下文不增、能删不增能改不增；④调度方只写工单/验收/调度，代码全由 codex 施工，codex 常规盲审、三次 FAIL 才换 opus；⑤版本升主 9.0.0；⑥APU 0914 案暂不重跑 price_check（进 −3 时补）。修的终点＝review 附录 C 三反例（`rpc_missing_result`、`price_gate_content`、`flow_migration`）在 HEAD 上必变拒，由盲审方与收官 review 独立复现，不以盲审 PASS 为准。
     - **F04**（`net.RpcPool._one`；`rpc_batch getcode`）：传输正常、无 truthy error 且响应非对象或缺 `result` 键 → `{"ok": False, "error": "rpc envelope: missing result (no error object)"}`（固定文案不拼端点/原文；合法 `"result": null` 键在场仍 `ok=True`，各方法消费者自核类型）；getcode 对 `ok` 结果用 `re.fullmatch(r"0x(?:[0-9a-fA-F]{2})*")` 校验，`0x` 为 EOA、其余偶数十六进制为合约，非字符串/奇数/非十六进制/缺失/null 记 `{"error": …}` 走既有 fail-loud 退出 1（原先 `r["result"] or "0x"` 把 None 折成 EOA；整数返回直接 `TypeError`）。同文件握手 `_attest_endpoint` 本就严格，两套标准合一。既有 11 处 RpcPool 消费者只让"缺 result"从假成功变为各自既有 `ok=False` 分支，不新增分支；`_run` 三端点 failover 轮转（A/B 坏时 C 不被访问）为既有缺陷登记 Q13 不修。测试：`test_batch1_rpc_attestation` 新增 envelope 用例与 getcode 七场景（missing/null/odd/badhex/int/eoa/contract，逐场景独立 RED：五异常场景基线 rc 0 误判 EOA/合约或 TypeError）。盲审独立复现：单端点握手 `0x38` 通过、业务响应 `{"jsonrpc":"2.0","id":1}` → 基线 `ok=True/result=None`、getcode rc 0"EOA 1 | 失败 0"；HEAD `ok=False`、rc 1、该址只记 error、"合约 0 | EOA 0 | 失败 1"。
     - **F05**（`price_check.py`；`stage2_closeout.price_receipt_errors`／`workorder_errors`）：收据对象唯一新增字段 `price_file_sha256`（主价格文件 SHA-256；stdout/退出码不变）；closeout 新 helper 取引用（`bindings.price_source_checks`，否则内联 `price_source.dual_source_check.receipt`，皆无＝纯申报拒）→读收据→`points` 非空→按 `PRICE_POINT_STATUSES` 重算 verdict（任一 FAIL/未知→FAIL，全 SKIP→ALL_SKIP，含 WARN→WARN，否则 PASS）须与收据 `verdict` 一致→只放行 PASS/WARN（WARN 记 NOTE "价格双源 WARN 点 N 个"）→`main_source`/`second_source` 非空→`price_file_sha256` 在场且等于 `bindings.price_source.sha256`；引用仍进 `required_refs` 走既有结构/在场/哈希/围栏遍历（收据路径越界或符号链接会得 helper＋遍历两条诊断，主源路径负例仍 1 条）。closeout 仍 12 项 checks、`workorder_errors` 返回形状与 `WORKORDER BLOCK: ` 前缀不变、19 条既有引用 mutation 仍 BLOCK。夹具改由真实 `price_check.main()`（第二源离线替身）生成收据、价格序列改三日（生产者要求 ≥2 天）。测试：`test_stage2_closeout.price_receipt_content_enforced` 七段（FAIL 拒/手改 verdict 拒/WARN 放行记 NOTE/哈希错绑拒/旧收据缺字段拒/ALL_SKIP 拒/内联纯申报拒＋内联带 receipt 放行），逐段先核生产者退出码（FAIL 2/WARN 0/ALL_SKIP 3/PASS 0）再核消费端，段 1–6、7a 基线 RED。盲审独立复现（内存文件系统、真实 `price_check.main()` 与 `check` 入口）：主 50/副 100 三点 FAIL 收据绑进其余合法工单 → 基线 check PASS；HEAD `WORKORDER BLOCK: bindings.price_source_checks.verdict: PASS|WARN（FAIL/ALL_SKIP 禁入装配：换源或人工裁决后重跑 price_check） != FAIL`、退出 2；340 组合法状态组合 verdict 汇总与生产者一致。
     - **F02**（`facts_gate.derive_facts`；`stage2_closeout.flow_selection_errors`）：`state_source.facts_inputs.circulating_supply {raw, asof, source}` 可选——raw 经 `_raw_str` 归一且 `0 < raw ≤ total_supply_raw`、asof 严格 `YYYY-MM-DD`（`date.fromisoformat` 往返相等）、source 非空口径说明；写入 `facts.token.circulating_supply_raw` 与 `circulating_supply_source {asof, source}`；扁平键 `facts_inputs.circulating_supply_raw` 明确拒"键名错位"；无声明时 token 字典与 8.0.0 逐字节相同（同版本 build 幂等，仅 `provenance.producer.sha256` 随生产者变）。消费侧 `stage2_closeout.flow_selection_errors` 早已按流通量分母判"必画"，本轮只补 NOTE "流通量 N（口径 source，asof）"；发布闸 `check_facts_vs_ledgers` 整 token 字典比对自动覆盖手改。流通量为第三方口径人工声明（CG/CMC/项目方披露），不做链上重算（Q11）。测试：`test_report_facts` 用例 22（声明→token 带字段、发布重算一致）/23（六非法变体各拒）/24（手补 facts 流通量无声明→闸拒，基线即绿）、"21 类"→"24 类"；`test_stage2_closeout.circulating_supply_producer_to_consumer` 用 `_r07_case` 独立夹具（total 1000/e1 current 100＝10% 总量分支不命中；声明 400→25% 命中必画）固化 producer→consumer 贯通。盲审独立复现：自行构造 total 1000/e1 current 100 夹具——无声明 10% 空 flow `errors=[]`；声明 400 后 token 新增两键、100/400=25% 报 `WORKORDER BLOCK: flow.eligible_entity_ids: 包含下限 ['e1'] != []`；扁平键抛“键名错位”；声明 400 而 facts 手改 401 → 发布闸“facts.token 与三账重算值不一致”；六非法变体（非 dict/浮点 bool 负数零/超总量/非严格 ISO 含 2026-02-30/空 source/扁平键）逐一拒；无声明时 token 序列化字节与基线相同、旧 facts 经 HEAD 发布闸仍通过。
     - **工艺**：三份工单先 codex 只读复核（并行）：F04 r1 退回 3 条→v2 r2 退回 2 条→v3 r3 通过；F05 r1 退回 5 条→v2 r2 退回 3 条→v3 r3 通过；F02 r1 退回 2 条→v2 r2 通过（十五条意见全部亲核代码属实后吸收，含 R1 揭穿的十六进制未校验、RED 预期与基线不符、amend 误用、夹具供给量前提错、逐字节不变不成立等）。施工串行 codex `--write`：F04 attempt1/2 两次按纪律停工（派工 HEAD 标注被提示词自身 commit 推后；§0.8 测试子进程执行历史 maintenance 脚本撞 §0.2 禁读，三工单补"测试依赖豁免"），attempt3 完成；F05/F02 各 attempt1 完成。每段 Fable 本机验收（diff 与工单逐字对照、RED 分布、定向测试、三处字节）后 commit，再 codex 常规盲审：F04 r1 FAIL 仅提示词口径（diff 范围误含 maintenance、沙箱临时目录不可写）→r2 PASS；F05 r1 PASS；F02 r1 PASS（9 例逐例复算：8 例 RED→GREEN、1 例 GREEN→GREEN）。收官 codex 整体 review 三反例独立复现 通过（`final_review_reply_r1.md`：三反例基线可复现、HEAD 全拒；F05 九场景、F02 五分支、F04 五种坏返回逐一核；流通量声明×价格收据交叉五组互不破坏；八文件 +317/−16）。本机环境项：macOS 27 `system_profiler` 字体枚举返回空致 matplotlib 3.11 从零建缓存 KeyError `_items`，测试自设 MPLCONFIGDIR 必重建——跑测试须 `MPLCONFIGDIR=$HOME/.matplotlib`（台账 Q15，不改代码）。
     - **字节与测试**：references 930076、SKILL.md 8021、commands-staging 8798 三处零改动（契约由 CHANGELOG 与 docstring 承载；`invariant_manifest`/`contract_manifest` 不动，`invariant_scan` 三段前后均 PASS）。scripts 改动八文件：F04 net.py/rpc_batch.py/test_batch1_rpc_attestation.py（+82/−4）；F05 price_check.py/stage2_closeout.py/test_stage2_closeout.py（+144/−10）；F02 facts_gate.py/stage2_closeout.py/test_report_facts.py/test_stage2_closeout.py（+92/−3）。本机定向测试 F04 14 项、F05 6 项、F02 11 项全 PASS；`test_stage2_reseal` 在 F05、F02 落地后各补验 21/21；run_all 151/151 全过（`MPLCONFIGDIR=$HOME/.matplotlib` 下，含 `test_stage2_reseal` 21/21）；九项守卫全 PASS。登记不修台账 `maintenance/repair-20260918c-p1-f02-f04-f05/code_change_pending.md` Q1–Q15。
     - **档位与迁移说明**：9.0.0（主）——F05 契约收紧不兼容，新增拒收面四类（台账 Q8）：①旧收据（当前 `price_check.py` 之前产出，无 `price_file_sha256`）；②自定义格式收据（非 price_check schema，如 APU 0914 的 primary/rows/comparisons 结构）；③纯申报内联 `dual_source_check` dict（无 `receipt` 引用）；④已绑定的 FAIL/ALL_SKIP 收据。ARC 案内联 dict 带 `receipt{path,sha256}` 的引用结构可保留，但所指旧收据仍须迁移；内联为字符串的形态在无顶层 `price_source_checks` 引用时基线本就 BLOCK（Q9），本轮不新增影响。命中任一类的存量案在 HEAD 下完整 `stage2_closeout check` 一律 BLOCK。迁移步骤＝用当前 `price_check.py` 重跑收据 → 直接更新工单 `bindings.price_source_checks`（或内联 `dual_source_check.receipt`）的 path/sha256 → 完整 `check` 至 PASS → `--receipt-only` 核验；**不得用 `amend`**（`bindings` 属冻结字段，frozen_sha256 漂移直接拒）。用户裁决 APU 0914 暂不重跑，进 −3 时补。ALL_SKIP（所选第二源全部抽样日期无对照）不再能进 −2 收口，`report-template.md` 的人工对图回退路径在本契约下失效，四条正式候选链均可能且不限币龄（Q14 待用户裁决是否另立小工单开人工旁证口子）。F04：行为只在坏包时从假成功变失败，已落盘 getcode 产物不追溯。F02：可选字段——未声明且保留原 facts.json 的旧案无需迁移；主动重 build 会改 facts 文件哈希，须随之刷新 A4、图 2 旁车/对账收据、工单与 closeout、A5/HTML 绑定；新声明流通量若使实体新命中"必画"下限，须补流转图及报告引用。
     - **成本-质量指标**：生产逻辑文件 5（net、rpc_batch、price_check、stage2_closeout、facts_gate）、新公开入口 0、新增产物输出键 3（收据 `price_file_sha256` 1；facts `token.circulating_supply_raw`、`token.circulating_supply_source` 2，均可选）、新增 SUITE 入口 0；外部网络调用 0（RPC 与第二源全部替身）；不运行真实案卷判断链，判断结论不自动变更。
     ```
   - 其他行不动；`archive/CHANGELOG-archive.md` 不动。

## §3 验收（施工者自跑并记入 E_done.md）
- `git status --porcelain=v1 --untracked-files=all` 只列四个文件（M）与 `E_done.md`（??）；`git diff --exit-code HEAD -- references/ scripts/` 退出码 0。
- `python3 -B scripts/tests/test_version_consistency.py` exit 0；`wc -c SKILL.md` 改前后均 8021。**不跑** `changelog_lint`、`docs_lint --all`（读 archive/，调度方本机跑并记 78→79）、不跑 run_all（调度方已跑并填入 §2）。

## §4 报告 `E_done.md`
首行 `# 施工 E：完成` 或 `# 施工 E：停工`；列四文件各自 diff 行数、`test_version_consistency` 退出码与输出行、`wc -c SKILL.md` 两值。不 commit。
