# 用户裁决 2026-09-18（第三轮，对 codex 8.0.0 六视角 review 8b041842 的 P1/P2）

出处：`~/Documents/5.6筹码分析/token-chip-analysis-main-v8.0.0-8b041842ea96-sixlens-full-review-2026-09-18.md`（PASS 需修复轮，P0 0 / P1 5 / P2 2 / P3 1）。

| review 编号 | 内容 | 引入归因（Fable 已按 git 历史逐条核实） | 裁决 |
|---|---|---|---|
| F02 · P1 | 流通量选材分支存在，但 facts 生产者无流通量通道；手补被发布重算拒 | 修复中新引入：consumer 82bb26c（7.0.4）→ producer 1b317b3（7.1.3→7.2.0 R07） | **修**（本工程段 F02） |
| F04 · P1 | RPC 响应缺 `result` 键被当成功空代码，getCode 判成 EOA、摘要"失败 0" | 历史漏审 637df73（3.16.0） | **修**（本工程段 F04） |
| F05 · P2 | closeout 对价格双源收据只核路径/哈希在场，不核 verdict；真实 FAIL 收据仍 PASS | 82bb26c（7.0.4）引入时即如此 | **修**（本工程段 F05） |
| F01 · P1 | ISO 日内 CSV 截断日期后取到当日最高价 | 历史漏审 8e566ef（3.19.0） | **不修**：用户判影响面有限（只影响核对环节，不改正式价格序列） |
| F03 · P1 | 峰值 override 证据仍是自报（三轮连报） | 半修复 1b317b3→332a582 | 维持 0918 晚裁决：威胁模型＝自己人，登记不修 |
| F07 · P1 | 价格 NaN 判 PASS（三轮连报） | 历史漏审 8e566ef | 本轮不在名单；与 F01 同属 price_check 解析层，登记台账 |
| F08 · P2 | 完整回归依赖 .staging_b3 与 /tmp/w3_acceptance | 6.51.0 / 7.1.0 | 已知环境项，不修 |
| F06 · P3 | multicall 跨链示例缺 `--chain` | 6.30→6.36 | 不修 |

用户原话（2026-09-18）："看起来 F01 影响面很有限，因为某天的最高价当成收盘价，对整体判断的影响不大，但是 F02、F04、F05 影响比较大，这一轮就修这三个：修复要遵循的原则：避免 skill 上下文增加、能删除的不新增，能修改的不新增，你只负责验收和执行多 Agent 调度，保持上下文干净隔离：不要读取、展开或透传其他子 Agent 的原始会话痕迹、内部执行栈帧、中间件 Scratchpad 日志，所有代码相关的改动都让 codex 去做，不要使用攻击性验收，用 codex 正常盲审即可，如果 codex 连续三次盲审失败就用 opus 替代。每次施工都要写工单——给 codex 复核——没问题再派 codex 去实施——然后再用 codex 盲审——最后都修完后用 codex review，确保问题真正解决了再收工"。

## 终点判据

修的终点是问题消失，不是盲审 PASS。review 附录 C 三个反例在 HEAD 上必须变为拒/失败，由盲审方独立复现：

- `rpc_missing_result`（F04）：握手 `eth_chainId` 正常返回 `0x38`，业务 `eth_getCode` 返回只有 jsonrpc/id 的对象 → `rpc_batch.py getcode` 必须退出 1，该地址记 `{"error": …}`，摘要"失败 1"，不得写 `is_contract=false`。
- `price_gate_content`（F05）：真实 `price_check.py` 产出的 FAIL 收据（主 50/副 100，退出 2）绑定进工单 → `stage2_closeout check` 必须 BLOCK（退出 2），错误文案指向收据 verdict。
- `flow_migration`（F02）：`_r07_case` 夹具（总量 1000、e1 current 100）声明流通量 400 → facts 生产者产出 `token.circulating_supply_raw="400"`、发布重算一致，`flow_selection_errors` 对空 flow 必须报"包含下限 ['e1']"；反例中直接往 `facts_inputs` 写扁平键 `circulating_supply_raw` 的写法必须被 derive 明确拒（不得静默忽略）；手补 facts.token 而不经声明仍被发布重算拒。

## 施工顺序与流程

F04 → F05 → F02（改动小→大）。每段：Fable 写工单 → codex 只读复核（三份复核可并行）→ 亲核采纳 → codex 施工（串行，`task --write --fresh`）→ Fable 本机验收 → codex 常规盲审 → 段收官。三段全收官后：codex 对整体 diff 做一次 review（终点判据三反例独立复现）→ run_all + reseal 补验 + 九项守卫 → 版本登记 → push。

## 版本档位（待用户追认）

候选 **8.1.0**：F02 为 facts/state_source 可选字段新增（向后兼容）；F05 收紧 closeout 契约并给 price_check 收据新增 `price_file_sha256`（旧收据在 closeout 被拒＝存量 −2 案须重跑 price_check）；F04 为行为收紧。按 CHANGELOG 头部规则可辩为 minor；若用户按上两轮先例判"契约收紧＝主版本"则改 9.0.0。收官段 E 前置顶请用户拍板。

## 追加裁决 2026-09-18（同日稍晚）

- **版本档位＝9.0.0（主版本）**：用户原话「版本允许升级9.0.0」。依据：F05 收紧 closeout 价格收据契约、旧收据一律拒＝不兼容变更；沿用 7.2.0/7.2.1→8.0.0 的"契约收紧＝主版本"先例。收官段 E 以 9.0.0 登记（VERSION / pyproject.toml:15 / SKILL.md:23 / CHANGELOG 索引行＋详细段），不回改历史条目。
- **APU 0914 案暂不重跑 price_check**：用户原话「APU也暂时不需要重跑」。该案进 −3 前仍会被新 closeout 拒（Q8 代价成立），届时再按 Q8 步骤补收据；本工程不动案目录。
