# 工单G2复核r4：通过

**G2-R3-01 已消化；未发现本次修订引入新问题。**

实际核查项：

1. **§2.6，第 115 行**：已按链族区分。EVM 在绑定合法且 facts/config decimals 相等时，非 dict checks 基线返回 `[]`，改后须拒收；Solana 基线拒收须保持。
2. **§2.9，第 131 行**：使用 EVM 夹具，断言不抛异常且 errors 含 `checks 非对象`，对应 RED→GREEN；明确不使用 Solana 作该例夹具。
3. **§2.11，第 143 行**：清单已列 `checks 非对象` 并引用 §2.9。三处口径一致。
4. **增量范围**：已核指定 `git diff HEAD~1 -- <工单>`。v3=`93419af`，v4=`5a3f6a6`，仅标题、v4 变更记录、§2.6 说明句三处变化。在内存中还原这三处后，全文与 v3 **逐字节相同**，无其他改动。

完整报告已打印到 stdout。本轮进行了差异比对和函数静态核对，未重跑锚点表、迁移核查或测试。全程只读、离线，未改文件、未 commit、未读取禁读内容；工作区干净。
