# 工单G2复核r3：退回

**G2-R3-01：§2.6 的基线说明未同步修订。**

[工单第 114 行](/Users/uravvv/.claude/skills/token-chip-analysis/maintenance/repair-20260918b-p0-fig2-decimals/workorder_G2_decimals.md:114) 仍写：

> 基线对此返回拒收理由，须保持。

这没有区分链族。内存实跑确认：合法绑定且 facts/config decimals 相等时，非 dict checks 在 **EVM 基线返回 `[]`**；Solana 基线才捕获异常并返回拒收理由。应同步改成 §2.9 已采用的分链说明。

其余核查结果：

- **锚点通过定位核验**：两处 schema 各唯一命中。40 条引用行中，37 条按 `-x` 唯一；两个界符按复合锚唯一；一个片段按 `-F` 唯一。
- **回归修订通过**：`checks 非对象` 匹配实际输出，EVM RED→GREEN 成立；§2.11 已列全四处。
- **迁移说明通过**：§4/Q7 三类失效原因与代码一致。
- **增量检查**：未发现新增函数逻辑问题；上述错误说明沿自 v2，与新版 §2.9 冲突。

完整报告及逐项锚点表已打印到 stdout。回归验证限于内存中的单函数路径，未运行完整 `gate.run`；全程只读、离线，未改文件。
