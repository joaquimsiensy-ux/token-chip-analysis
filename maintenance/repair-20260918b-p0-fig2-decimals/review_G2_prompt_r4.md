# 工单 G2 复核提示词（只读，r4：核 v4 对 r3 唯一一条 G2-R3-01 的修订）

## 纪律
同 `review_G2_prompt.md` 第 1–3 条；报告全文打印到 stdout，首行 `# 工单G2复核r4：通过` 或 `# 工单G2复核r4：退回`，退回逐条编号 G2-R4-NN。

## 任务
只核 `workorder_G2_decimals.md`（v4）对 `review_G2_reply_r3.md` 的消化：
a) R3-01：§2.6 说明句的 checks 非 dict 基线行为是否已改为分链族表述，且与 §2.9 用例、§2.11 清单三处口径一致（EVM 基线 `[]`→改后拒收；Solana 基线拒收须保持）。
b) v4 相对 v3 除标题/变更记录/§2.6 该句外有无其他改动（`git diff HEAD~1 -- <工单>` 应只这三处）；有无引入新问题。
不需重跑锚点表与迁移核查（r3 已通过）。
