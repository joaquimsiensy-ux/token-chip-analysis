# 工单G1复核r2：通过

已对比 v1（0f448c0）与 v2（b0d92b8），仅复核本轮变更。未发现需要退回的问题，完整报告已打印到 stdout。

1. **R1-01 已消化。** [§2.5 用例1](/Users/uravvv/.claude/skills/token-chip-analysis/maintenance/repair-20260918b-p0-fig2-decimals/workorder_G1_fig2.md:108)同时要求“先执行两端”和“RED 取证时逐条捕获断言”。内存执行真实函数，访问顺序为 `producer → consumer → assert:producer → assert:consumer`，基线确实取得两条独立 RED。§0.7 已订正为 §2.4/§2.5。

2. **R1-02 用例成立。** 按 §2.1/§2.2 在内存修改真实校验函数，收据使用输入字节的实际 SHA-256，结果如下：

   | 输入 | 基线 producer／消费者 | 修改后 producer／消费者 |
   |---|---|---|
   | 必画 e1，空 series | rc=0／errs=[] | rc=1／拒，缺 `['e1']` |
   | 必画 e1、e2，仅画 e1 | rc=0／errs=[] | rc=1／拒，缺 `['e2']` |
   | 补齐 e1、e2 | rc=0／errs=[] | rc=0／errs=[] |

   缺线时两端错误均含 `缺必画实体线 ['e2']`，消费者另带 `figure2 发布期重算:` 前缀。e2 为 `100/1000×100=10.0%`，误差 0pp；e1 浮点误差约 `3.55e-15pp`，均在 0.05pp 容差内。补齐段读取重新生成的收据验证放行。

3. **R1-03 没有残留错误前提。** G1 第5行明确转交 G2。G1 剩余“基线 rc=0、errs=[]”针对图2路径，与本轮执行结果一致；没有继续声称 facts/config decimals 不一致时基线放行。本轮未复核 G2 修订结果。

4. **行号、锚点和白名单正确。** importer 文件名确在 `test_repair_batch_c.py:1641/:2117`，加载调用在 :1644/:2121；未读取或加载 importer。三个测试插入锚分别唯一命中 :1563、:1584、:1601。v2 未改变原生产修法及白名单，新增用例仍位于允许修改的测试文件内，未引入新的生产回归面。

5. **§4 迁移说明的核心结论成立。** [发布闸](/Users/uravvv/.claude/skills/token-chip-analysis/scripts/report/audit_release_gate.py:1645)重算会拒绝缺线、重复线的旧 PASS 收据；schema 保持 v1，合法完整且绑定有效的旧格式收据仍可通过。CHANGELOG 交收官段 E，没有突破白名单。A4/stage2/A5 可作为完整迁移流程，但各封口是否必须重建取决于实际哈希绑定变化，不能理解为只改 series 就必然使所有 A4/A5 失效。

**不阻塞的文字勘误：** §2.5 标题仍写 `_g1_case_1..3`，建议改为 `_g1_case_1..4`；正文已明确四个函数、四行调用。

验证采用真实源码函数的内存执行，未实跑 CLI 或完整发布流程。全程离线、只读，未读禁区、未改文件、未提交；结束时工作区干净，`scripts/` 相对 f1f473f3 的提交差异为空。
