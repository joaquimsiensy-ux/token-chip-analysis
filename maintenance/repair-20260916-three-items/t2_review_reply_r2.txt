# 复核 T2 R2: 通过

1. 通过——行号、锚文本、字节与 docs_lint 均符合。
已用 nl -ba 亲核 report-template.md 的 :145、:147、:148、:149、:150、:153、:222、:265。实际为七处替换、一处新增，与 v2 一致。
UTF-8 独立精算：42499 → 42491 字节，净减 8；逐项差值为 +20、−6、−27、−17、−15、+120、−83、0。
对内存替换后的文本实际运行 docs_lint.main(True)：退出码 0，59 个文档 PASS。未写入文件。无需改法。

2. 通过——零代码方案成立，但 DISCLOSED 有前提。
a5_report_seal.py:153–172 识别行首一至六个 # 加空白的标题；指定标题包含“翻转披露”，能够命中。正文只写“见附录 F”不影响核验。
真实切片函数和 provenance_flip_bundle 的内存实验结果：附录 F 写法 DISCLOSED；六级标题仍 DISCLOSED；提前出现同名标题、F 内插入子标题、披露值仅在切片外，均被拒。
隐藏前提如下：
- :161–171 按 locations、候选串、标题顺序取首次命中；包含空格的位置串还会尝试最后一个空格后的尾串。
- :170 遇下一任意级标题即截止；没有下一标题则一直取到文末。
- :185–234 仍要求 new-analysis、真实翻转及合法且绑定一致的 ledger、freeze、裁决收据。
- :249–255 只检查切片内字符串是否存在，不验证策略、地址与份额是否逐行对应；其他章节的地址不能补足切片缺项。

工单新增“唯一”“F 内不再分标题”足以约束本方案。无需改 seal；不能将 DISCLOSED 理解为正文零地址检查通过。

3. 通过——新增用例位置与作用域正确。
test_repair_batch_d.py:538 后、:539 前是正确插入点。a5/hm 定义在 :512–513，tmp 在 :515，real 在 :525，a4obj 在 :526；夹具 :375 确为 ["翻转披露"]。
内存插入后的完整测试源码编译通过；工单新增代码原样在内存夹具下执行，断言通过。
验证边界：内存实验替代了上游收据验证和文件读取；原测试会创建临时目录、写夹具并启动子进程（:514–524），因此本次只读复核未直接执行完整测试脚本或 run_all，不宣称集成测试全绿。未发现需修改的变量或调用。

4. 通过——正文零地址与附录 B/F 分工自洽。
:53 限制正文地址及“标签↔地址对照”的位置；:284 要求正文零地址、B 表完整。新 :222 将披露定位到 F，新 F 行只允许各策略主导终点完整地址与份额，不替代 B。
新 :265 的“F 仅限翻转主导终点”构成紧接 B/JSON 规则的明确例外，未扩大为任意地址清单。无需再改 :53/:284。

5. 通过——未发现必须同步的正文披露旧规定。
对 references、commands-staging 及其他现役 Markdown 搜索“翻转披露／披露章节／report_locations”：
- scan-schemas.md:366 描述标题切片和收据绑定，不限定正文，保留正确。
- split-run.md:170③ 要求“报告含完整披露章节”，附录 F 属于报告章节；①的“在正文”只修饰分布图，不约束③，确实不必改。
- analyze-workflow.md:195 要求对最终 Markdown 重验披露，并指向报告模板，无冲突。

结论针对 T2 v2 工单可施工性：未发现需退回的硬伤。全程离线，未改文件，未执行 Git 写操作，未读取禁止目录。
