# 复核 T2: 退回

退回原因：零代码方向成立，字节预算与 docs_lint 均通过；但保留 report-template.md:265 不动，会留下“完整地址只在附录 B 与 JSON 附录”与新增附录 F 的直接冲突。

以下行号均为施工前基线。

1. 通过：五处锚点、字节数与 docs_lint。

已用 nl -ba 核对 references/report-template.md:145、148、149、153、222，行号与锚文本全部吻合。

从仓库工单提取替换文本，在内存中实施，按 UTF-8 编码精算：
改前 42499 字节；改后 42489 字节；净减 10 字节。
五处变化依次为 +20、−27、−17、+117、−103 字节。

将目标文件读取重定向到内存替换结果，执行真实 docs_lint.main(all_mode=True)：退出码 0，59 个文档 PASS。该检查不证明条文语义自洽。

2. 通过，但须补明边界：seal 无需修改。

a5_report_seal.py:153–172 按标题子串定位，不要求披露位于正文。指定标题“## 附录 F：溯源三策略翻转披露”可以命中夹具的“翻转披露”；正文只写“见附录 F”、不含地址，不影响 DISCLOSED。

内存定向执行未修改的两个源码函数：新增用例返回 DISCLOSED，改为三级标题仍 DISCLOSED；插入子标题、前置同名摘要标题、将披露值移到其他章节，均被拒绝。此次文件读取和收据绑定依赖使用替身，不能冒充真实夹具端到端通过。

隐藏前提：
- :160 只认行首的 1–6 个 # 后接空白；不是任意 Markdown 标题形式。
- :170 遇到下一个任意层级标题即结束切片；附录 F 下另开“### 明细”会截断。
- :161–171 按位置串、候选串、标题顺序首次命中即返回，不会寻找后续内容更完整的同名章节。
- :185–234 仍要求 new-analysis、真实翻转、有效 ledger 和绑定收据；freeze 已记录哈希时还须一致。
- :244 并非始终只核 terminal[2]：为空时回退 terminal[1]。
- :249–255 只是切片内子串在场检查，不验证策略、地址、份额逐行对应，也不强制百分号或地址边界。

其他章节的地址不能补足切片缺项；但重复命中标题或切片内无关同串可能影响判断。它也不检查正文是否零地址。

建议将拟替换 :222 中的标题条件写为：
“标题含收据 report_locations 位置串且唯一命中；全部披露置于下一任意级标题之前”。

3. 通过：插入位置、变量与 receipt 一致；完整运行尚未验证。

test_repair_batch_d.py:537–538 的 check 调用结束后、:539 注释之前，插入位置正确。
real、hm、a5、tmp、a4obj 均已在 :512–526 定义且仍在作用域内；:375 的 report_locations 确为 ["翻转披露"]，无需修改。

插入后的完整测试文件已在内存编译通过；新增片段在上述替身环境中执行通过。原函数 :514 起创建临时目录，:516–524 写夹具并启动 trace，因此本次遵守零写入要求，没有直接运行原测试脚本或 run_all，不能认定它们全绿。

建议顺手修正新增示例的模板一致性：
“项目方钱包 W1”改为“项目方钱包#1”；
“## 附录 G：数据来源”改为“## 后续章节”。
这些不影响 seal 判定，但可避免示例违反钱包标签及附录 D 的既定用途。

4. 退回：地址例外范围没有闭合。

report-template.md:265 明确规定“完整地址只在附录 B 与 JSON 附录”。新增 F 放地址直接违反该排他条款，不能以“附录本就允许”解释掉。

:53 限制的是“标签 ↔ 完整地址对照”只放 B/JSON，可保留；:284 要求正文零地址及 B 表完整，也可保留。:146 的附录 A 规定的是交易哈希，不能作为钱包完整地址例外的依据。

建议具体修改：
- :265 改为：
  “- **正文（含表格）零地址**，一律钱包标签；地址只在 B/JSON；F 仅限翻转主导终点。”
- :153 后新增 F 行改为：
  “  F. 溯源三策略翻转披露（仅真实翻转；仅列各策略主导终点完整地址与份额，见图层同源④）”

附录 B 对照表仍不可省，F 不替代 B，也不扩展为其他地址清单。工单须撤销“:265 不动”，并对修订后的整套文本重新精算字节、运行内存 lint；42489 仅是当前工单原文的结果。

5. 通过：未发现其他必须同步的“正文披露”旧要求。

对 references 与 commands-staging 检索“翻转披露”“披露章节”“report_locations”，相关命中为：
report-template.md:222、scan-schemas.md:357/:366、split-run.md:170、analyze-workflow.md:195；命令目录无命中。

scan-schemas.md:366 规定标题切片与收据绑定，不指定正文位置，与附录 F 兼容。
split-run.md:170 第③项说“报告含完整披露章节”，没有要求该章节属于正文；同一行第①项的“在正文”仅修饰分布图。
analyze-workflow.md:195 要求对最终报告实物复验，不限定披露位置。

这三处不必为 T2 修改。另外，原 report-template.md:222 本身也未明说“正文”；工单对旧问题的描述宜改为“披露需终点标识，但地址允许位置未覆盖披露附录”。
