# 批 3 工单（codex 施工）：修复生产者 `sqd_gap_repair.py` ＋ 代/修复层/resolution/bundle/发布协议/CAS ＋ resolver 与身份闸 v2 ＋ validator repair 段 ＋ `--blocks-cache` 离线回归（分支 fix/sqd-gap-v6520）

- 前置：批 2 验收通过（探针可用、coverage validator 段、net.py 结构化状态、guard 覆盖 `data/sqd_coverage/`）。
- 权威：`PLAN.md` §4.1 A2.2／§4.2.0／§4.2.2–4.2.7／§4.2.10／§4.3.2–4.3.4／§4.4.1／§4.4.2／§4.4.6 ＋ `PLAN_errata_batch0.md` **E1/E6/E10/E17**（E9 探针侧已在批 2）＋ 契约草案（`canonicalization` `publish_protocol` `sqd-solana-repair-layer_v1` `sqd-solana-repair-bundle_v1` `sqd-solana-repair-pointer_v1` `sqd-solana-coverage-resolution_v1` `evidence_tables` `sqd-solana-cache_v4_repaired-meta` `sqd-solana-slot-index-map_v1` `rpc_ledger`）＋ `references/scan-schemas.md` §14 对应节（冲突：errata ＞ 草案 ＞ PLAN）。
- 目标对齐：交付**可在本机联网运行**的修复生产者（live 模式走 Helius getBlock；`--blocks-cache` 模式走本地参考缓存只产 exploration 代）、代/bundle/指针发布协议、resolver＋身份闸 v2、validator repair 段；**不碰 reconcile（批 5）与其他消费端（批 4）**。离线施工、不 commit、完成即停；行号/函数名与实况不符即停工报告。
- 开工门禁：`cd .staging_b2 && shasum -a 256 -c STAGING_B2_SHA256.txt`（21 OK）且 `cd .staging_b3 && shasum -a 256 -c STAGING_B3_SHA256.txt`（14 OK）。

## 参考实现与样本（只读；staging 不入库）
- `.staging_b2/arc_reference/routeA_full/run_full.py`：`helius_block`/`tx_records`/`is_nonce`/`b58decode`/`process`/`ledger`/`redact`——ARC 案 83 边实战代码，**移植语义重写成 skill 风格**（经 net.py、kernel 原子写、脱敏、契约协议）。
- `.staging_b2/arc_reference/routeA_full/hunt_remaining/hunt_step2_bisect.py`：β 余额连续性二分（SQD 免费 tokenBalances 探针，每 owner ≤40 次）。
- `.staging_b3/helius_full_blocks/{426649168,426651094,439000000}.json`：**原始 Helius getBlock full 响应**（两缺陷块＋一健康块，3.9–10MB）——抽小样本做单测 fixture（vote/nonce 判定、tokenBalances 产边、签名差集）；`.staging_b2/arc_reference/sqd_query_variants/sqd_census_{426649168,426651094,439000000}.json`：同 slot 的 SQD 整块普查（`transactionIndex/signatures/err`）——配对用。
- `.staging_b3/routeA_pilot/426649168.json.gz`：routeA 块缓存格式样本（`routeA-block-cache/v0`：`{slot, blockhash, sqd_blockhash, blockTime, helius_sigs[按位置全部签名], sqd_sigs[排序], missing_full[{pos,sig,nonce,failed,tx(含 meta pre/post tokenBalances)}]}`）；`repair_edges_pilot.jsonl`（20 边）、`pilot_slots.jsonl`、`ALL_REPAIRS_DONE.json`、`repair_edges_full.jsonl`（61）、`repair_edges_hunt.jsonl`（2）＝83 边真值；`shadow/MANIFEST.json`、`shadow_reconcile.json`。
- 现役只读复用：`scripts/solana/spl_edge_core.py`（`validate_edge_row` :19-35、`edge_sort_key` :38-41、`owner_deltas_by_tx`/`pair_tx`、`soltx_cache_paths` :230-237）、`scripts/solana/fetch_sqd_transfers_v2.py`（SQD 端点/分页/meta 写出字段 :521-537 等）、`scripts/lib/receipt_kernel.py`（`build_envelope` :126、`finalize_envelope` :159、`publish_exclusive` :574、`publish_overwrite` :590、`_stage` :327 只 fsync 文件）、`scripts/lib/endpoint_identity.py`（`redact_endpoint_text` :60）、批 2 交付的 `scripts/solana/sqd_coverage_probe.py` 与 `scripts/lib/solana_exact_validate.py` coverage 段、`scripts/lib/net.py`（批 2 后含 http_status/retryable/no_retry_statuses）。

## 白名单
1. 新件 `scripts/solana/sqd_repair_core.py`：纯函数核——规范化 JSON/`plan_digest`/`gid`（4.2.0＋E17：rpc_ledger 不进 gid）、签名差集、vote 判定（任一顶层指令 programId==Vote 程序）、nonce 判定（System 指令 data 小端 u32==4）、分类（nonce/other/missing_block/refuted）、产边（`meta.err==null`、`mint==本 mint` owner 级 delta → `spl_edge_core.owner_deltas_by_tx`/`pair_tx`，ts=blockTime）、slot_index_map 双射构造（SQD (index,signature) × 参考 (位置,签名,isVote) → 非投票序号；base 该 slot 每条边恰一解否则中止）、合并构造（base 非缺陷 slot 行 ∪ base 缺陷 slot 行重映射 ∪ 修复边，按 `edge_sort_key` 排序，逐行序列化与采集器一致，`edge_logical_sha256` 按 `_replay_with_evidence` 同算法）、routeA 缓存格式解析（非投票集合＝`sqd_sigs ∪ missing_full.sig`，前提 `sqd_subset`；缺失交易全量取 `missing_full[].tx`）。
2. 新件 `scripts/solana/sqd_gap_repair.py`（producer：`sqd-solana-cache/v4`（repaired meta，collector `sqd_gap_repair.py/v1`）、`sqd-solana-repair-bundle/v1`、`sqd-solana-coverage-resolution/v1`、`sqd-solana-repair-pointer/v1`）：子命令 `plan|repair|verify`；`--mint --case-root`（必填、拒 symlink）；`plan`：读 resolver（当前 base 或代）＋coverage CURRENT → 候选清单（coverage 候选 ∪ `--residual-owners` β 兜底）→ `plan_digest` → 预估 slot/请求/credits（信息，不是闸）；`repair`：α 逐候选 slot：参考源 getBlock(full, finalized, maxSupportedTransactionVersion 0, rewards false) ＋ SQD 同 slot 全交易普查（`transactions:[{}]`＋fields block.number/hash/parentSlot、transaction.transactionIndex/signatures/err，`includeAllBlocks:true`）→ 差集/分类/产边/映射/证据表（4.2.5）→ 前置一致性（coverage 阶段与 repair 阶段 SQD 签名集哈希一致、拟修复签名仍不在 SQD、blockhash/查询体/端点一致；任一变化 ⇒ 本次生产中止，已发布旧代不受影响）→ 发布协议九步＋E10 幂等分支＋崩溃恢复三段（4.2.3）；β（`--beta`，≤3 轮；残差 owner 数不降反升即停；禁 BFS）；`--blocks-cache <dir>`（routeA 格式或本工程 evidence 格式）⇒ `mode=exploration`、`reference.source=local-evidence-cache`、**不发指针**；`--resume`（plan_digest 绑定、ledger 幂等）；配额停工（402/429/配额错误体 ⇒ 停派发、在途落账、`pending-<plan_digest>/STOPPED.json{reason,cursor,plan_digest}`、退出码 3）；`verify <gid> [--live-canary N]`（深验退出）；key 只读 `~/.config/helius/api-key`（沙箱无→mock），全链路 redact；`rpc_ledger.jsonl` 首行 header（E6/E17）＋逐行 4.2.10，step ③ 前 fsync 定稿。
3. `scripts/lib/solana_exact_validate.py` **repair 段**：`validate_repair_bundle_deep(bundle_path, *, case_root, current_base)`——重算 merged==f(base,layer,map) 逐行等价、映射双射、修复签名 ∉ SQD 证据集、err 排除、blockhash 一致、evidence_manifest 哈希、gid 重算、plan_digest 全等（resolution/layer header/map header/merged meta/bundle/rpc_ledger header）、行数恒等式、resolution 候选归宿与有效 verdict、formal 拒 exploration、`bundle.base.edge_sha256 == 当前 base`、E25 反例（merged meta 含 bundle 哈希/gid 拒）；可选 `--live-canary N`。**不 import replay_edges/sqd_repair_core**（独立重算）。
4. `scripts/solana/sqd_cache_identity.py`（现 73 行；`validate_cache_meta` :20 保留为 legacy/探索）：新增 `COLLECTORS` 闭集、`validate_cache_meta_v2(meta, mint, *, case_root, meta_path)`（正式路径集合规则：meta_path ∈ {base 对, `gen-<CURRENT.gid>` repaired 对}；CURRENT 有效时 base meta 拒；repaired ⇒ CURRENT→bundle 核对 `merged.meta_sha256`、`plan_digest` 相等、`base_edge_sha256`==当前 base 并浅验 bundle；返回 `(frm, upper, kind, gid, binding)`）、`validate_repair_bundle(bundle_path, *, deep)`（浅验清单 4.4.2；深验委托 §3；无静默回退）、`resolve_formal_cache(mint, case_root) -> (edge_path, meta_path, kind, gid, binding)`（CURRENT kernel 收据校验→代目录→浅验；无指针 ⇒ base 对 gid=null；任一无效＝硬错；`pending-*`/孤儿 gen 忽略）。
5. `scripts/solana/spl_edge_core.py`：**只加**路径辅助（如 `sqd_repair_paths(case_root, mint)` 返回 `data/sqd_repair/<h>/` 与 CURRENT/.lock 路径）；7 元组/语义常量/`edge_sort_key`/`soltx_cache_paths`/`validate_edge_row` 一字不改。
6. `scripts/hooks/guard_file_ops.py` RAW_PATTERNS 增 `/data/sqd_repair/` 下规范件（bundle.json/repair_layer.jsonl/slot_index_map.jsonl/coverage_resolution.json/rpc_ledger.jsonl/evidence/*/evidence_manifest.json/soltx-*.repaired.*/CURRENT.json/.lock/pending-*/STOPPED.json）。
7. 测试：`scripts/tests/test_sqd_gap_repair.py` 各项由红变绿（(2) 机制半边、(4)(5)(6)(7)(8)(10)(15)(16)(18)(25)(26)(27)(29a)(29b)(29c)）＋单测（规范化/gid/plan_digest 向量、签名差集、vote/nonce 判定用 staging 原始块抽样、产边与真值（staging 83 边中落在三块样本内的边）逐值一致、映射双射、合并确定性＋`edge_sort_key`、发布九步/CAS/幂等/三次目录 fsync（monkeypatch）/崩溃恢复三段注入/`--resume`、STOPPED、redact、exploration/formal 分立、routeA 缓存解析、前置一致性中止）；`scripts/tests/test_reconcile_v4_receipt.py` 中 (17) 由红变绿（validate_cache_meta_v2 拒正式路径外的复制 meta）；`scripts/tests/fixtures/sqd_repair/`（≤300KB）。
8. `scripts/tests/invariant_manifest.json` 核对到实现（不得为绿删条目）。
9. `maintenance/repair-20260823-sqd-gap/batch3_done.md` ＋ `batch3_green_evidence.txt`（红→绿对照、`run_all.py` 全量并逐红项解释：预期仍红＝invariant_scan 剩余缺口（wave v5/flow v3/reconcile v4/validator reconcile 段）＋batch4 guards:198（replay 半边）；其余全绿）。
**不动**：`fetch_sqd_transfers_v2.py`、`replay_edges.py`（批 5）、`wave_scan/flow_anomaly_scan/entity_source_trace/curve_cost/camp_series_provenance/audit_closed_accounts`（批 4）、`producer_history.py`、`run_all.py`、版本文件、references 文档、PLAN/errata/契约草案、上述以外测试。

## 验收口径（Fable）
离线单测绿、红→绿对照、run_all 仅预期红；**Fable 本机**：`--blocks-cache` 用 ARC 全部 routeA 块缓存（6,759 文件）跑 exploration 代 → 修复边与 `ALL_REPAIRS_DONE.json` 三份真值文件**逐边一致（83/83）**、merged 行数恒等式、深验 PASS；然后（批 5 合并后）live 代。done 报告附本机回归建议命令。
