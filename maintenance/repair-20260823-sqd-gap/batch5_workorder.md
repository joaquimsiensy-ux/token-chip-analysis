# 批 5 工单（codex 施工）：`solana-reconcile/v4` receipt ＋ coverage 强制输入与组合规则 ＋ 受控对账第五项 `exact_reconcile` ＋ wrapper 一律 `reconciliation-report/v3`（EVM `--reseal`）＋ handoff/audit_release/camp 同步 ＋ E20 replay 纵切片半边（分支 fix/sqd-gap-v6520，基线 8df0695）

- 权威：`PLAN_errata_batch0.md` E8/E11/E12/E14/E18/E20/E25–E27 ＞ 契约草案 `solana-reconcile_v4.json`、`reconciliation-report_v3.json` ＞ PLAN §4.1 A2.1 组合规则／§4.2.8／§4.4.3／§4.4.4；scan-schemas §14 对应节核对。
- 目标对齐：把"精确对账全平"接进正式放行闸的机器层——receipt v4 envelope 直绑边源/coverage/代；受控对账 Solana 五项；handoff verify 深验＋在场 Solana 派生产物 binding 与 exact receipt 全等；旧 v2 wrapper 全 fail-closed（EVM 走 `--reseal` 迁移）。离线、不 commit、完成即停；锚文本不符即停工报告。
- 开工门禁：`git rev-parse --short HEAD` == 8df0695。

## 实况锚点（8df0695，已 grep）
- `scripts/solana/replay_edges.py`：`cmd_reconcile(edges, dec, *, mint, cache_meta_path)` :310；base meta 回写 :330-331（**删除**）；receipt v3 构造 :374-389（`_atomic_json("data/reconcile_receipt.json", …)`）；`_read_frozen_formal_edges` :222；evolution 绑定 :556-560；批 4 已有 `load_edges(case_root=)` 与 WARN 兼容路径（本批**强制**：reconcile 子命令 `--case-root` 必填、经 resolver）。
- `scripts/report/shared_release_receipt.py`：`RECON_PRODUCERS` :53；family 检查 :120；`validate_reconciliation_check(root, key, item, target, family)` :1164；`validate_reconciliation_report(...)` :1358（现要求 schema `reconciliation-report/v2` :1363）；producer 校验 :1385。
- `scripts/report/reconciliation_report.py`：`CHECK_KEYS` :19；wrapper schema v2 :119；spec family 外部声明 :144-146（E12：改由 target 推导）；checks 键集 :161-165；producer 白名单 :170；dynamic_solana 顺序 :189-221；`.lower()` 比较 :248-254（E12：分家族 canonical）；`main` :287。
- `scripts/report/handoff_manifest.py`：import :45；`CONTRACT_FILES` :66；`AUTO_GATES`（键 `reconciliation_four_checks`）:97-99；generate 读 gate :271-285；`validate_reconciliation_report` 调用 :377；`verify_case` :446；:528-532。
- `scripts/report/audit_release_gate.py`：`check_reconciliation` :467（改为复用 shared_release 深验）；:250-307、:549。
- `scripts/lib/camp_series_provenance.py`：`RECONCILE_SCHEMA="solana-reconcile/v3"` :407-408（v4 现役、v3 归 LEGACY）；schema 检查 :517-524；`inputs.soltx_meta` 消费 :586-588（扩 edge/coverage/bundle/binding 校验＋深验）。
- validator：`scripts/lib/solana_exact_validate.py`（批 3/3b 已有 coverage/repair 段；本批加 **reconcile 段**：`validate_reconcile_receipt_deep`＋`validate_verdict_gate_triad`——`test_reconcile_v4_receipt.py` RED (32) 明示该函数名）。
- 先红项（`batch1b_red_evidence.txt`＋`test_reconcile_v4_receipt.py`/`test_recon_fifth_check.py` 现红）：(1) gate_pass=false 仍 READY；(11) repair_bundle:null 过 envelope；(12) upper≠snapshot 仍 PASS；(13) base meta 被回写；(14) audit_release 只看 status；(19)(24) 在场产物 binding 不等仍 READY；(31) coverage 指针更新后旧 receipt 仍被接受；(32) verdict/exit_code/gate_pass 三元；(33) v4 raw 字段字符串被接受；E20 replay 半边（`invariant_scan` 6 缺口＋`test_batch4_invariant_guards.py:198`）。

## 白名单
1. `scripts/solana/replay_edges.py`：`cmd_reconcile` → v4（PLAN 4.2.8 全字段：envelope（`receipt_kernel.build_envelope/finalize_envelope`）、`target{chain,token,as_of_block}`、`inputs{soltx_edges,soltx_meta,holders_owners,holders_snapshot_meta,coverage_map,coverage_slot_counts,[coverage_resolution,repair_bundle,repair_pointer]}`（base 模式省略后三键不置 null——先红 (11)）、`edge_source_binding`、`coverage_effective_verdict`、E14 三 raw 字段 JSON int、E11 verdict/exit_code 与 gate_pass 同值）；coverage 强制输入（缺失/并集不覆盖/producer 未登记/SQD 指纹不一致/UNSCANNED ⇒ FAIL）；组合规则（PLAN 4.1：base 只允许 NO_KNOWN…；repaired ⇔ 代 resolution 重算 DEFECTS_CONFIRMED ∧ 逐修复交易/重映射 slot confirmed 支撑 ∧ 当前 coverage 候选 ⊆ 代 census；INCONCLUSIVE 一律 FAIL）；`--as-of-slot == snapshot slot == finalized_upper_slot`（`==` 先红 (12)）；**删 :330-331 回写**（先红 (13)）；`--receipt <path>`（默认仍 `data/reconcile_receipt.json`）；`--case-root` 必填经 resolver（批 4 WARN 兼容路径删除）；evolution sidecar 绑定不变。
2. `scripts/lib/solana_exact_validate.py` **reconcile 段**：`validate_reconcile_receipt_deep(receipt_path, *, case_root)`（重放边文件重算余额/负余额/mismatch/digest/count、coverage 重算有效 verdict、组合规则、binding 全等、`inputs` 哈希、**coverage 指针当前性**（receipt.inputs.coverage_map 必须 == CURRENT 指的 probe——先红 (31)）、repaired 深验委托 repair 段）；`validate_verdict_gate_triad`（先红 (32)）；E14 字符串 raw 拒收（先红 (33)）。
3. `scripts/report/reconciliation_report.py`：wrapper v3（`reconciliation-report/v3`；E12 单层 checks、family 由 target 推导不接受外部声明、键集按家族 `RECON_CHECK_KEYS`；E18 `checks.exact_reconcile` 仅 solana 必填、EVM 出现即拒）；dynamic_solana 顺序 `(supply, balance, supply_truth, time, exact_reconcile)` ＋占位符要求；:248-254 `.lower()` 改 `canonical_target()` 分家族；**`--reseal <旧 wrapper>`**（仅 EVM：重新深验四 receipt 实物、从实物重建 target/checks、不信旧 wrapper 状态、当前 runner 原子重封 v3；任一验不过拒）。
4. `scripts/report/shared_release_receipt.py`：`RECON_PRODUCERS["solana"]["exact_reconcile"]={"scripts/solana/replay_edges.py"}`；`RECON_CHECK_KEYS` 按家族；wrapper 校验一律 v3（v2 fail-closed 提示 EVM `--reseal`／Solana 重跑）；`validate_reconciliation_check` 加 `exact_reconcile` 分支（schema v4、mode formal、`gate_pass is True`、两计数 0、组合合法、inputs 在案根哈希可核、`inputs.holders_owners` 与 supply 观测 bundle 同文件、调用 validator 深验）。
5. `scripts/report/handoff_manifest.py`：`AUTO_GATES` 键 `reconciliation_four_checks`→**`reconciliation_checks`**（旧键读入别名）；exact receipt 引用文件须同时进 data_map 与 manifest artifacts（verify 检查，`CONTRACT_FILES` 不加动态名）；verify 对 Solana 调 `validate_reconciliation_report()` 深验＋**在场/被引用 Solana 派生产物（wave/flow/entity/curve/audit_closed/camp sidecar）`edge_source_binding` 与 exact receipt 全等**（先红 (19)(24)；(1) gate 闭合）。
6. `scripts/report/audit_release_gate.py` :467：改复用 `shared_release_receipt.validate_reconciliation_report()` 深验＋在场产物 binding 全等（先红 (14)）。
7. `scripts/lib/camp_series_provenance.py`：`RECONCILE_SCHEMA="solana-reconcile/v4"`、v3 归 LEGACY fail-closed（提示重跑）；:517-588 校验扩 `cache_kind/gid/coverage_effective_verdict/inputs.soltx_edges/coverage/bundle`＋深验＋sidecar binding 与 receipt 全等；edge_file_size 来源改读 receipt.inputs.soltx_edges（PLAN 4.2.8）。
8. E20 replay 半边：`test_batch3_solana_vertical_slice.py` 让 `replay_edges.py reconcile`（v4）在离线 fixture 案根真实执行产 exact receipt（含 coverage fixture）→ `invariant_scan` 6 缺口＋`test_batch4_invariant_guards.py:198` 自然转绿；`invariant_manifest.json` 对齐（replay v4 producer、validator reconcile 段消费点、failure artifact）。
9. 测试：`test_reconcile_v4_receipt.py` 全部转绿；`test_recon_fifth_check.py` 全部转绿；`test_reconciliation_runner.py`/`test_batch2_ready_reconciliation.py`/`test_handoff_manifest.py`/`test_audit_release_gate.py`/`test_evm_observation_release.py`/`test_r9_batch3_dynamic_runner.py`/`test_formal_chain_support.py`/`test_repair_batch_c.py`/`test_repair_batch_d.py` 等受影响面按 v3 wrapper/v4 receipt 升级（EVM fixture 走 `--reseal` 路径测试）；fixture ≤300KB。
10. `maintenance/repair-20260823-sqd-gap/batch5_done.md`＋`batch5_green_evidence.txt`（红→绿逐项；run_all 预期红应清零＝仅剩沙箱 EPERM；发现项；Fable 本机复验命令：ARC base 案 reconcile v4 全量重放（26.6M 行）＋深验计时（PLAN 4.5.2 性能验收：单次峰值内存与耗时记录；>5 分钟报告即可另立裁决，不自行加缓存））。
**不动**：`fetch_sqd_transfers_v2.py`、`sqd_gap_repair.py`/`sqd_repair_core.py`/`sqd_coverage_probe.py`（除非测试 import）、`producer_history.py`（收口 commit 登记）、`run_all.py`（SUITE 124→128 收口才改）、references 文档、PLAN/errata、契约草案（本批不修；差异记录留批 6）、版本文件。

## 验收口径（Fable）
离线：`test_reconcile_v4_receipt.py`/`test_recon_fifth_check.py` 全绿、run_all 仅剩沙箱 EPERM 类；本机：ARC base 案跑 `replay_edges.py reconcile --case-root`（预期：coverage 缺失 ⇒ FAIL 干净报错——ARC coverage 尚未发布时；发布后重跑真值）；深验性能计时记录。
