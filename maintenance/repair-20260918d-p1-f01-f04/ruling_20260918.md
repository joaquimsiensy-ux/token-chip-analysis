# 用户裁决 2026-09-18（第四轮，对 codex 9.0.0 六视角 review 868d3f6 的 P1/P2）

出处：`~/Documents/5.6筹码分析/token-chip-analysis-main-v9.0.0-868d3f6-sixlens-full-review-2026-09-18.md`（PASS 需修复轮，P0 0 / P1 4 / P2 6 / P3 0）。

| review 编号 | 内容 | 引入归因（review §7，Fable 已对照源码核实） | 裁决 |
|---|---|---|---|
| F01 · P1 | 主价格文件含 NaN 时 `price_check.py` 判 PASS 并写含 NaN 的收据；closeout 只重算 status 集合不核价格数值 | 半修复 8e566ef2→47e9efb0（9.0.0 F05 加了绑定与汇总重算，未核点数值） | **修**（本工程段 F01） |
| F04 · P1 | camps spec 显式配置「散户」时两 EVM 引擎同日追加两个元素，rc=0 产坏形状序列，靠下游长度检查兜底 | 历史漏审 0fe2d601 / ff477632 | **修**（本工程段 F04） |
| F02 · P1 | 峰值 override / metrics / 流通量可由调用者自证 | 半修复 1b317b37→332a582b | 不修（用户 0918 只点名 F01/F04；CHANGELOG 旧裁决"自己人威胁模型"沿用） |
| F03 · P1 | 历史图六项布尔声明可替代实际对账 | 历史漏审 721070f7 | 不修（同上） |
| F05–F10 · P2 | 同源双源 / late_additions 哈希 / FOT 探针余额 / CEX 通道命题 / 本机夹具 / 固定 worktree | 见 review §7 | 不修（同上） |

用户原话（2026-09-18）："我决定就修 F01、F04，修复要遵循的原则：避免skill上下文增加、能删除的不新增，能修改的不新增，你只负责验收和执行多 Agent 调度，保持上下文干净隔离：不要读取、展开或透传其他子 Agent的原始会话痕迹、内部执行栈帧、中间件Scratchpad日志，所有代码相关的改动都让codex去做，不要使用攻击性验收，用codex正常盲审即可，如果codex连续三次盲审失败就用opus替代。每次施工都要写工单——给codex复核——没问题再派codex去实施——然后再用codex盲审——最后都修完后用codex review，确保问题真正解决了再收工"。

## 终点判据

修的终点是问题消失，不是盲审 PASS。review 附录 A 两个反例在 HEAD 上必须变为拒/失败，由盲审方与收官 review 独立复现：

- `price_nan`（F01）：三天 `close=NaN` 的 CSV 作主价格文件、第二源固定合法响应 → HEAD 下 `price_check.py` 必须 `[fatal]` 退出 1、**不写收据**；另：人为构造 points 含 `main_price=NaN`（status 仍 PASS）的收据绑进其余合法的 −2 工单 → `stage2_closeout.price_receipt_errors` 必须拒（错误字段名含 `points[i].main_price`）；手改 `second_price` 使偏差 >15% 但 status 仍 PASS → 必须拒（字段名含 `points[i].status`）。
- `retail`（F04）：mint 100→A、A→B 40，`camps={大庄:[A],散户:[B]}` → HEAD 下 `replay_pass2.py`（经 pass1）与 `replay_duck.py` 均 exit 2、stderr 含 `[camp-spec]`、**不产 camp_series.json**；Solana `validate_camp_spec({"散户":[…]}, chain_family="solana")` 仍接受（LAYOFF 案 `entity_camps.json` 有 27 处显式「散户」为存量，build_evolution 默认桶+残差标量相加、无重复 append）。

## 施工顺序与流程

F04 → F01（改动小→大）。每段：Fable 写工单 → codex 只读复核（两份复核并行）→ 亲核采纳 → codex 施工（串行，`task --write --fresh`）→ Fable 本机验收 → codex 常规盲审（三连 FAIL 换 opus）→ 段收官。两段全收官后：codex 对整体 diff 做一次 review（终点判据两反例独立复现）→ run_all + reseal 补验 + 九项守卫 → 版本登记 → push。

## 版本档位（待用户追认）

候选 **9.0.1**：F01 为生产者对坏输入 fail-closed（原本就不该进报告的输入）＋消费者补核；F04 为 spec 校验收紧（拒一种从未在正式案出现过的配置）。两者都不改任何收据/序列的 schema 或键，旧合法产物全部照常通过；按 CHANGELOG 头部规则属 patch。若用户按"契约收紧＝主版本"先例判 10.0.0，收官段改登记即可。
