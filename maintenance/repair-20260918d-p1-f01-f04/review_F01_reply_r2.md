# 工单F01复核：退回

指定修复的运行行为均符合 v2；剩余 **1 处兼容性硬约束未同步**，需要修正文案，无需改变当前修法。报告全文已打印到 stdout。

**F01-R2-01：§1.5 的“全有限”兼容范围与新增正数要求冲突。**

工单位置：[workorder_F01_price_nonfinite.md §1.5，第 26 行](/Users/uravvv/.claude/skills/token-chip-analysis/maintenance/repair-20260918d-p1-f01-f04/workorder_F01_price_nonfinite.md:26)，关联 §1.7、§2.1 和用例 6b。

事实：§1.5 仍承诺“合法输入（全有限）”的 stdout、退出码和收据键集合不变。0 和负数都是有限数，但内存执行结果如下：

| 主价／第二源 | 基线 | v2 |
|---|---|---|
| `[0.0, 1.0, 1.0]`／固定 `1.0` | SKIP/PASS/PASS，汇总 PASS，返回 0，生成收据 | 返回 1，未打开收据输出 |
| `[-1.0, 1.0, 1.0]`／固定 `1.0` | 同上 | 同上 |

§1.7 已正确说明消费者的旧收据兼容范围，但生产者的这条硬约束仍需同步。

修订建议：将 §1.5 的兼容前提明确为“主价格文件各点均为有限正数，第二源为 None 或有限数的合法输入”，并明确“非正主价改为 fatal 1 属本轮预期变化”。保留现有实现方案。

**其余实际核验结果：**

1. **R1-01 已消化。** 归一化明确插在基线 `:175` 之前，原判点行不改。第二源 `nan/inf/-inf` 带 `--out` 均生成完整、严格可解析的 JSON：三点 `second_price=null`、SKIP，汇总 ALL_SKIP，返回 3；没有 ValueError，`:185` 打印正常。6c 的 NaN 基线返回 0、改后返回 3，Q1 一致。

2. **R1-02 的实现、§1.7 与 Q4 逻辑成立。** `_load_series` 的有限正数条件与消费者对生产者输出数值的 `ok1` 一致。6b 的 NaN、0 输入分别验证：基线均返回 0 并生成收据；改后均返回 1、不打开收据输出。6d、6e 基线均 `errors=[]`；改后各产生一条 `points[0].main_price`“有限正数”错误。6f 改后精确产生 `points[1].status`、预期 FAIL 的错误。

3. **R1-03 已消化。** 测试 `:15` 的路径设置可解析 `price_check`。`1.0/1.052` 得到 5.07% WARN，真实收据放行并记 NOTE。全部点 status 与 verdict 改为 PASS 后，仅产生三条逐点 status 错误，首条为 `points[0].status`、文案含 WARN，没有汇总或哈希错误。阈值断言在基线抛 AttributeError，在 v2 通过；消费者常量单独改为 6/16 时抛 AssertionError。

4. **R1-04/R1-05 的指定修订成立。** 汇总层确实可额外产生 0、1、2 条诊断。CSV 日期截断位于 `_load_series:70-72`，§0.4 已订正。真实解析加有限正数检查能覆盖小写非有限值、`1e309` 溢出及 0/负价。Q4 已明确执行方、目录深度和解析规则；其迁移结论按所述核验范围理解。本轮未读取案卷，未独立确认存量数字。

5. **段间状态无冲突。** 内存执行原段 6 → 6b–6g → 段 7 的价格收据相关语句，断言均通过。6c 写入 ALL_SKIP；6d 重建后供 6e 复用；6f、6g 各自重建。顶层引用保留到段 7 主动移除，段 7 先重建收据再验证内联引用。`update()` 忽略回调返回值；6g 元组表达式合法，两个修改均执行。

6. **r1 已通过内容保留。** 对照 v1→v2 差异，11 个锚的基线定位、19 条 mutation 要求、终点拒绝位置、白名单及 invariant 登记边界未发生实质变化，未重跑这些完整验收。§0.5 明确行号指施工前 `868d3f61`，因此仍使用原 `:175/:199` 定位一致；插入后预计分别对应第 180、204 行。

复核 HEAD 为 `edd43d0078874bd71315cef53d8bad31276d611e`；`scripts/` 对 `868d3f61` 差异为空。全程离线、未修改文件、未 commit、未读取禁读路径。动态结果来自内存执行，文件 I/O、第二源及无关图表校验使用桩；未运行完整回归或段 7 最后的完整 `check_result`。
