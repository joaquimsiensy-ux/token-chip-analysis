# 工单F04复核：通过

v2 已消化 r1 四条意见；指定复核范围内未发现新增问题。

实际核过的项：

1. **R1-01**：§0.7 已明确“四项、三 RED 一 GREEN”，逐表达式独立取证。§2.3 RED 第 3 项已改为 `len(散户)==2*len(dates)==8`、`expect_rc=0`；第 4 项同步明确长度 8。与四天夹具、工厂退出码断言及两引擎追加逻辑一致，取证步骤可执行。改后验收用例保留 `expect_rc=2` 正确。

2. **R1-02**：Q5 已正确区分 `replay_edges.py:634–639` 的 producer 分列和 `camp_series_provenance.py:66/:318` 的 consumer 并桶；`:865/:875` 的既有末点对账冲突已登记为本轮保留不修。`build_evolution` 的标量累加说明仍在，与源码一致。

3. **R1-03**：§2.3 说明①已改为 `:577`，保证限定为“全新输出目录不生成 `camp_series.json`”。已核对调用顺序，校验前 pass1 确已写出余额、统计等文件。

4. **R1-04**：§2.1 说明③及 Q6 的检索分类已订正，包含 `standard_charts.py:81` 颜色映射，与实际检索一致。“正式案零实例”已归属此前 review 记录；LAYOFF 27 处已在工单及 Q5 标为调度方本机核验。本轮未独立核验案外存量。

5. **Q7**：已核对引用文档及台账差异。自然语言命中的分类准确；CHANGELOG 登记明确留在收官段，与本段文档零改动一致。

6. **r1 已通过部分**：已对比 v1（`ff4be9e`）与当前 v2。修法、六处施工锚、拟议代码块、回归清单、终点测试断言、白名单及不改清单均保持原文；未重演此前验收。

本轮为文档差异与源码静态复核，未运行引擎或回归测试。全程离线，未修改文件、未 commit；未读取 `~/.codex/`、memories 或其他指定禁区。报告全文已打印到 stdout，未落盘。
