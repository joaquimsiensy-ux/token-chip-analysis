# 盲审 F01（只读，常规盲审，非攻击式）

## 纪律
1. 禁读 `~/.codex/`（插件启动搜索若已读 memories 如实披露一次，之后不再读）；禁读本仓库 `archive/`、`blind-reviews/`、`.staging_*`、`references/attic.md`、`maintenance/repair-20260918d-p1-f01-f04/` 以外的全部历史 maintenance 目录；禁读 `/Users/uravvv/Desktop`、`/Users/uravvv/Documents`；**禁读 `F01_done.md` 与 `F01_red_evidence.txt`**（盲审不看施工方自述，独立判断）。豁免：d) 项测试自身以子进程访问历史 maintenance 目录属测试依赖，允许原样运行，审方本人不主动打开那些历史文件。
2. 只读、离线、不 commit、不改任何文件。报告全文打印到 stdout，首行固定为 `# 盲审 F01：PASS` 或 `# 盲审 F01：FAIL`。FAIL 时逐条给出：编号（F01-B1-NN）、文件:行、事实、后果、建议。PASS 时列出实际核过的项与实跑的命令/结果尾行。
3. 工作目录＝`/Users/uravvv/.claude/skills/token-chip-analysis`，以当前 HEAD 为审对象；施工 diff＝`git diff 7a0b083 HEAD -- scripts`。`maintenance/` 目录整体不在审查范围。

## 任务
审 F01 施工是否**真正解决**了 review 反例并符合工单 v2（`maintenance/repair-20260918d-p1-f01-f04/workorder_F01_price_nonfinite.md`）：
a) **终点判据（必做，独立复现）**：`ruling_20260918.md` 的 `price_nan` 反例——三天 `date,close` CSV 全为 `NaN`（另各试一份 `inf`、`0`、`-1` 首日），第二源用内存替身固定合法响应（替换 `_get` 或 `second_llama`），HEAD 下 `price_check.py` 是否 `[fatal]` 退出 1、**不写收据**（拒在哪一行、哪句文案）；第二源替身返回 `nan`/`inf` 带 `--out` 时是否产出完整收据（`second_price=null`、SKIP、ALL_SKIP 退出 3）、不抛 ValueError。消费者：构造其余合法的 −2 工单（可复用 `test_stage2_closeout.build_closeout_case`），把 points 手改为 `main_price=NaN`/`0.0`（status 仍 PASS）→ `price_receipt_errors` 拒（字段名含 `points[i].main_price`）；手改 `second_price` 使偏差 66.67% 但 status PASS → 拒（字段名含 `points[i].status`、文案含 FAIL）；WARN 边界 1.0/1.052 真实收据放行、手改全部 status＋verdict 为 PASS 后拒。**基线 RED 复现**：用 `git show 7a0b083:scripts/prices/price_check.py` 与 `git show 7a0b083:scripts/report/stage2_closeout.py` 做内存对照，确认基线对三天 NaN 判 PASS 退出 0 且收据含 `NaN` 字面量、消费者 errors 为空。
b) 修法与工单一致：`price_check.py` 只有 `import math`、`:18` docstring、`:84` 后非有限/非正 fatal、`:175` 前 `p2` 规范化、`:199` `allow_nan=False` 五处；对合法（全有限正）输入 stdout/退出码/收据键集合逐字不变（不新增收据键）。`stage2_closeout.py` 只有 `import math`、`PRICE_WARN_PCT/PRICE_FAIL_PCT` 常量、docstring 两行、`:268` 后逐点重算块；`round(…, 2)` 与生产者 `:179` 同规则（边界 5.00/15.00 两侧各抽一值）；汇总规则/`price_file_sha256` 绑定/12 项 checks/返回类型/`WORKORDER BLOCK: ` 前缀不变；`workorder_reference_contracts` 的 19 条既有 mutation 仍 BLOCK 且文案含 field 名。
c) 白名单：**只核 `git diff --stat 7a0b083 HEAD -- scripts references SKILL.md commands-staging VERSION pyproject.toml CHANGELOG.md`**，须恰为 `scripts/prices/price_check.py`、`scripts/report/stage2_closeout.py`、`scripts/tests/test_stage2_closeout.py` 三文件；`SKILL.md` 8021 B、`references/**/*.md` 合计 930076 B、`commands-staging/*.md` 合计 8798 B 不变。
d) 实跑（贴尾行）。**沙箱口径**：若某测试因只读沙箱临时目录不可写而报 `No usable temporary directory found`，记为 `SANDBOX-BLOCKED`（贴该错误行）而**不计 FAIL**——由调度方本机补验；能跑的须 PASS。命令：`python3 -B scripts/tests/test_stage2_closeout.py`、`test_a4_gate.py`、`test_audit_release_gate.py`、`test_batch4_invariant_guards.py`、`test_exemption_guards.py`、`python3 -B scripts/tests/invariant_scan.py`。**不跑 `test_stage2_reseal.py`**（硬依赖预建验收 worktree，调度方本机补验）。
e) 结论规则：a)/b)/c) 全过且 d) 无真实 FAIL（SANDBOX-BLOCKED 不算）即 PASS。另核新增段 6b–6g 的 RED 是否真能在基线上失败（`git stash` 禁用；用 `git show 7a0b083:…` 读基线源码做内存对照）。
