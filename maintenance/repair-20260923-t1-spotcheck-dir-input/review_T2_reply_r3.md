# 工单T2复核r3：退回

仅一处未逐字吸收：v3 第 124 行 H16 兜底文字遗漏 r2 替换文末尾的 `。`；语义一致，但不满足本轮逐字核验要求。

基线：`bf180a5e6ec57d9dd67130a0300904a100c49c3f`，工作区文件与 HEAD 一致。

| 核验项 | 结果 | 证据（v3 行号） |
|---|---|---|
| §1.2 整行 | 通过 | L28 与 r2 替换文逐字一致 |
| §2.3 H16 括号内兜底 | 退回 | L124 仅缺末尾句号 |
| §2.1 注释锚三行补全 | 通过 | L65–67 逐字一致；L62 引导文字一致 |
| 范围外变化 | 通过 | 完整差异仅涉及上述三处、版本标题及 v3 修订说明 |

将 L124 括号内从“若实跑发现”至右括号前的文字替换为以下原文，保留其后的 `）：`：

> 若实跑发现其他夹具文件冲突，使用新建子目录作为 H16 的独立 case root：先在该目录调用 `make_case`，再调用 `_produce_time`，再另存 H11 收据并生成相对于该目录的 item 引用；随后读取该目录的 wrapper、替换 time 项并执行正负例。该分支不再重复调用 `make_case`，不得混用父目录的 `h11_item` 或引用基准，并在完成报告说明冲突文件及处理顺序。

本轮只读、离线，无文件修改或提交。初始文件名搜索越出本工程 maintenance 子目录，列出了其他目录的路径，但未读取其文件内容；后续读取均限定在本工程目录。
