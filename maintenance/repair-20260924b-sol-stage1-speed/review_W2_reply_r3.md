# 工单W2复核r3：通过

v3 已吸收 r2 的三项必改，**§2.1 退出码优先级与轻校验边界已定形，§2.4 字节预算成立，可以进入派工流程**。实际开工仍按工单要求，由调度方填写前序收官后的 `<W2_BASE>` 并重核锚。仅有两项顺序文字建议，不阻断 W2。

本轮基线为 `6b36dcdd043d0b2b51c03de9ab0bb555b25f436a`，包含 `cc6298b`，开始与结束工作树均干净。指定源码、文档、版本及资产范围内，相对 `cc6298b` 仅 `scripts/lib/net.py`、`scripts/tests/test_net_result.py` 有差异，符合 W3 已施工的说明。

全程离线，未读取任何禁读路径或 memories，未新建、修改文件或 commit。一次 here-document 命令因 shell 无法创建临时文件而未执行，随后改用无落盘的 `python3 -B -c` 完成核算。未运行测试、完整 lint 或 `run_all.py`。

**r2 六条吸收核对**

以下 W2、WR、README 均指本工程目录中的对应文件。

| r2 条目 | r3 核对结果 | v3 对应位置 |
|---|---|---|
| 1．预算、时效起点、β 范围 | 已吸收。三段原文复算合规；时效明确从首次直接 census 所属 coverage 发布时间起算；区分 β 搜索与 β 候选共用修复流程 | W2:68–72；W1:36、48；W4:26–27 |
| 2．事实快照、派工基线、完整整行锚 | 已吸收。事实快照与 `<W2_BASE>` 分离；六处完整原文均唯一命中，行号正确 | W2:6、9–34、41、46 |
| 3．部分扫描故障、退出码、轻校验 | 已吸收，契约明确，组合故障测试要求齐全 | W2:59–65 |
| 4．WR 时序、真实注册表验收、README | 核心要求已吸收。源码提交与最终收官分开；README 已明确 producer_history 仅由 WR 修改。顺序文字残留见建议 | WR:1、17–18；README:12、19 |
| 5．CHANGELOG 日期与检查器能力 | 已吸收。索引复算为 173 B；日期格式与日期真实性已区分，完整检查交调度方 | W2:35、74 |
| 6．W1 过渡 probe | 已吸收为明确的派工前核查与分支处理。是否存在需保留的正式产物仍须调度方核实，不能把最终 probe 登记视为覆盖过渡哈希 | WR:19／§0.8 |

**§2.4 字节复算**

从工单引号内提取原文，以 UTF-8 字节经标准输入交给 `wc -c`；下表均不含外层引号、行末 LF 或追加分隔空格。

| 原文 | 实测 |
|---|---:|
| W2:69 生命周期替换段 | 890 B |
| W2:70 压缩追加句 | 192 B |
| W2:71 W4 追加句 | 278 B |
| 原生命周期段 | 288 B |
| W2:66 split-run 追加句 | 641 B |
| W2:67 commands 追加句 | 120 B |

`references/` 净增：`890 − 288 + 192 + 278 + 641 + 3 = 1,716 B`，低于 1,800 B 上限，余量 84 B。

commands 净增：`120 + 1 = 121 B`，低于 260 B 上限。W2:72 的核算正确；其中 192 B、278 B 是正文长度，各自前置空格另计。

六处锚均通过实际 `grep -n -F -x`，各命中一次：`split-run.md:41`、commands `:12`、采集分册 `:202/:107/:198`、资产 README `:3`。

**§2.1 定形判断**

退出码已无原先的优先级冲突：

- 扫描级故障优先返回 **1**，即使存在 chosen，也不得使用部分结果。
- 无扫描级故障且有 chosen 返回 **0**。
- 正常扫描无 chosen 返回 **2**；只有同时取得合法结果 JSON 且 `chosen=null`，才允许按无图执行 `--full`。
- 缺失目录、空目录、单候选读取或校验失败，以及 argparse 的 exit 2，均已分别规定处理方式。

轻校验明确排除 bool，要求二进制引用与资产 JSON 同目录，并检查 refuted 子集、origin 索引、等长与逐证据计数；输出时间统一 UTC。目录约束与 `solana_exact_validate.py:797–799` 一致，证据形态衔接 W1 §2.1。二进制内容验证仍留在正式加载路径，职责边界清楚。

**1．【建议】W2:74／§2.6：更新 CHANGELOG 施工顺序文字。**

当前“按实际顺序 W1→W4→W3→W2＋WR 登记”仍是旧顺序，与 README:19 的 W3 先行不一致。该条同时要求依据实际完成报告，因此属于文字同步问题。

将行内原文：

> 改法（按实际顺序 W1→W4→W3→W2＋WR 登记）

替换为：

> 改法（按实际施工与登记顺序 W3→W1→W4→WR-a→W2→WR-b）

**2．【建议】README:14、19：明确旧顺序的历史性质及 WR 登记的阶段位置。**

README:19 已给出新顺序，但 :14 仍写 W1 基线为 `cc6298b`；:19 的通用“每单验收后进入下一单”也可进一步与 WR §0.6–0.7 对齐。WR 的专门条款已经明确，不构成 W2 阻断项。

将 README:14 整行替换为：

> 上表顺序列为原计划；实际施工顺序以下方「顺序调整」为准。施工严格串行（同一工作树），复核/盲审只读可并行。`cc6298b` 是工程原始基线；各单实际派工基线取前序施工及必要登记完成后的 commit，由调度方更新工单基线、差异检查、事实与行号。

将 README:19 整行替换为：

> 实际施工顺序：**W3（先行）→ W1 → W4 源码提交 → WR-a 登记提交及真实注册表入口验收 → W4 最终收官 → W2 源码提交 → WR-b 登记提交及最终 probe 注册核验 → 工程最终验收与 codex 收官 review**。W3 与 W1/W4 无文件交集。各单经只读复核通过后施工，由调度方验收、commit，再经只读盲审；盲审连败三次换 opus。W4 与工程最终收官分别以完成 WR-a、WR-b 的对应验收为前置，具体按 `workorder_WR.md` §0.6–0.8 执行。施工者不 commit。

| 本轮意见 | 等级 | 位置 | 阻断 W2 派工 |
|---|---|---|---|
| 同步 CHANGELOG 的实际施工及登记顺序 | 建议 | W2:74／§2.6 | 否 |
| 统一 README 旧基线说明与分阶段收官顺序 | 建议 | README:14、19 | 否 |
