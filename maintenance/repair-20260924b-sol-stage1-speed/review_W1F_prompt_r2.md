# 返修单 W1F 复核提示词 r2（只读）
纪律同 r1（禁读 `~/.codex/`、`archive/`、`blind-reviews/`、`.staging_*`、`.hypothesis/`、`references/attic.md`、其他 `maintenance/`、Desktop、Documents；只读离线不改文件；报告全文放最终答复；这是数据校验器 fail-closed 正确性审查）。工作目录 `/Users/uravvv/.claude/skills/token-chip-analysis`，基线 HEAD。
首行固定 `# 返修单W1F复核r2：通过` 或 `# 返修单W1F复核r2：退回`。
## 任务
复核 `maintenance/repair-20260924b-sol-stage1-speed/workorder_W1F.md`（**v2**，已吸收你 r1 五条 `review_W1F_reply_r1.md`）。逐条对照吸收；亲核 §2.1 两个整行锚（`:566`、`:586`）唯一性与修法在反例 A/B、首块伪报、跨案边界、canary、部分继承下的行为；§2.2 各用例是否可在 `_w1_reseal` 夹具体系构造。可派工写「通过」。
