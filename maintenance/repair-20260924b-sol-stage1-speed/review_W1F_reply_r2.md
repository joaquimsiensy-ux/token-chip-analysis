# 返修单W1F复核r2：通过

**W1F v2 可派工。r1 五项意见已全部吸收，未发现阻断项。** 本结论针对返修单的正确性与可执行性；生产修复尚未实施。

复核基线为 HEAD `4df32cad332120be2ec51d6dfa9bfc441a003883`。工单固定基线 `65132abda8253cf34562f92f726d311d9a32b4f8` 的祖先检查、指定生产路径无差异检查均退出 0；复核前后 HEAD 未变，工作区均干净。

**1．r1 五项意见逐条核对**

| r1 项目 | v2 吸收情况 | 结论 |
|---|---|---|
| 1．预填映射破坏首块检查 | §1.1、§2.1 改为全部事实检查后补值，保留原 `values` 构造与首块判据 | 已解决 |
| 2．B 构型与拒收理由不足 | 保留正确证明，追加缺目标的完整响应；要求完整冲突理由；补首块伪报负例及 helper 解码正例 | 已解决 |
| 3．事实②③行号错误 | 函数、合并段、空响应及非空覆盖赋值行号均与 HEAD 一致 | 已解决 |
| 4．锚与 tempfile 纪律 | §0.5 要求唯一完整语义行；§0.6 明确系统 tempfile 及环境不允许时停止对应测试 | 已解决 |
| 5．无依据的“小区间”断言 | §1.4 明确没有固定宽度上限，且不得忽略案外重叠冲突 | 已采纳 |

**2．§2.1 两个整行锚及修法**

对整个 [solana_exact_validate.py](/Users/uravvv/.claude/skills/token-chip-analysis/scripts/lib/solana_exact_validate.py:532) 执行 `grep -n -F -x`，两个指定整行均恰好命中一次：

```text
566:            raise ValueError("empty recheck response facts invalid")
586:    return values
```

`:566` 后的 `:567` 是目标空响应返回；`:536` 是非目标行早退，语义不同，工单已明确不改。

两处替换增删合计 **4 行**。`:583` 的 `next(iter(values))` 仍取首个实际返回块；只有事实检查通过后，返回映射才补入缺失 slot 的值 1。因此既能发现跨记录冲突，也不会把请求起点冒充实际首块。

**3．亲核行为结果**

本次直接提取 HEAD 函数，在内存中应用工单两行替换。通过纯内存文件适配器执行现有 `_w1_reseal` 和完整 `validate_coverage()`，对两个实现各运行 **17 个场景**；所有场景均未出现引用、摘要、seq 或 probe_id 错误。

| 场景 | HEAD | v2 修法 |
|---|---|---|
| A：正确值 2＋完整空响应 | 错误接受 | 冲突拒收 |
| B：正确值 2＋仅返回 501 的 `[500,501]` 响应 | 错误接受 | 冲突拒收 |
| B 删除原正确证明 | 拒收 | 拒收 |
| 单条正确证明／两条同值证明 | 接受 | 接受 |
| 首块晚于起点，却伪报 `returned_from=起点` | 拒收 | 仍按首块事实拒收 |
| 跨案完整 recheck，`counts_coverage=False` | 接受 | 接受 |
| 冲突仅发生在案外重叠 slot | 错误接受 | 冲突拒收 |
| 正常 canary 完整记录 | 接受 | 接受 |
| 仅继承来源部分 slot，保留完整 evidence | 接受 | 接受 |
| 把 evidence 计数改为本案子集计数 | 拒收 | 拒收 |
| 失败记录后有成功重试／非 verified 行不参与证明 | 接受 | 接受 |
| 继承 slot 落入 unverified／短返回／mismatch | 拒收 | 拒收 |
| 无继承路径 | 接受 | 接受 |

A/B 的 reasons 均包含完整理由：

```text
inherited refuted recheck results conflict
```

不是因响应事实错误提前拒收。首块伪报则命中：

```text
inherited refuted recheck complete response facts invalid
```

另外枚举长度 1–5、值域 `{1,2,3}`：修法正确解码 **247 个完整响应**，拒绝 **116 个短返回**；对其中 **80 个前缀空洞响应**，如实首块通过、伪报请求起点均拒收。非目标早退和 255 饱和值也通过检查。

**4．§2.2 用例可构造性**

全部可在现有夹具体系构造：

- **A/B**：从 `_w1_run` 的合法继承产物保留正确行，追加绑定正确的响应行，再调用 `_w1_reseal`。B 的字段设置与实际首块一致，能到达冲突检查。
- **两项正例**：保留单条正确证明，或追加其副本；`_w1_reseal` 自动重编号，避免重复 seq。
- **首块伪报负例**：构造请求起点早于首块的完整响应，只伪报 `returned_from`，其余绑定保持正确即可。
- **helper 解码正例**：正确填写实际首块，直接断言缺失 slot 返回 1。v2 已明确不把它当成正常生产者生成的 verified 发布正例。
- **既有回归**：[继承测试](/Users/uravvv/.claude/skills/token-chip-analysis/scripts/tests/test_sqd_coverage_probe.py:876) 已覆盖部分继承、重试、canary 失败回退及跨案边界，并登记在 main；新增函数仍须按工单登记。

`_w1_reseal` 已实际完成本次内存场景的 ledger、成功区间摘要、probe_id、CURRENT 引用及代目录重绑，未发现夹具能力缺口。

本次全程离线、只读，未创建或修改文件，未提交，未读取指定禁区。没有执行落盘 tempfile 测试或整套发布流程；上述动态结果来自内存验证，施工后的 §0.7 定向测试仍须执行。
