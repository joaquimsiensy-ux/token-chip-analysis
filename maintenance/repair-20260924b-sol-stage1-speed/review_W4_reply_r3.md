# 工单W4复核r3：通过

**v3 已具备派工条件，无必改项。** r2 的唯一阻断项已由 P3 三组内容对照补齐；P4 明确承认未取得无块头在线样本，没有继续引用误判结果。W1 的函数保护边界、继承状态回归和派工基线要求均已写清。

本次复核 HEAD＝`6b36dcdd043d0b2b51c03de9ab0bb555b25f436a`，工作树干净，`cc6298b` 祖先检查 exit 0。`scripts/` 相对 `cc6298b` 仅 `scripts/lib/net.py`、`scripts/tests/test_net_result.py` 有差异，符合 W3 已施工的说明。全程只读、离线，未新建或修改文件、未 commit、未运行测试；未读取 memories、`~/.codex/` 或其他指定禁区。

以下工程文档路径均相对于 `maintenance/repair-20260924b-sol-stage1-speed/`。

**r2 四条吸收核对**

| r2 条目 | v3 落点与证据 | 判断 |
|---|---|---|
| 1．必改：补齐三组内容对照 | `workorder_W4.md:L12`；P3 `L24–36` | 已吸收。记录五个 slot 的三组块头比较、完整所选交易内容比较、保留重复项的指令内容比较；覆盖有匹配和有头零匹配。交易数远大于匹配指令数被准确标为间接依据。 |
| 2．建议：补无目标块头在线样本 | `workorder_W4.md:L12、L44`；P4 `L38–39` | 已处理，但在线样本仍未取得。已撤销截断误判，保留 MISSING_BLOCK 离线正反例。r2 明确此项不阻断派工，现无理由升级。 |
| 3．建议：自包含深验兼容构造路线 | `workorder_W4.md:L47–48、L50、L55` | 已吸收。独立旧模板、已登记前代 sha、前代 plan 重算、旧／新／混合代、至少一个 confirmed 缺失交易及真实入口验收均已明确。 |
| 4．建议：W1 保护边界及继承状态回归 | `workorder_W4.md:L6、L16、L28、L31–32、L49` | 已吸收。禁止修改 W1 判定函数，覆盖继承状态 β 正例及 α／无头／非零 nonce 反例，保护 W1 测试和 main；差异与行数额度均相对 `<W4_BASE>`。 |

**证据足以支持派工，尚不构成施工验收。** 支持合并的最强依据已经从“数量一致”提升为 P3 记录的内容一致。反面的关键限制是：原始结果位于未入仓库的 scratchpad，本次没有独立重算；无块头服务端样本仍缺失。但这些限制已公开，且工单要求离线边界、故障、摘要兼容及登记后正式入口验收，因此不构成新的设计阻断。

源码核对也支持兼容路线：`solana_exact_validate.py:1601–1606` 不要求两组 SQD 摘要互异；旧摘要恢复路径原样保留；`sqd_cache_identity.py:143–147` 确实要求 producer 已登记。W4 §3 与 WR-a 的先登记、后真实入口验收安排一致。

**W1/W4 边界清楚。** W1 在 repair 脚本内仅改 `validate_coverage_state_consistency`；W4 改请求构造和调用流程，并明确保留该函数。当前 HEAD 尚不是 W1 收官实现，故本次通过的是工单设计；实际开工仍按现有 §0.1、§0.5 填实 W1 收官 commit 并重核行号，无需新增审批步骤。

仅有以下两项建议：

**1．建议：事实⑥结论明确限定为已记录样本**

位置：`workorder_W4.md:L12`，末尾“结论：合并请求可等价替代……”句。现有证据足够派工，但样本对照不宜读成所有响应形态均已在线验证。

可直接替换为：

> 结论：P3 已记录样本中的交易内容与匹配指令内容支持采用合并查询替代「探针＋census」；有头零匹配样本的 `instructions` 键缺失，符合 `or []` 语义。P4 未取得无目标块头在线样本；该路径及其他边界仍按 §2.5 离线验证。本段为设计依据，不替代施工验收。

**2．建议：新增源码注释同时写明 α、β 修复范围**

位置：`workorder_W4.md:L40`，§2.4。现有“α 修复”措辞偏窄，§1.1 已明确 β 候选也进入共用合并流程。

可直接替换为：

> - 2.4 模块 `:46-49` 注释旁追加 ≤4 行：「9.2.0：α/β 候选共用修复流程的状态探针并入 census 请求（`_census_body` 经 `sqd_query_body` 复用探针选择器）；β 搜索查询保持不变；新采 evidence 的 `coverage_probe_*` 与 `query_body_sha256/response_sha256` 同值；改 `_census_body` 语义须换代并登记 producer_history」。

| 汇总项 | 等级 | 位置 | 是否阻断派工 |
|---|---|---|---|
| 将等价结论限定为 P3 已记录样本，明确 P4 证据边界 | 建议 | `workorder_W4.md:L12`，事实⑥结论句 | 否 |
| 新增源码注释写明 α/β 共用修复流程 | 建议 | `workorder_W4.md:L40`，§2.4 | 否 |
| 最终结论 | **通过** | r2 四条均已落实或明确处理；W1 边界清楚 | **无必改项** |
