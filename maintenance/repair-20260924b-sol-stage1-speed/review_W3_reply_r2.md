# 工单W3复核r2：通过

v2 已可派工。r1 的 7 条意见均已正确吸收；未发现阻断施工的事实错误或设计缺口。仅有一处可选改名说明建议修正。

复核基线为 `HEAD=60c88b88a024bf463e819d846b1ba3a9e06c7ecd`，包含 `cc6298b`；工作树干净，生产源码相对 `cc6298b` 无差异。本轮只读、离线，未新建或修改文件、未 commit，未读取禁读路径或 memories。

**r1 逐条对照**

以下工单行号均指本工程 `workorder_W3.md`。

| r1 条目 | v2 吸收情况与亲核结论 |
|---|---|
| 1. ledger 来源与产物不变承诺 | 已吸收，见工单第 8、28 行。规范化块列表、空数组约定、本地错误对象、map-reuse 二进制切片及 `result_sha256` 均与源码相符；已撤去整个产物逐字节不变的承诺。 |
| 2. 真正调用方范围 | 已吸收，见第 7 行。直接调用者确为四个生产脚本；getSlot/getBlocks 属于 reference RPC；manifest 登记不再被当作调用关系。传输字节及响应头相关源码检索仅命中两份 vertical-slice 测试。 |
| 3. 可执行打桩与紧邻断言 | 已吸收，见第 29–65 行。完整替换成功用例，验证唯一参数、紧邻位置、write-out 格式及 URL；返回码 61 用例及断言可直接插入当前测试。 |
| 4. 失败分类和解析顺序 | 已吸收，见第 22 行。与 `net.py:133–166` 一致：先剥离状态尾巴，再判断退出码，最后解析正文；61 归 transport，不会误入 SQD decode/200/空正文分支。 |
| 5. backend 与 manifest | 已吸收，见第 23 行。登记确为 curl/httpx；所引 AST 守卫识别传输种类，未固定完整 argv，无须修改 manifest。 |
| 6. 性能证据归属与验收 | 已吸收，见第 9、69 行。案卷转述、SQD 历史记录与 Helius 本机记录已区分；验收不固定 gzip，也不使用 ledger.bytes 衡量传输节省。 |
| 7. W3 派工基线 | 已吸收，见第 15 行。明确使用 W4 收官提交并重核行号，不继承 W1 的生产源码零差异条件，符合 README 的串行顺序。 |

**源码锚与测试验证**

已对事实段所引关键语义行及施工锚执行 `grep -n -F -x`。三个直接修改锚均唯一命中：

- `scripts/lib/net.py:106`：curl 命令列表。
- `scripts/tests/test_net_result.py:72`：成功 JSON 用例起始行。
- `scripts/tests/test_net_result.py:36`：`not-json` case；第 46 行的完整错误键断言也唯一命中。

现有 `calls = []` 在测试第 48、56 行重复出现，但工单没有将它作为施工锚。第 62 行的三次重试断言先执行，第 72 行成功用例位置随后重新绑定 `calls`，因此不会破坏前面的断言。

仅在内存中提取并应用工单三个 Python 代码块，验证结果为：

- 当前原始 `test_net_result.py` 通过。
- 新测试搭配未修改生产源码时，按预期在缺少 `--compressed` 的断言处失败。
- 仅在内存中的命令列表加入 `--compressed` 后，新测试全部通过，包括返回码 61 分类及现有 NDJSON 用例。
- 测试 diff 为新增 18 行、删除 2 行，满足新增 ≤25 行的约束。

未运行会落盘的其他套件，也未进行真实联网压缩验收。

**事实④与 P1 的准确性**

工单第 9 行与 `fable_probes_20260924.md:4–11` 一致：同一 slot、同一端点及相同 getBlock 参数，明文 4,516,539 B，压缩传输 737,282 B，响应头为 gzip，解码后逐字节相等。比值为 `6.125931…`，写作 **6.1×** 正确。

这是调度方记录的单 slot、两种方式各一次的实测；工单没有将其写成普遍提速倍数。本轮核验的是记录引用及计算，未独立重跑网络请求。

**唯一建议：修正可选改名说明的引用数量**

「建议」替换工单第 49 行整段，锚「注意：该函数早前已有一个名为 `calls` 的变量」：

> 注意：该函数早前已有名为 `calls` 的变量用于重试计数（`assert len(calls) == 3`），上述代码在其之后重新绑定 `calls = []`，不影响前面的断言；若施工方认为易混淆，可将上述代码块中涉及 `calls` 的四行统一改用 `argv_calls`，包括断言失败信息中的引用。

依据：工单第 33、35、39、40 行涉及该变量，“两处同步”不准确。保留现有变量名的原文已经验证通过，此建议不影响派工。

| 条目 | 等级 | 依据文件:行 |
|---|---|---|
| 可选改名说明：“两处同步”改为四行全部同步 | 建议 | `workorder_W3.md:33、35、39、40、49`；`scripts/tests/test_net_result.py:56、62、72` |
