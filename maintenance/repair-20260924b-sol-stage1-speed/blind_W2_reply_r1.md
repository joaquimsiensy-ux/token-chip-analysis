# W2盲审：PASS

本次指定范围内未发现违反 W2 v3.2 工单的正确性问题。

实际 HEAD 为 `96c60c70c60de2ea0465466338ac920a60d722ea`；已确认它与施工提交 `f78b5c4` 在本次审查范围内无差异。

**独立核验结果**

1. **改动边界符合要求。** 相对 `97e888d`，指定范围仅十个白名单文件变化；校验器、修复生产者、`net.py`、`producer_history`、manifest、scan-schemas 和资产数据未动。AST 对比确认：既有生产函数、类和测试函数均未改变；`main` 仅增加入口或测试登记，新增四个生产私有 helper。`git diff --check` 通过。

2. **轻校验符合 §2.1。** 核对了默认目录定位、直接子文件扫描、八位 ASCII 数字文件名、目录和候选 resolve 去重、CLI 区间约束，以及 schema、版本、TTL、时区、元数据、区间、引用路径和 size、hex64、升序去重、W1 来源证据、旧图兼容、supersedes、canary、查询模板、子集关系与整数排除 bool。过期条件为严格的 `now > generated_at + 30d`，与加载路径一致。

3. **独立非法输入验证通过。** 使用从当前源码提取的真实函数、真实 parser 和内存文件系统，验证了 27 项拒收情形，包括用户指定的：
   - 过期图；
   - 引用文件位于资产目录外，以及嵌套子目录引用；
   - size 不符；
   - 查询模板 SHA 不符；
   - refuted 不是 candidate 子集；
   - candidate、refuted、origin、证据计数、TTL、size、区间、canary 中混入 bool。

   另验证了损坏 JSON、非法 UTF-8、单文件读取失败等。均返回 exit 2、`chosen=null`，并记录 rejected。

4. **指标、排序和输出契约通过。** 独立验证了交集长度、比例和交集内计数，以及 overlap、refuted 数、UTC 时间、绝对路径四级排序。反转目录枚举顺序后结果不变。还验证了多目录与候选别名去重、非递归扫描、TTL 临界点、旧图兼容、单行 JSON 和确切字段，以及：
   - 扫描故障有／无 chosen 均返回 1；
   - 缺失目录加合格图返回 0；
   - 空目录返回 2；
   - argparse 错误返回 2，stdout 无 JSON。

5. **真实入口与仓库资产验证通过。** 实际导入生产模块并调用 `main`，读取真实地图 JSON、检查引用文件 stat：
   - 固定时间为 `2026-09-20T12:00:00Z`：exit 0，交集 **133,916,665** slots，candidate **153,667**，refuted **0**。
   - 固定时间为 `2026-09-24T12:00:00Z`：exit 2，原因 `shared map expired`。

   网络、完整校验、二进制读取／解压／哈希和分类均设置调用即失败的拦截；未触发。

6. **测试与文档登记符合要求。** 四组新增测试已登记 `main`，源码覆盖①–⑥及三条补测，包括枚举中途失败保留部分结果；所要求的失败桩确为调用即抛错。§2.2–2.5 七段指定文本逐字匹配，README 的 W1 段保留。

   | 项目 | 独立核算 |
   |---|---:|
   | references 净增 | 1,716 B ≤ 1,800 B |
   | commands 净增 | 121 B ≤ 260 B |
   | SKILL 总长 | 8,021 B ≤ 8,192 B |
   | CHANGELOG 索引 | 173 B，不含 LF |

   四处版本均为 **9.2.0**；CHANGELOG 四条详细段齐全，未将待验收事项预填为 PASS。探针 SHA 与完成报告一致：`d4adc0c88f87bc03b3d847db7df9c9f7e588cb503734dfd977b818b581d998d8`。

**未独立验证的部分**

- 未运行会创建临时文件的原版完整测试套件；新增用例覆盖由源码核查，并以独立内存输入补充执行验证。
- 未运行触及禁区的用例、`docs_lint.py`、`changelog_lint.py`、`run_all.py`；`W2_acceptance.md` 中的结果仅作为调度方记录。
- 未以真实新建文件复测权限故障和符号链接场景；这些场景使用内存模拟。
- 未验证二进制内容、联网复用、WR-b 最终登记或仓库外 commands 同步；这些不属于本次轻筛正确性结论。

全程离线、未修改或新建文件、未 commit；未读取 `~/.codex/`、memories 或其他指定禁读路径。