# 工单R6复核：通过

v1 两条修复均可采纳；锚点、依据和字节预算成立，未发现必须扩大白名单的遗漏。**这是工单内容复核通过；当前工作区非空，施工仍须遵守 §0.1。**

1. **a）锚点：通过。** 从工单代码块直接提取原文，分别执行 `grep -n -F`、`grep -n -F -x`，均恰好命中一次：D1 为第 201 行，D2 为第 184 行。

2. **b）D1：采纳，代码事实成立。**  
   [replay_pass1.py:72](/Users/uravvv/.claude/skills/token-chip-analysis/scripts/evm/replay_pass1.py:72) 在 `standard8` 分支取 `log_index`，否则解析 `uniqueId` 最后一个冒号后的整数；第 79 行确为 `(c["tag"], tx.lower(), li)`。内存执行现行 AST 表达式也确认：同键覆盖，不同事件序号保留，交易哈希大小写归一。

   这里的 `li` 不能统称链上 `log_index`：Alchemy 尾号是类别内序号，[evm-sources:64](/Users/uravvv/.claude/skills/token-chip-analysis/references/data-pipeline-evm-sources.md:64) 已说明差异。删除错误短语后，“容忍少量重复”仍有该处段内去重规则及实现支撑，没有新增断言。

采纳的整行替换文本：

```text
- pageKey 有有效期，长任务中断后必过期：断点续拉一律读 CSV 末行区块号置 fromBlock 重开游标，容忍少量重复。（SIREN，07）
```

3. **b）D2：采纳，引用真实，收窄成立。**  
   [tiering:53](/Users/uravvv/.claude/skills/token-chip-analysis/references/playbook-entity-cluster-tiering.md:53) 的类型②措辞上限确为“链上铁证 / 高度疑似”；第 60 行确有实锤①②与高度疑似③分开报的要求。不能将第 60 行进一步解释成“类型②无条件确证”。

   [solana-scan:116](/Users/uravvv/.claude/skills/token-chip-analysis/references/data-pipeline-solana-scan.md:116) 明确限制共同出纳证据的措辞；[methods:102](/Users/uravvv/.claude/skills/token-chip-analysis/references/playbook-entity-cluster-methods.md:102) 确实称其为强证据。强证据、允许成边和确权不是同一门槛，二者可以并存。替换也没有豁免 §6 的中间节点检验、公共设施剔边和 CEX 热钱包禁作共源规则。

   D2 的依据属于现行文档规范，不能据此声称代码自动执行了这些措辞等级；工单未作此断言。

采纳的整行替换文本：

```text
**措辞锁定：** 共用出纳型聚类一律"高度疑似同一实体"，不写确权——共用出纳也可能是做市商/OTC 服务商同时服务多个独立客户；把证据链逐条列出（每条边标日期+金额）让读者自判（IO，07）。
```

4. **c）字节与行数：通过。** UTF-8 重算如下，整行字节数不含未改变的换行符：

| 项目 | 替换前 | 模拟替换后 | 差额 |
|---|---:|---:|---:|
| D1 整行 | 189 B | 162 B | −27 B |
| D2 整行 | 251 B | 260 B | +9 B |
| `SKILL.md` | 8021 B | 8021 B | 0 |
| commands 四份文档合计 | 8789 B | 8789 B | 0 |
| references 三组合计 | 929549 B | 929531 B | −18 B |

D1 文件保持 **333 行**，仅第 201 行改变；D2 保持 **259 行**，仅第 184 行改变。两处都是整行替换，没有删行。D2 局部增加 9 B，但工单规定的合计预算成立。

5. **d）回归面与白名单：通过。**  
   检索 46 份现行文档并补查其他允许范围内的 Markdown，未发现其他同款“按 tx hash 去重”或无范围限定的“一律高度疑似”。

   邻近表述也已核读：`solana-scan:192` 明确针对共用中转和时序同步；`report-template:142–143` 是观测边界提示，并把“高度疑似”上限明确写给伪装分散型，没有重复 D2 的全局封顶要求，不据此扩大修复范围。

   模拟替换后，两目标文件涉及的 **23 条 manifest required/banned 契约全部满足**。现有白名单足够，无须改代码或 manifest。

6. **e／f）最小修改与范围：通过。**  
   D1 删除错误短语；D2 限定原句适用范围，保留已有风险解释和证据展示要求。两项分别修复事件去重粒度错误、证据等级冲突，均有实际依据，不是措辞偏好。`SKILL.md` 不变，三组总字节下降。

   更短的等价写法存在：D2 将“共用出纳型聚类”改成“共用出纳聚类”，再省 **3 B**，references 合计为 **929528 B**。这是可选压缩，不构成退回理由；本次采纳的仍是上面的 v1 原文。

| 审项 | 结论 | 关键结果 |
|---|---|---|
| a 锚点 | 通过 | 各 1 次，201／184 行一致 |
| b 依据与替换真实性 | 通过 | D1 对应代码；D2 对应现行规范 |
| c 字节与整行替换 | 通过 | 合计 −18 B，行数不变 |
| d 回归与白名单 | 通过 | 未发现同款遗漏，23 条契约满足 |
| e 最小修改与上下文 | 通过 | SKILL 不变，总字节下降 |
| f 范围控制 | 通过 | 未夹带范围外修复 |

复核 HEAD 为 `11ee76377cadc3a871ec29b8e2849b31aefd708d`，指定内容路径相对 `8e54242` 的差异为空。末次状态检查发现本工程目录下有三个未跟踪文件：`blind_r7a_prompt.md`、`blind_r7b_prompt.md`、`construct_r6_prompt.md`，因此**当前不满足施工前工作区为空的条件**；本次未处理这些文件。

全程离线，未修改或新建文件、未 commit，未读取任何禁止路径内容；`attic.md` 仅以文件大小参与预算。工具链缓存和 here-document 临时文件创建尝试均被沙箱拒绝，随后改用不落盘命令。完整守卫未运行，因为部分守卫会读取禁区或创建临时文件；上述契约检查是内存模拟，不能冒充完整守卫全绿。
