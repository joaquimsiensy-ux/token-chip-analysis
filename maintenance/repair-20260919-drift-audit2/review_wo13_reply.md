# 工单R13复核：退回

D1 问题属实，但 v1 方案不通过：**第 95 行仍保留无条件 `exit 1` 的断言，且 `+28 B` 不是必要最小增量。** 建议在现有白名单内替换第 95、96 行，净减 **13 B**；第 97 行不动。

审查 HEAD：`c2c2b38c63b9f16bce25d6c9c2a69bc87328cf82`。前后工作区均为空；排除禁读路径后，获准核对的内容相对 `16f9c44` 无差异。本次执行另有禁读违规，详见末尾披露。

**a）锚点：通过。**

从工单代码块直接提取旧锚，以 `grep -n -F -x` 核验，恰好命中 [目标文档第 96 行](/Users/uravvv/.claude/skills/token-chip-analysis/references/data-pipeline-solana-capture.md:96)。内存模拟仅改变该行，文件仍为 **250 行**，没有删行。

**b）代码事实属实；第 95 行需要联动，第 97 行可以保留。**

- [脚本第 526 行](/Users/uravvv/.claude/skills/token-chip-analysis/scripts/solana/audit_closed_accounts.py:526)确实先判断 `events["missing"]`，返回 `LEAK_FOUND/2`；随后才判断 `invalid_reasons`，返回 `INVALID_SAMPLE/1`。
- [墙钟分支第 71 行](/Users/uravvv/.claude/skills/token-chip-analysis/scripts/solana/audit_closed_accounts.py:71)确实保留已有 `LEAK_FOUND/2`。即使截断是在 `_build_report` 内首次发现，也不会覆盖漏边结果，无效原因仍保留。
- **第 95 行需联动。** 支持 v1 的最强解释是：紧邻第 96 行的优先级补语可以作为第 95 行的例外。但第 95 行自身仍将“过半 undetermined”直接对应 `exit 1`，正是 R13a 已定位的同款断言。存在样本无效原因，不等于最终必然返回 `INVALID_SAMPLE/1`。
- **第 97 行不必修改。** 它说明各 `status` 对应的退出码，没有声称 `invalid_reasons` 非空必然选择 `INVALID_SAMPLE`，也没有排除 `LEAK_FOUND` 保留无效原因。

纯内存执行现行分支及 builder，**7 个场景通过**。具体反例：3 个深挖账户中，2 个 undetermined、另 1 个发现漏边，最终为 `LEAK_FOUND/2`，`invalid_reasons` 仍含“过半”。

v1 新增的优先级补语本身正确，没有引入新的代码事实错误。

**c）UTF-8 字节及整行替换：v1 算术通过。**

| 对象 | 改前 | v1 模拟改后 | 净变化 |
|---|---:|---:|---:|
| 第 96 行，不含换行符 | 368 B | 396 B | +28 B |
| 目标文件 | 38997 B | 39025 B | +28 B |
| `SKILL.md` | 8021 B | 8021 B | 0 |
| `commands-staging/*.md` | 8789 B | 8789 B | 0 |
| references 三组合计 | 929105 B | 929133 B | +28 B |

references 三组分别为 `824150 + 74493 + 30462 B`。此次预算计算对 `attic.md` 仅取文件大小。

错误在“必要的最小增量”这一结论，不在加法。

**d）回归面：第 95 行漏修；文件白名单够用。**

改用明确文件清单后，对 **54 份获准文档**做定向检索，另发现：

- [CHANGELOG.md:604](/Users/uravvv/.claude/skills/token-chip-analysis/CHANGELOG.md:604)仍写“五类样本无效……一律 exit 1”。它属于 **6.40.0 历史条目**；两份盲审提示词第 22 行明确排除历史条目，工单也禁止修改 CHANGELOG。因此记录其存在，不要求本单扩修。
- 脚本第 **26、28、444、446、507、510** 行的 docstring、注释或日志也有同款表述，属于本单排除的 `scripts/`，不据此扩大范围。

本次定向检索未发现其他现行必审文档中的同款漏点。第 95、96 行均在既有白名单文件内。

**e）删除优先及不增加上下文：退回，建议采用以下替换。**

即使只追加补语，`；漏边优先 exit 2` 也只需 **22 B**，已足以反证 `+28 B` 为最小。更合适的方案是删除第 95 行的无条件退出码，并在第 96 行直接替换错误断言。

第 **95 行替换全文**：

```text
- **undetermined 语义（诚实纪律）**：深挖账户按结果分类 events_found / all_zero_delta / fetch_failed——后两类是"没查出来"不是"没事件"（高频中转户 delta 笔可能在 --deep-sigs 窗口外），不构成"无漏"证据；过半 undetermined＝样本无效（批 D GPT-F-06 起不再只告警）。
```

第 **96 行替换全文**：

```text
- **退出码**：0=抽样零漏边；2=发现漏边（对账 gate 语义，报告 missing_detail 带 tx 级证据）；1=运行失败/样本无效。**样本无效机器判据（批 D GPT-F-06 收口，漏边优先 exit 2）**：任一 getMultipleAccounts 批失败／深挖账户全部 fetch_failed／checked=0 且 closed>0／墙钟截断／undetermined 过半。
```

两个旧整行均已核验为唯一命中，行号准确。替换保留批次说明及全部判据：

| 位置 | 原行字节 | 新行字节 | 净变化 |
|---|---:|---:|---:|
| 第 95 行 | 344 | 334 | −10 |
| 第 96 行 | 368 | 365 | −3 |
| 合计 | 712 | 699 | **−13** |

目标文件变为 **38984 B**，仍为 **250 行**，仅两行变化。

工单 §2 应补入第 95 行原文锚及替换，修正“不动 :95”的依据；§1.1 建议替换为：

```text
1.1 字节：`SKILL.md` = 8021、`commands-staging/*.md` 合计 = 8789 不变；references 三组 glob（`references/*.md references/casebook/*.md references/labels/*.md`）合计 = 929092（基线 929105；两处整行替换按 UTF-8 字面模拟净变动 -13 B；实测数写入报告，须等于该值）。
```

**f）范围与问题性质：通过。**

D1 涉及实际返回码 `1/2` 的差异，属于机器行为承诺不符。上述建议仍处理同一条 D1，只改文本，不改变程序逻辑，不扩修历史条目。字节缩减依据本次明确要求，不另立“措辞漂移”问题。

| 审项 | 结论 | 关键意见 |
|---|---|---|
| a 锚点 | 通过 | 唯一命中第 96 行 |
| b 代码事实与联动 | 退回 | 代码引用属实；95 联动，97 保留 |
| c 字节及行数 | 通过 | v1 的 +28 B、929133 B 正确；不删行 |
| d 回归面与白名单 | 退回 | 95 残留；现有文件白名单足够 |
| e 文本及上下文约束 | 退回 | 同一问题可用两行替换净减 13 B |
| f 范围及措辞偏好 | 通过 | 属真实返回码断言，不扩大修复范围 |

**执行披露：本次未完全遵守禁读纪律。** 首次递归搜索的 glob 排除规则失效，工具返回了 `archive/CHANGELOG-archive.md` 的 3 条命中；无法据匹配输出保证该次扫描未读取其他禁区。已停止该搜索，后续采用明确安全清单，误读内容未用于结论。未读取 `~/.codex/` 或 memories。

全程离线，未修改或新建工程文件，未 commit。Git 包装器及 here-document 的临时文件创建尝试被只读沙箱拒绝，随后改用直接 Git 路径及 `python3 -B -c`。报告已打印到 stdout；完成静态核对、字节模拟及 7 项分支/builder 内存验证，**未运行九项施工守卫，不声称守卫全绿**。
