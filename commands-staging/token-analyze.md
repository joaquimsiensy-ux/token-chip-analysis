---
description: 对指定代币做完整庄家链上行为分析（v5.0 三问框架 + 对抗复核 + 自包含 HTML 报告）
argument-hint: <代币名或合约地址> [链名等补充信息]
---

调用 token-chip-analysis skill，对以下标的做完整分析：

**标的：$ARGUMENTS**

执行要求：
1. 严格按 skill 的全流程路由表执行（A0–A6，细节手册＝`references/analyze-workflow.md`），铁律 7 条全程有效。
2. 回答三问框架：①有几个庄（按标签体系计数，不分级）②每个庄什么类型（单地址明牌/多地址互转·gas同源/伪装分散·指纹一致）③各阵营全历史持仓占比演变（占总供应，锁仓/销毁单列）；**开放条款**：框架外的显著结构性异常必须单列章节，TL;DR 设"本次特有发现"。
3. 若只给了代币名没给合约地址，先多源交叉核定合约地址与所在链，AskUserQuestion 和我确认后再开工；同名多币把候选列表摆出来让我选。
4. 交付到当前工作目录：自包含 HTML 报告（三张标准图＋≥20% 大庄/项目方流转路径图＋附录四件套）＋ `analysis-state.json`。**监控包默认不生成**，我确认买入后再补。
5. 结论独立性铁律：禁止复用任何历史代币的结论/数字/判定，报告正文零外部代币名，不与其他代币对比。
