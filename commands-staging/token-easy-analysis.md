---
description: 对指定代币做简化筹码筛查——引擎与完整版同强度（采集/对账/深挖/复核不减），只交付阵营演变图(含价格)+阵营划分表+判定块的单页 HTML
argument-hint: <代币名或合约地址> [链名等补充信息]
---

调用 token-chip-analysis skill 的**简化筛查模式**，对以下标的做筹码结构筛查：

**标的：$ARGUMENTS**

执行要求：
1. 严格按 skill 的 `references/easy-workflow.md` 执行（E0 画像与前置黑箱关卡 → E1 采集 → E2 对账 → E3 深度分析 → E4 对抗复核 → E5 两件套交付 → E6 按需复盘），铁律 7 条全程有效。
2. **分析引擎与完整版同强度，一分不减**：全量转账采集、对账关卡四查、深度关联全套（金库归因/gas 溯源/行为指纹/女巫深挖/关联聚类/标签判级/大户排查双闸）、对抗复核路数。砍掉的只有完整报告的排版交付。
3. 交付到工作目录 `<代币>分析/`：单页 HTML（图 1 阵营占比演变**必传价格右轴**＋按实际实体结构细分的阵营快照表＋3–5 行判定块＋局限声明）＋ `analysis-state.json` 必落盘＋charts png。build_html WARN=0 才交付。
4. 判定块只给参考意见（有无庄/控盘集中度/黑箱占比/是否值得跑完整版），**绝不自动转完整版**——我人工看完自行决定；若转，新会话跑 /token-analyze 同目录衔接（E7 继承清单）。
5. 若只给了代币名没给合约地址，先多源交叉核定并 AskUserQuestion 确认；来自初筛清单的地址可直接采信，但多链硬关卡与供给口径核定不可跳。
6. 结论独立性铁律：一币一会话，禁止复用历史代币结论，交付物零外部代币名；跨币汇总不在本会话做。
