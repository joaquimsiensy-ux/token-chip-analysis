# v6.0.0 路由推演记录（9/9，2026-07-30）

> 推演问法：新骨架下按流程走这道题，**哪一步会拦住 B 节的确证错误**。验收标准：每题至少一个明确拦截点；找不到＝骨架有洞回炉。

| # | 题 | 拦截链（按流程先后） | 拦截强度 |
|---|---|---|---|
| 1 | PYTHIA 9Z 判庄 | ①A3.2 判例过闸：casebook C-01 触发现象（Alpha 在架＋大额高频枢纽仓）命中→必做检验＝标签双源＋ed25519＋Alpha 集齐率 ②A3.3 entity_identity_gate：label_lookup 已并源 address-book（9Z 在册直接命中）；即便零命中，≥1% 大仓触发 BIG_UNLABELED flag ③build_html G8：flag 未解决**报告物理编不出** | 机械闸（三重） |
| 2 | TROLL ATA 常数判托管 | ①A3.2：casebook C-02 触发现象（协同组共同小额"指纹"）命中→普及率检验（rent-exempt 常数一查即知）＋gas 溯源＋托管需正向专属证据 ②A4 怀疑者备择弹药：C-02 是现成反例模板（过判托管方向 gate 不管，判例＋复核双拦） | 判例＋复核 |
| 3 | IQ Upbit 托管/假增发 | ①A3.2：C-03 触发现象（迁移史币巨额 mint 进单一静置大仓）→对面链同期 burn 配平＋当期名称确认 ②A3.3：58.5% 大仓必触发 BIG_UNLABELED 托管假设三查→G8 ③casebook S 册（mint 配平同族） | 机械闸＋判例 |
| 4 | EIP-7702 聚类误判 | ①A3.1 聚类硬规则（合并只认专属性证据，playbook §6）②A3.2：E-01 触发现象（getCode 返回 0xef0100 前缀）→前缀判定＋通用实现普及率豁免 | 判例 |
| 5 | QUQ 设施混入实体 | ①A3.2：E-02 先验三测（标准字节码/部署早于代币/多币服务）②A3.3 entity_identity_gate INFRA_IN_ENTITY flag→**G8 物理拦截**（本题是 G8 原生用例）③惯犯库入库前同一检验（防二次污染） | 机械闸 |
| 6 | GNT 静默改账 | **A2 第 3 查供给真值闸（v6 新代码）**：mint−burn vs 链上 totalSupply，exit 2 FAIL 硬停→余额禁用重放改实时直查。契约测试已复现 GNT 场景（重放 10 亿 vs 链上 2.035 亿→FAIL）。旧版全部自检对此静默放行——本题是新闸的存在理由 | 机械闸（fail-closed） |
| 7 | GMX 镜像相加 | ①A0 多链硬关卡：链分布盘点必做＋"镜像关系先做锁仓适配器配平"明写（指 casebook S-02）②S-02 必做检验（恒等式配平→分母取主链）；配平不成立则双口径并列禁静默选边 | 流程关卡＋判例 |
| 8 | IQ 分母 221% | ①A3.6：分母＝当期净供应＋**逐时点 assert Σ阵营=100%±容差超差不出图**＋改名册反向断言 ②casebook S-03（含"校验计数为 0 必须硬失败"通则） | 流程断言＋判例 |
| 9 | PYTHIA W1 历史波次漏检（验收期补录） | ①A3.6 历史清零层检测：跟踪集必须按**全期 max 仓位**筛（按现仓筛在流程里不再是合法动作），63% 历史波次在名单构建步必然入网 ②A3.2 行内引用 casebook S-04：触发现象（名单来源＝现仓 top N）命中→必做检验＝max 筛＋清仓窗口聚类＋回收去向追踪 ③A4 完整性批评（v5 时代唯一的一层，保留为第二层兜底） | 流程断言＋判例 |

## 结论

- 9/9 全部有明确拦截点，无回炉项（第 9 题为验收期用户挑错补录）。
- 其中 4 题（1/3/5/6）落在**机械闸**上（G8 编译门/供给真值闸 exit 2）——错误物理上到不了报告；其余 5 题靠判例过闸（A3.2 新增的强制步骤）＋复核弹药双层。
- 与 v5 的差异本质：v5 的三闸（v4.1/4.2 装的）已覆盖题 1/5 的机械层；v6 增量＝题 6 的新闸（v5 零覆盖，全自检静默放行）＋题 2/3/4/7/8/9 的判例前置（v5 靠散落在 playbook/坑表/复核 prompt 里的文字，读到与否看运气；v6 在 A3.2 设强制过闸步骤）。
- **题 9 是 v5/v6 区分度最高的一题**：v5 对"历史清零层"的唯一防线在阶段 4 复核 prompt 的完整性批评（兜底层，靠复核员想到才拦，当年实际就是漏到交叉核实才翻案）；v6 把检测前移为 A3.6 分析流程明写步骤＋S-04 判例，复核降为第二层。
