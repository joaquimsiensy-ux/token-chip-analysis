# v6.35 阶段 1 施工工单：invariant manifest + 15 项先红测试

- 任务基线：任务书声明 `main@d8bd3c5`；本阶段未执行 git 命令，不自行复核或改写基线。
- 施工边界：零生产行为变更；不改 `references/`、`VERSION`、`CHANGELOG.md`、`pyproject.toml`、`SKILL.md` 版本注释；既有文件只在 `scripts/tests/run_all.py` 增加两项测试挂载。
- 临时产物纪律：所有反例均使用 `tempfile` / 系统临时目录，未把 case 数据写入仓库。

## 工单 A：invariant manifest 与复算守卫

1. **不变量**：正式发布实施面的 receipt 生产/消费、transport、原子落盘与正式入口必须有一份可由当前代码双向复算的完整分母；代码新增未登记或清单登记已不存在都必须 exit 1。
2. **实施清单**：新增 `scripts/tests/invariant_manifest.json` 与 `scripts/tests/invariant_scan.py`；扫描范围固定为 `scripts/report`、`scripts/evm`、`scripts/solana`、`scripts/lib`、`scripts/robinhood`，排除测试/fixture；阶段 1 只检查登记完整性，不检查是否已迁入未来公共库。
3. **实施面分母**：receipt producer 34 个脚本、43 个 schema 绑定；receipt consumer 17 个脚本、51 个 schema 绑定；transport 32 个脚本/通道绑定；atomic write 30 个函数点位；正式入口 46 个；例外 0 个。consumer 扫描同时覆盖直接 schema 比较、先赋局部变量再比较、expected identity 整体字典比较。atomic 语义逐点人工回读后登记为 `exclusive_new`、`overwrite_single`、`dual_file_txn`、`restore_on_fail` 四选一，scanner 只对账点位存在性。
4. **自检反证记录**：使用 `python3 scripts/tests/invariant_scan.py --self-test` 在系统临时目录生成两份注入 manifest，并分别走完整 CLI scanner；两次均如期变红：

```text
SELFTEST delete scripts/evm/accounting_gate.py:requests -> RED (rc=1)
SELFTEST add scripts/report/does_not_exist.py:requests -> RED (rc=1)
```

5. **基线验证**：未注入时执行 `python3 scripts/tests/invariant_scan.py`：

```text
PASS invariant manifest: receipt_producers=43, receipt_consumers=51, transport_calls=32, atomic_writes=30, formal_entrypoints=46, exceptions=0
```

## 工单 B：R7-01～R7-15 独立先红测试

1. **不变量**：15 项测试全部断言“修好后的目标行为”；在 `EXPECTED_RED` 内必须真实失败才记 `EXPECTED-RED`，仍在集合却转绿属于漏摘牌并使 suite 失败，不在集合却失败也使 suite 失败。
2. **实施清单**：新增 `scripts/tests/test_r7_findings.py`，每个 finding 一个独立函数；支持 `--show R7-xx` 输出单项原始 traceback；所有外部调用均 mock 或使用本地 fixture，不发真实网络请求。
3. **反例覆盖清单**：R7-01 手拼 wrapper；R7-02 curl rc=7 空响应；R7-03 跨 mint resume；R7-04 raw override/input ref/context slot 三子断言；R7-05 reconciliation producer 缺失；R7-06 反向窗口/旧正式文件隔离；R7-07 READY 链集合；R7-08 declared PASS/exit=2；R7-09 空 labels formal；R7-10 archive copy 回滚；R7-11 verified_at 方向回退；R7-12 RPC chainId；R7-13 plan target/file_ref；R7-14 flags 去重与 trim；R7-15 字段数/三闸文档。
4. **首跑红证据全文**：执行 `python3 scripts/tests/test_r7_findings.py`，15 项均观察到真实红，expected-red 包装后整体 exit 0：

```text
EXPECTED-RED R7-01: AssertionError: fabricated wrapper + receipts accepted without controlled runner binding
EXPECTED-RED R7-02: AssertionError: transport failure returned successful payload []
target days: 0 (skip 1), order new->old
DONE 0 days, fails=0, 0s
EXPECTED-RED R7-03: AssertionError: mint-b silently reused identity-free mint-a anchor rows
[supply_truth] PASS  重放净供给=100  链上=100  差=0（0.0bps，容差 10bps）→ /var/folders/z6/mlppyd097493blf6v8_h29dw0000gp/T/tmp9p27j0g4/receipt.json
EXPECTED-RED R7-04: AssertionError: formal --replay-net-raw was accepted without --exploration; receipt has no inputs file_ref; Solana receipt omits observed_context_slot
EXPECTED-RED R7-05: AssertionError: no production script writes reconciliation-report/v2
segments: 0 x 2000 slots, conc=1
PASS 0 segs (0 gaps) in 0s -> /private/var/folders/z6/mlppyd097493blf6v8_h29dw0000gp/T/tmp0cvh74mb/reverse.jsonl
segments: 1 x 2000 slots, conc=1
FAIL 1 segs (1 gaps) in 0s -> /private/var/folders/z6/mlppyd097493blf6v8_h29dw0000gp/T/tmp0cvh74mb/old.jsonl.partial
EXPECTED-RED R7-06: AssertionError: reverse range rc=0, out=True, receipt=True; gap run did not quarantine old formal output (rc=2, stale=[])
EXPECTED-RED R7-07: AssertionError: handoff generate accepted READY for exploration-only arbitrum; handoff and audit release chain sets differ instead of sharing one source
EXPECTED-RED R7-08: AssertionError: declared gate PASS/exit=2 survived generate and verify
EXPECTED-RED R7-09: AssertionError: formal empty-label trace accepted rc=0: [wave_scan]   已装载 edges.jsonl → 累计 1 边
[source_trace] 边表就绪 1 条——按 handoff cutoff/block 物化索引表 e(t)…
[source_trace] 物化全局出入度（设施启发式）…
[source_trace] 溯源 E1（1 址，三策略模拟）…
[source_trace]   子图：祖先 0 / 终点 1（深度截断 0·预算截断 0）/ 边 1
[source_trace]   峰值锚点 1970-01-02 库存 10.000%供应 | 终点 1 类 | 未决 0.0% | 进货单 1 上家 | 敏感性 稳定
[source_trace] 1 实体溯源完成 → /var/folders/z6/mlppyd097493blf6v8_h29dw0000gp/T/tmpa1b3v1t0/out.json

[eth] 新增 0 | 合并进已有行 1 | 总 1
EXPECTED-RED R7-10: AssertionError: archive failure left published labels/manifest changed and rollback backups gone
EXPECTED-RED R7-11: AssertionError: verified_at regression was WARN-only exit 0
[verify_recon] PASS exit=0 → /var/folders/z6/mlppyd097493blf6v8_h29dw0000gp/T/tmpor8pgjk6/receipt.json
EXPECTED-RED R7-12: AssertionError: wrong-chain RPC reached balance comparison (rc=0, methods=['eth_call'])
[time_spotcheck] PASS  1/1 一致（balance 1 + tx 0；mismatch 0，rpc_err 0）→ /var/folders/z6/mlppyd097493blf6v8_h29dw0000gp/T/tmp8ssj_091/bad_receipt.json
[time_spotcheck] PASS  1/1 一致（balance 1 + tx 0；mismatch 0，rpc_err 0）→ /var/folders/z6/mlppyd097493blf6v8_h29dw0000gp/T/tmp8ssj_091/good_receipt.json
EXPECTED-RED R7-13: AssertionError: plan chain/token mismatch accepted; successful receipt lacks plan file_ref
EXPECTED-RED R7-14: AssertionError: semantic flag sets differ: {'category': '', 'tier': '', 'merge_policy': '', 'balance_policy': '', 'status': '', 'name': '', 'risk_flags': 'a|b'}, {'category': '', 'tier': '', 'merge_policy': '', 'balance_policy': '', 'status': '', 'name': '', 'risk_flags': 'a|a|b'}, {'category': '', 'tier': '', 'merge_policy': '', 'balance_policy': '', 'status': '', 'name': '', 'risk_flags': ' b|a'}
EXPECTED-RED R7-15: AssertionError: docs field count does not match DECISION_FIELDS=7
PASS R7 expected-red quarantine: 15/15 observed red
```

5. **单项重放验证**：`python3 scripts/tests/test_r7_findings.py --show R7-04` 输出原始 `AssertionError` traceback，并保持 expected-red suite exit 0；证明 `--show` 不是仅打印自报摘要。

## 挂载与阶段边界

- `scripts/tests/run_all.py` 仅新增 `invariant_scan.py`、`test_r7_findings.py` 两个 SUITE 项。
- 阶段 1 没有修复 R7-01～R7-15 的生产行为；15 项仍全部在 `EXPECTED_RED`。
- 最终执行 `PYTHONDONTWRITEBYTECODE=1 python3 scripts/tests/run_all.py`：exit 0，汇总“全部通过”；其中 invariant 显示 producer 43、consumer 51、transport 32、atomic 30、formal 46、exceptions 0，R7 显示 15/15 observed red。
- 副作用复核：施工窗口内除本工单列出的 5 个文件外无其他普通文件变更；首次验证生成/刷新过的 7 个 `.pyc` 已按单文件明确路径逐个移除；精确扫描无 `.tmp`、`.bak`、`.partial`、`.previous.*` 遗留。
- 未执行任何 git 命令。

## 【验收返工:SCOPE 缺口】

- **原因**：阶段 1 原任务书的目录分母清单遗漏三处生产区，原 scanner 只覆盖 `report/evm/solana/lib/robinhood`；本次为验收追加，不归因于施工偏离。补入 `scripts/labels`（递归，实收 18 个生产 Python）、`scripts/prices`（递归，2 个 Python）及 `scripts/*.py`（只收根部一级，实收 `proclock.py`、`run_guarded.py`）；复算确认 `scripts/tests` 命中数为 0。
- **扫描逻辑**：`SCOPE` 新增 `scripts/labels`、`scripts/prices`、`scripts/*.py`；`production_files()` 对前两者继续 `rglob`，对 `scripts/*.py` 单独使用非递归 `glob("*.py")`，并以 `set` 去重。atomic 发现补充“`NamedTemporaryFile/mkstemp` staging + `shutil.move` 提交”模式，使 `add_labels.main` 进入分母。

| 点位类别 | 返工前 | 返工后 | 增量 |
|---|---:|---:|---:|
| receipt producer schema 绑定 | 43（34 脚本） | 43（34 脚本） | 0 |
| receipt consumer schema 绑定 | 51（17 脚本） | 51（17 脚本） | 0 |
| transport 脚本/通道绑定 | 32 | 35 | +3 |
| atomic write 函数点位 | 30 | 35 | +5 |
| formal entrypoint | 46 | 53 | +7 |
| exceptions | 0 | 0 | 0 |

- **新增 transport**：`scripts/labels/sourcify_check.py:requests`、`scripts/prices/llama_price.py:requests`、`scripts/prices/price_check.py:requests`。
- **新增 atomic 与人工 semantics**：`scripts/labels/add_labels.py:main=restore_on_fail`；`scripts/labels/pull_verified_contracts.py:main=overwrite_single`；`scripts/labels/sourcify_check.py:main=overwrite_single`；`scripts/prices/llama_price.py:cmd_series=overwrite_single`；`scripts/run_guarded.py:write_status=overwrite_single`。
- **新增 formal entrypoint**：`accumulate_offenders.py`、`gen_manual_from_addressbook.py`、`label_lookup.py`、`sourcify_check.py`、`llama_price.py`、`price_check.py`、`run_guarded.py`（manifest 内均登记完整相对路径）。
- **验证**：
  - `python3 scripts/tests/invariant_scan.py` → PASS：producer 43、consumer 51、transport 35、atomic 35、formal 53、exceptions 0。
  - `python3 scripts/tests/invariant_scan.py --self-test` → 删除真实 transport 登记 RED(rc=1)；添加不存在 transport 登记 RED(rc=1)。
  - `python3 scripts/tests/test_r7_findings.py` → `15/15 observed red`，整体 exit 0。
  - `PYTHONDONTWRITEBYTECODE=1 python3 scripts/tests/run_all.py` → exit 0，汇总“全部通过”。
- **边界**：本次只修改 `scripts/tests/invariant_scan.py`、`scripts/tests/invariant_manifest.json` 与本工单；零生产脚本变更，未修改 `run_all.py`，未执行 git 操作，验证临时件只进入系统临时目录。
- **缓存清理**：最终边界扫描发现 6 个时间戳早于本次返工的上一轮 `.pyc` 残留，已按明确路径逐文件删除；未删除目录，复核无 `.tmp`、`.bak`、`.partial`、`.previous.*` 或本轮 `.pyc` 遗留。
