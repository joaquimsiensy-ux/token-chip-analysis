# 分析方法学 Playbook · 实体识别与聚类 · 标注/归因/聚类（entity-cluster 分册 1/3）

> 母路由：`analysis-playbook.md` 全局路由表直达本册。本册覆盖 **§3 地址身份标注 / §4 金库与核心实体逐笔归因 / §6 关联地址聚类规则 / §6.5 节拍指纹 / §6.6 否证检验**；§6a 判级见 `playbook-entity-cluster-tiering.md`，§6b 成本见 `playbook-entity-cluster-cost.md`。规则行尾标注来源会话。

## 本册路由

- [§3 地址身份标注](#3-地址身份标注批量标签库前置--三级兜底)：标签、身份与设施先验。
- [§4 金库与核心实体归因](#4-金库与核心实体逐笔归因)：逐笔去向、在途与行为拐点。
- [§6 关联地址聚类](#6-关联地址聚类规则)：构边、设施剔除、投毒防护与完整性扫描。
- [§6.5 买入序列节拍](#65-买入序列节拍指纹聚类三规则同时失效时的破局判据)：孤立节点的跨批次秩相关扫描。
- [§6.6 gas/vanity 否证](#66-gas-同源与-vanity-指纹的两个必做否证检验)：共同 funder 与随机碰撞的反例检验。

## 执行契约总表

| 规则族 | 触发条件 | 必做动作 | 阻断语义 | 权威脚本 |
|---|---|---|---|---|
| 身份标注 | 任一候选大户、关联地址或拟纳入成员的设施 | 先跑标签库，再按官方标签/外部证据/行为兜底三级核验；派生映射不得当标签源 | 未完成身份与设施核验，不得定性庄家或冻结成员 | `scripts/labels/label_lookup.py`；EVM `cluster.py` / `analyze_holdings.py`；Solana `replay_edges.py` / `build_evolution.py` |
| 金库归因 | 官方金库、vesting、做市、回购或大额流出 | 逐笔穿透到终点，分 CEX/囤币/在途，记录未解释流出与去向变化 | 终点未定型时只写“在途”；结构成立不得越级写意图 | 链别采集器与重放产物；本节为人工裁决权威 |
| 聚类构边 | 拟以直转、gas、时序、面额、枢纽或合约关系合并地址 | 先过分母/对照组/协议机械行为；满足 ≥2 类证据或 1 类强证据 | 未过反例检验不得成边；行为指纹上限按措辞册执行 | `scripts/evm/cluster.py`、`cluster_sensitivity.py`、`lp_positions.py` |
| 设施与投毒 | 中间节点、合约、公共 funder、零值或尘埃事件进入候选图 | 跑三段式出度、合约四测、原子中转率与尘埃过滤 | 公共设施只截断边；协议机械行为、零值/尘埃事件不得作为实体证据 | `sourcify_check.py`、链别 `getCode`/pool probe、重放边表 |
| 完整性扫描 | 准备写“无其他达标实体”或冻结庄级实体 | 补行为线、历史静置仓、反向 gas、边界外一圈与成员卫生审计 | 任一机械扫描未完成即 fail-closed，不得冻结阴性结论 | `cadence_fingerprint.py`、`cadence_rank.py` 及本节列明扫描器 |

> 下面条目是上述契约的判据展开。行尾“案源”只用于追溯，不改变成熟度；翻案经过与案例数字统一在 `casebook/` 短册运行。

## 3. 地址身份标注（批量标签库前置 + 三级兜底）

**第零步（强制前置，先于一切人工定性）：批量标签库过一遍。** 全部候选大户/关联地址先跑
`python3 ~/.claude/skills/token-chip-analysis/scripts/labels/label_lookup.py --chain <链> --file <地址清单>`（加 `--json` 得机器可读 JSONL）。
七段输出的处置纪律（v4）：[SERIAL] 惯犯庄家命中=最高优先级，立即调案源比对手法（跨案命中必写报告）；[RISK] 定性风险命中=必写进报告；[RISK-CANDIDATE] 社区候选=降权提示不作定性；[RISK-UNKNOWN] 白名单外旗标=人工核验后扩白名单或修数据，不自动定性；[EXCLUDE] 设施=不计入实体持仓/大户榜（资金路径叙事保留）；[IDENTITY] 身份=不剔除、报告标注（locker/airdrop-distributor/token-sale/charity 禁作聚类合并边；locker/分发器持仓按 bucket 单列桶）；[PRIVACY] Tornado 使用记录=陈述事实不定性。
**库无记录≠白户**：未命中仍走三级兜底；跨会话只信带来源与核验日期的标签主库和 address-book，案目录派生判定必须重核。中间判定产物不得作标签源；判例见 casebook C-01/E-14。
  缺表显式报 degraded_mode——看到该警告说明本次运行无标签防线），但对"人工逐个定性"环节，第零步照跑。Robinhood 链疑似公共 bot 合约再加跑 `fingerprint_check.py --chain robinhood <地址>`（字节码模板指纹，命中=candidate 级、行为复核后才按设施处理）。
  对未命中库的候选大户可选跑 `goplus_check.py --chain <链> --file <清单>` 做恶意地址体检（candidate 级降权提示，EVM 主力可用、SOL 覆盖未证实——纪律见 labels/README「运行时风险通道」）。分析收尾把 miss-queue 新增条目扫一眼（跨 token 反复出现者优先核验回填）（标签库 v4 接入 07-17；v4.1 GoPlus 通道同日）。

对 Top 持有人逐个定性，把 CEX 托管/路由/池子/官方金库从"疑似庄家"中剥离——五次分析中这一步多次直接改写筹码结构结论（五会话共见，07）。证据强度按级递减，用哪级就按哪级措辞。

**第一级：官方/浏览器标签（最强）**
- EVM：WebFetch 区块浏览器地址页取 Public Name Tag——曾靠它把"疑似老庄分仓""神秘大户"改判为 CEX 储备合约与热钱包，把占供应近四成的 Top1 识破为 CEX 场内托管路由。**大户定性必须查官方标签，不能只看链上行为猜**（SIREN，07）
- GMGN holders 自带 tags / start_holding_at / native_transfer.from（gas 来源），是免费标签与聚类线索的复合来源（OPN，07）
- Solana：Solscan 公开标签；托管类型可从 token account 的 owner 程序直判（多签程序=官方金库、vesting 程序=解锁托管 PDA）（IO，07）
- **标签冲突裁决：以区块浏览器官方标签为准**——聚类工具/子代理/第三方 tracer 转述的 CEX 标签与浏览器官方标签不一致时（曾有 tracer 报 MEXC、Etherscan 官方标签实为 Binance 20），一律以浏览器页面亲验为准，转述值不落库（外部 ASTEROID 考古，07）

**第二级：外部证据（次强，逐条留 URL）**
- CEX 的 Proof-of-Reserve 审计 PDF：免费、权威的交易所钱包地址来源（IO，07）
- 媒体报道与链上侦探推文的地址指认；链上行为与官方分配文档对表（排放节奏、配额结构吻合即可定性官方金库）（IO，07）
- 后台子代理批量考证：给统一 JSON schema（标签/首笔注资来源/钱包年龄/是否仍持仓），强制每条论断带来源 URL + 置信度、查不到就明说（FIL/SIREN，07）

**第三级：行为特征兜底（只够"疑似"级，措辞必须降级）**
行为特征库（IO/FIL，07）：
- 整数余额、长期只进不出 → 冷储特征
- 同时是多个热门代币的最大持仓者 → CEX 归集钱包特征
- 多对象、小额、规律发放 → 空投发放地址
- 固定周期向大量小地址发奖 → 排放/奖励金库
- 每月同日 + 金额分毫不差 + 金额整除还原出归属参数 → 机械 vesting 解锁（区分"机械解锁"与"主观卖出"的硬指纹）
- 一组地址只进不出且按固定额度顺序填充 → 同一实体拆分冷储

**兜底级的边界教训：**
- "单向把币打进某 CEX 热钱包"只能证明客户存币关系，**不能证明该地址归属该 CEX**——交易所内部冷热调拨与客户充值是两种不同链路，归属判定被对抗复核降级过（FIL，07）
- **CEX 归属正证**：先用交易所 PoR 官方地址清单与候选集取交集，再以浏览器标签互证；清单外周边仓须有 ≥99% PoR 钱包资金源与所内调度指纹，且降级注明行为学。判例见 casebook C-07。
- **判例指针**：本组方法级失败模式及案源见 casebook C-07。
- **【正式·机制解释】托管判定的反向闸** — **触发条件**：拟把只进不出、批量建仓或固定建 ATA 金额的地址归交易所托管。**必做动作**：至少取得一条硬证据：标签库/Vybe/Arkham 链根、PoR、所方 gas supplier、或带集齐率的批量批次；先排除协议物理常数（Solana 建 ATA 的 0.002246 SOL）与长持私人大户。跨所作业——gas 来自甲所、筹码来自乙所——一票排除单所托管。**阻断语义**：行为相似和上一会话派生标签均不得定托管。**权威脚本**：label_lookup.py、PoR、gas/筹码双线重放。翻案见 casebook C-01/C-02；案源：TROLL 2026-07-29。
- 免费标签覆盖不全（部分 CEX 的钱包无人标注）时，表格里明写"未能免费确证"，合计数旁标注"可能低估"（IO，07）
- **transfer_in 先判程序归属**：Solana 跑 owner/program 两跳，EVM 查合约身份；未完成前不得把归集画像判庄。判例见 casebook E-14。
- **Safe 多签解读两坑**：①创建 tx initializer 里的 SafeToL2Setup `0xBD89A1CE4DDe368FFAB0eC35506eEcE0b1fFdc54` 是 Safe 官方跨链部署设施，勿误读为 owner；②threshold/owners 必须"创建 tx setup 解码 + 当前 RPC getOwners/getThreshold"双时点核对——实测 1/2 创建后扩 2/4，单看任一时点都错（VIRTUAL，07-16）

---


## 4. 金库与核心实体逐笔归因

**建清单：** 项目方金库、vesting 托管、做市商、官方回购/护盘基金、外部披露指认的操盘实体，全部进实体清单，逐地址拉全量流水。

**逐笔归因规则：**
- 每笔大额流出对应到一个公开活动（上所划拨、做市补给、公告解锁、生态发放、回购执行）；**对不上的单列成"未解释流出"清单重点追查**（五会话共见，07）
- 解锁/分发款穿透中转层追到终点，按终点二分：进 CEX = 进入可售状态；进囤币钱包 = 被收走。终点未定型的标注"在途"（IO，07）
- 按时间轴对比同一来源的去向变化：同一 vesting 源不同月份从"进交易所"变为"进囤币鲸"（或反向）即行为拐点——这是单份报告里最强的证据形态（IO，07）
- **轮换出货通道也是行为证据**：同一来源每期固定经同一中转、但每期更换下游终点地址（本月 CEX、下月新地址 A、再下月新地址 B）= 刻意轮换路径规避追踪——处置行为+隐匿意图的组合指纹，单列报告（IO 实录核验，07-12）
- 接收方是实体地址时再追一跳并分类（转 CEX / 市场卖出 / 再质押 / 桥出），逐类计数而非笼统描述；注意"第一跳没卖"不等于"没卖"，追到几跳就说几跳（HYPE，07）

**结构指纹与意图克制：** "长期沉寂后在拉升次日突然划出大额 → 拆成面额规整的一次性新地址 → 收币后分钟级充所"这类完整链路要逐环节记录并画图；但结构成立≠意图成立——高位变现与做市/所内活动供库存在链上不可区分，定性只写"协调投放"，两种意图并列（OPN，07）。

**杠杆手法：**
- 外部情报→链上复核范式：先拿第三方分析师披露的实体地址清单，再用批量余额查询（EVM 用 Multicall3）核验现仓，直接量化实体当前持有——绕过自建聚类的冷启动（SIREN，07）
- 大额批量分发交易可直接解码提取全部接收地址（一笔近千地址的分发交易即一份现成实体名单）（SIREN，07）
- vesting 是链原生结构时，锁仓明细（initialBalance/unlock 起止）链上直读，无需外部解锁表（FIL，07）
- "官方解锁新增流量归零"≠"卖压消失"——已解锁未处置的存量仍在，必须分开表述（FIL，07）
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。

---

- **平台出纳收款人全集**：作为运营侧隐性关联仓候选发现通道；买入资金来自自身提款时不得写官方出资。判例见 casebook E-16。


## 6. 关联地址聚类规则

**前置硬步骤：聚类候选先过批量标签库**（§3 第零步；cluster.py 已默认内置 resolver 拦截——
tier=exclude 设施与 locker 禁作合并边，被拦地址写入 clusters.json 的 label_excluded_nodes 供对账）。
手搓聚类脚本或新链临时脚本时同样必须先过 `label_lookup.py`，防 CEX/桥/路由被串进实体簇（标签库 v3 接入，07-16）。

**证据边类型（多类证据构边，连通分量 + 证据打分）：**
- **大额直转（R1 机制显白）**：两个地址间累计直接转账超过阈值线（默认总量 0.005%，老币档 0.05%——币龄 >1 年或全史换手 >50 倍上调一档，见下方阈值校准条）即成合并边。为什么可信：进这一步之前，已识别的池子/路由/CEX（标签库命中）已被剔除、行为守门员再兜住漏斗形状的未知设施，剩下的地址之间大额转币没有正常商业理由——独立主体之间买卖走池子、托管走交易所，不会白给对方大额筹码；零对价的大额直转（含买手买入后整仓转给持仓钱包、A 把全仓转给 B 代卖这类形态）都是这条边的天然命中，无须单列（2026-08-02 用户定显白版）
- 共同资金来源（首笔注资同源）/ 来自金库一跳 / 时序同步（如 72h 内共同注资）/ 共用中转地址（五会话共见，07）
- 共用中转 = "共用出纳"：两个"独立"大户在各自解锁日汇入同一中转、中转链路又闭合归集到同一终点，是集群判定最经典的强证据（IO，07）
- **小额测试后 48 小时内向同一目标跟进大额（成边前必过 OTC 排除检验）** — **触发条件**：小额试路后 48h 内同目标大额跟进，窗口内不按间隔远近分级。**必做动作**：核两项：大额笔是否按当日价聚于美元整数档；接收方是否自主处置、与发送方不再往来且节奏独立。两项均不命中 OTC 侧才可把该模式作为强证据；一对二 SOP 与秒级交替时序只作跨簇待证信号。**阻断语义**：命中 OTC 侧即记录交割/转让关系，不作实体合并边；单独一对测试转账不作实锤确权。**权威脚本**：全史边表、时价折算与后续行为重放。案源：BANANAS31、PYTHIA、EGL1；2026-08-02 复核补闸。
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。
- 成边条件：≥2 类证据，或 1 类强证据（HYPE，07）；注资/gas 同源边的强度分级与"私人 funder"公共性三步体检见下方「gas/资金面溯源纪律」块（未过体检不得计为"强证据"）
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。
- **同一最小 tick 档的同期挂撤 ＝ 强协同指纹** — **触发条件**：两个主体在 V3 池选择完全相同的 1 宽 tick 档，并在 7 天内挂入/撤出；单双边不影响判定。**必做动作**：统计同期该池全部头寸 tick 分布。**阻断语义**：热门拥挤档位分母过大时降权，不得只凭撞档成边。**权威脚本**：scripts/evm/lp_positions.py 与池内全头寸分布。案源：KOGE 2026-07-25；2026-08-02 修订。

**硬规则（违反任何一条都产出过垃圾结论）：**
- **中间节点（枢纽）三段式检验（统编硬规则，2026-08-02 用户定）**：凡要用某个中间节点（gas funder、币流中转、归集/分发点）做合并证据，先查它的**全历史出账对手数**——口径＝该地址全部历史的普通交易＋内部交易里的不同出账对手（不是只看本币种；实操查到明显超过 1000 即可停止计数）。按数分三段处理：
  - **≤15＝私人级**：可作合并边——正常人自用钱包的出账对手就这个量级。
  - **16–1000＝灰区，必做收款人构成检验**：依次核服务对象重叠度、净留存 <累计流入 2%、同 tx 进出 >90%、以及自有设施三判据（单一对手流量近半 + 生命周期同步 + 跨代连续性）。原子管道再按 txfrom 用户上千＝公共设施、上游 ≤5＝私有漏斗分层。**阻断语义**：灰区未完成构成检验不得成边；三条自有设施判据全中才可按吞吐并入。
  - **>1000 或跨币种大规模服务＝设施级**：一律剔除其串联边——不剔的实测后果：单个"实体"聚出 848 个成员的大杂烩（"所有从同一交易所提币的人"会被并成一个实体）。
  **枢纽只剔边、不剔成员资格**：与实体核心强连接的枢纽（如百万笔级专属互转）本身可保留在成员表（吞吐口径，现仓另计），只是不许经它把外人串进来——BFS 抵达枢纽即停、不经它继续扩散。**脚本口径分工**：cluster.py 的 >200 度数线是本币种图上的初筛保护线（先粗剔明显设施防串并），wave_scan 的 top_sender_global_out_degree 是裁决参考字段（本币种全史边表口径）——两者都不是终裁；终裁按本条全历史口径逐个枢纽人工核查，写进报告前过一遍（HYPE/OPN 848 员大杂烩 07；Pointless 原子中转 07-13；VEX 二次分层 07-13；ASTEROID 半枢纽 3367 假簇 07-18；LPT 自有设施判据 07-21；QUQ 两段处理 07-22；GMX 出度检验 07-26；2026-08-02 用户统编六条旧目）
- **同模板 bot 路由同样剔除（与上条互补——模板孪生体单个度数可能不超线）**：同一字节码模板批量部署的 bot 路由（双胞胎/多胞胎合约，各自数千对手方）会把互不相关的用户粘成两位数百分比的假"大实体"——设施剔除名单必须含"同模板高对手方合约"排查（getCode 哈希分组 + 度数阈值）（PING，07-17）
- **公共尘埃/撒币/清扫网络剔除（"专属后勤"错觉的解药）**：集群外围常见一个向各分仓统一发小额币的"撒币"地址、或回收各分仓尘埃的"清扫"地址——**看着像集群专属后勤，实测常是服务上万地址的公共设施**（1inch 生态尘埃分发、swap WBNB 中转跳板等）。任何拟作"集群服务地址"的候选，落笔前必查其**全网服务面**（对手方总数）：对手方 >1000 且集群地址只占其流量个位数百分比的，判公共设施、**不作实体专属证据**（实锤：SIREN 两个初判"集群撒币/尾币归集"地址实测服务 2 万/9 千地址、集群仅占 3.8%/0.44%，被复核剔除）（SIREN 复核，07-19）
- **CEX 热钱包不可作为共同资金来源证据**：从同一热钱包提币的地址不构成关联（热钱包是全用户共用资金池）（OPN/HYPE，07）
- **同尾数批量提币＝弱指纹（同批次信号，须带分母）**：CEX 按固定费率扣手续费后，同费率期的整数提币到账额尾数相同（如多址同现 `.974486181` 尾数）——它首先只证明"同一时段从同一所提币"，**单独不证明同人**（体系外反例实证：同尾数地址同日独立清仓、与体系行为完全无关）。按弱指纹使用：必须给出批次规模分母（同尾数同期总共多少地址），大批次作废；只作 ≥3 条独立弱指纹组合的一员，与同步移仓窗/共享归集口/长周期同构囤仓等其他证据合用。伪影对照法：真同批伪影=跨多日小额分散；可疑簇=单日窄窗+金额同量级+后续囤仓协同（BANANAS31 复核，07-22；2026-08-02 用户修订升弱指纹）
- **DCA 定投服务合约会伪装成"化整为零分发器"**：出方是数百个互不相关用户、形似庄家分仓网——**入方构成检验**一步拆穿：入方 99%+ 是池子（服务代买）=定投服务；真分发器的入方是金库/母仓。判定为服务后其用户间不构成任何关联边（PING，07-17）
- **两跳下游逐节点合约体检**：每跳跑 getCode、标签与服务度数；公共合约只作路径节点。大集群须先清假桥再报 strict/expanded 双层口径。判例见 casebook E-02。
- **"标签库命中 registry 官方地址"优先级高于"行为像私有合约"**：跨链桥履约地址（deBridge taker 类）行为形似私有执行合约（低余额、定向转账、少数对手方）——裁决规则：labels 库/官方 registry 亲验命中即判**履约管道**、禁作合并边，行为学证据不得推翻 registry 身份（PING 复核，07-17）
- **最大集群必须回查首笔资金来源验证性质**（官方资助网络 vs 操盘实体是两种完全不同的结论）（HYPE，07）
- 区分"官方控制"与"来源官方"：从金库拿过币 ≠ 被官方控制，两种标签分开打（OPN，07）
- **【正式·三见】截断地址禁止补全——补全=编造**：完整值只从落盘原始交易/JSON 取得；拿不到即标“未核验”，不得继续查询或入表。判例见 casebook E-14。
- **原子中转率扫描的工程细节（三段式检验判据③的批量执行法）**：重放前先算每地址"原子中转率"= Σ min(该地址在某 tx 的转入额, 同 tx 转出额) / 总流入，>90% 名单按三段式检验判据③处理（公共/私有二次分层同见该条）。命中的公共管道并入 `market/infra` 集合当"市场腿"处理——否则它们的巨额过手流量（单个可达数百 M）会顶在庄家榜前排。比逐个 getCode 快且不漏；判据独立于"是不是合约"，7702 账户/EOA 中转也能抓到（Pointless/VEX 实测 2026-07-13；2026-08-02 统编入三段式，本条留工程执行细节）。
- **漏斗地址检测必做**：扫"收自非市场地址且 ≥90% 即收即卖"的地址（RAXOL：12 个经手 9.37% 总量），再对最大"散户"卖家逐个查上游——归集出货网络（N 上游买入→1 hub 卖出）会完美伪装成散户卖压（RAXOL，07-12）
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。
- **★“归集出货 hub”定性前必查出账签名结构** — **触发条件**：地址呈多上游归集、同 tx 代卖或即收即卖。**必做动作**：核出账 txfrom 是否为 hub 本体、eth_getCode、浏览器全链 tx/币种/调用者分散度。**阻断语义**：出账均由外部调用说明是合约工具；“经同一合约出货”永不构成协同证据，单币用户少也不证明私有。**权威脚本**：交易调用者、getCode 与全链 counters。案源：RAXOL 2026-07-14。
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。

**伪造与投毒防护（对抗性数据环境的免疫层）：**
- **地址投毒防护**：资金上游归因只认大额转账（设阈值过滤尘埃转账），且比对完整地址而非前缀——曾把伪造 vanity 前缀的投毒攻击地址误判为大户资金上游（FIL，07）
- **zero-value transferFrom / Solana 非零粉尘投毒过滤** — **触发条件**：构边、活动性、静默起点或同秒共现统计读取 Transfer 事件。**必做动作**：EVM 一律 value>0；Solana 对 amount ≤1 raw（或相对供应可忽略尘埃阈）一律剔除，并在正文把仿冒地址零值事件写作钓鱼信号而非本人动作。**阻断语义**：未过滤不得构边、计算 last_ts 或引用同时性。**权威脚本**：BFS、cluster_prep_duck edges_agg 与链别重放过滤。案源：BANANAS31、EGL1、TROLL 2026-07-22～29。
- **vanity 定制地址的两种操盘方用法**（与上条"被投毒"相反，这是操盘方自用）：①**配对自证**——注资金主与收币大户地址共享 8–26 位十六进制**中段**（前后缀不同），=批量定制的配对地址，同一实体最硬指纹之一（铁证级；识别看中段不看前缀）；②**仿冒基建伪装**——生成前 12+ 位仿冒 LiFiDiamond/Relay 等真实路由的假基建 EOA 分发筹码，专骗肉眼排查。识别：与真基建地址逐位比对（前缀同、中段不同）+ `is_contract=false` + 无浏览器标签 + tx 数少。三条 vanity 规则合一：**凡涉 vanity 一律完整地址逐位核对**（外部 CASHCAT/GME 考古，07）
- **外部尘埃投毒可伪造“出纳网络”/gas 边** — **触发条件**：共同来源仅发尘埃级资金（提示线 ≤0.001 ETH）。**必做动作**：拉 funder 本体全量 tx；检查全部收款人是否混有未持仓/已清仓/公共 bot；再做同链跨币同额同窗重叠。真出纳旁证为 <500 tx、发射前注资、0.1–1 ETH 建仓级金额；dust 常在持仓形成后且 <$2。**阻断语义**：≤0.001 ETH 不是自动否决，但无差别撒网必须剔边；只看与大户相交子集不得定专属。**权威脚本**：funder 全史与跨币收款人构成。案源：CASHCAT 2026-07-13；2026-08-02 修订。
- **实体启动 gas 的上游可以是灰产资金边（背景画像，不是合并边）**：实测某吸筹中枢的启动 gas 上游带 ChainPatrol 投毒 bot 资金池标记——可入报告作背景画像与风险提示，但公共灰产池逻辑同公共热钱包，不构成实体合并边（PING，07-17）
- **关键地址与 topic0 一律取落盘完整值**：终端、memory 与复核转述的截断串不得补全；后续引用输出完整值。判例见 casebook E-14。

- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。

- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。

**行为指纹判定（同步/面额/委托类——每条指纹先问误报率）：**
- **行为指纹三问总闸与强弱两档（2026-08-02 定）** — **触发条件**：任何同步、面额、委托或结构怪癖拟进入报告。**必做动作**：依次回答同期分母、同工具/环境对照组出现率、协议机械行为排除。过闸后，带统计/分母的强档（§6.5 节拍、同秒等额、金额+间隔、tick 同期挂撤）单条可到“高度疑似”；弱档需 ≥3 条互相独立，且同一证据家族只计 1 条。**阻断语义**：任一问未过即作废；候选发现扫描与归属规则不占指纹档位。**权威脚本**：对应扫描器与全场分母统计。
- **同时性共现（同秒/同块）家族——一个逻辑、三档用法（2026-08-02 用户定，三条同族规则合并呈现；各档数值与出处保持原条原样）**：核心逻辑一条：独立**人工**操作难以形成"同一秒/同一区块"的集体动作；但共享工具、协议机制、公共执行服务同样能产生同时性，任何一档使用前都要按三问总闸排除。链别边界："同块≈同秒"仅 BSC 类快链成立（~0.45s/块）；慢链（ETH ~12s/块）同块内可有十余秒先后——同秒判据（时间戳同一秒）跨链通用，"同块"口径的证据力按该链出块速度评估。三档按用途分：
  - **① 候选发现档（低档同秒共现扫描）**：候选全集须覆盖 0.1%–候选线地址；同一秒 ≥5 址买入即进入裁决，不占指纹档位。不得只靠份额线筛选。判例见 casebook E-15。
  - **② 单币强指纹档（同秒等额买入）**：≥5 钱包在同一秒从池买入，且报价币输入 wei 级面额一致；必须做同期全场面额独占检验，大众整数面额作废。组内资金闭环可升铁证；没有则上限“高度疑似”；<5 钱包不作指纹。对照组必须取同工具全体用户。
  - **③ 跨币强证据档**：跨代币共现聚类场景的同块共买强证据（同块比例 ≥85–90%＋块内位置恒差）细则见下方「特殊场景手法库」跨代币共现条——上限"高度疑似同一执行端"，升"同一实体"须补独立控制证据
- **"同分钟同金额转出"≠洗仓分仓**：必须核实是否存在"老仓→马甲"的直接转账；RAXOL 案例中马甲实为同分钟**从池子市场买入**（老仓早已割肉卖出），机制完全不同、成本完全不同。判据：grep 组内互转，零条即排除转账式洗仓（RAXOL，07-12）
- **面额指纹要先对全体参与者做独占性检验再引用**（COMPUTE 教训：初稿引用的 0.12×5/0.1667×3 实际属于名单外地址，复核抓出张冠李戴；0.05/0.1/0.25 等常见整数面额大量撞额无区分力，wei 级奇葩面额如 0.025225 才是硬指纹）（COMPUTE，07-12）
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。
- **EIP-7702 delegate 指纹的正确用法**：不能笼统以"是通用/参考实现"驳回同 delegate 关联信号——判据是**产品分发面**：主流钱包各用自家 delegate 合约，无产品前端的参考实现 singleton（如 Simple7702Account）使用面极窄，多个大户委托字节级一致恰是小众指纹；单独同 delegate 仍不足成边，须与其他证据组合（bibi，07-12，复核曾据此翻出漏检的 PLAUSIBLE 集群：同 delegate + 同跨链通道 + 归集硬边 + 时间同步四指纹）

**gas/资金面溯源纪律（正查+反查+性质分级）：**
- **注资证据分级（gas/资金同源边的强度标尺）**：**私人 gas 钱包**（本体 <20 笔交易）供养多个母钱包＝强证据；**同窗批次注资**（多地址在同一 ~90 分钟窗集中入金、多波重复）＝中等证据；**公用桥 solver / CEX 热钱包同源**默认剔除，仅当"同 48h 窗 + 行为指纹一致"才升中等。报告按分级分开计数/画图，强证据簇与同窗批次簇不混在一个数里（外部 GME 考古，07）
- **gas funder 判“私人”前必过公共性三步体检** — **触发条件**：拟以 funder 建 gas/注资合并边。**必做动作**：在 funder 活跃窗核总笔数、余额量级、收款人分散度，并检查其是否同时服务已独立定性的地址。**阻断语义**：已弃用清空不证明私人；出现互不相关服务对象即公共设施，基于该单一 gas 边的聚类整体作废。**权威脚本**：data-pipeline-robinhood-methods 链上操作与 gas_origins 反向索引。案源：Pointless、GOAT 2026-07-14～24。
- **★Solana 注资边的 my_sol_delta 先决字段闸** — **触发条件**：消费 gas_origin/gas_origins 的 funder 字段。**必做动作**：先查 my_sol_delta；≈0 表示目标未收 SOL，该账户只是 fee payer。再核同 tx 是否为 Jupiter DCA/Router 等自动执行、同 keeper 是否服务互不相关地址；approx=true 单独不成边。**阻断语义**：my_sol_delta≈0 一律禁作注资/gas 同源边。**权威脚本**：gas_origin/gas_origins 与交易级 SOL delta。案源：GOAT 2026-07-24。
- **gas 档案必须双向用（正查金主 + 反查下游）**：只查"候选的金主是谁"会漏"候选自己是别人的金主"（实锤：某"独立新面孔"实为已知庄家集团最大成员的 ETH 金主，发射前 6 笔 8.7 ETH，这条边一直躺在档案里）。gas_in 档案建 funder→下游反向索引，聚类前对全部候选跑"它给谁供过金"；已知实体成员的 funder 也回头对照候选集（TRASH 更新复核，07-17）
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。
- **gas 同源边要查时序**：母钱包供 gas 若晚于建仓，只能证明"建仓后共用补给"，不能写"母钱包出资建仓"（GME/RAXOL 合并手册，07-12）
- **发射前预注资是"预谋建仓"最硬的时序证据**（COMPUTE：专属母钱包发射前 1-2 天批量注资矩阵成员，且与官推注册同 48h 窗口），值得专门做链上-链下时间对齐表（COMPUTE，07-12）

**全景完整性与阴性结论（写"无"之前的义务清单）：**
- **发射窗口买家全景从第 1 秒扫起**：按窗口内累计买入而非期末持仓取候选；否则快闪客物理不可见。判例见 casebook E-16。
- **金额+间隔指纹（发射窗协同检测的独立通道，对"gas 刻意分离"的协同单元有效；原"镜像执行扫描法"）**：对发射窗全部达锚仓（如峰值 ≥1%）做**全对（all-pairs）买入序列扫描**，命中条件＝**①≥10 个地址；②对应笔下单间隔 <20 秒；③金额比（小额÷大额）≥0.8；④以上模式在 ≥2 个批次重复出现**。价值：互转/共同 gas/两跳中转全阴的协同单元，仍会在**执行层**露出节奏——同一操作者先后操作一批钱包，下单间隔和金额比例都会贴得很近。命中后按各批次内的先后顺序分两支处理，**两支都必须先过行为指纹三问总闸**（见上方总纲条）：
  - **分支 A（各批次内动作顺序稳定，或已通过 §6.5 节拍检验）**：形似"同一份钱包名单被同一程序按序遍历"，但公开跟单也会产生稳定顺序（跟单者永远慢于信号源）——必须补**信号源检验**：查组内是否有恒定先手、组外前窗是否存在被全组跟随的共同先行地址。查到公开跟单信号源（该先行地址同时被大量组外用户跟随）→整组作废；查无信号源→升至顶格"高度疑似"。
  - **分支 B（各批次先后顺序完全随机、无恒定先手）**：跟单结构必有先手，先手随机轮换说明不存在"信号源→跟随者"关系，是同一控制者手里的钱包池被随机调度——直接按顶格"高度疑似"处理。
  确权措辞上限为“高度疑似”（行为指纹永不实锤）；参数与两分支规则以本节契约为准。
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。
- **大户入方溯源必须独立于峰值普查表（低余额高吞吐"隐形管道"盲区）**：按"历史峰值 ≥0.1%"建的普查网会系统性漏掉余额从不过线的执行/中转合约（收→转瞬时过账，峰值≈0 但累计吞吐巨大）——"大户的币从哪来"必须按该大户**全部入账对手方**独立拉清单逐个定性（实测：某大户主力仓位经一个峰值从未上榜的执行合约进货，主分析漏检靠复核补齐）（PING，07-17）
- **历史静置仓反向扫描硬闸（冻结实体前必做；正向 BFS 不能替代）** — **触发条件**：完成初步聚类、准备冻结实体/算峰值/出图。**必做动作**：生成 dormant_warehouse_audit.json；候选全集从三条现役机械通道取齐：wave_scan v3 全体历史峰值 ≥0.02% 地址、entity_source_trace direct_upstream、边界外一圈反查。scan_universe 必逐址记录峰值/现仓/留存桶与 must_adjudicate；必裁决包括峰值榜前 200、越线 ≥0.1%、回落 ≥80% 或静置 ≥30 天且峰值 ≥0.05%。以 universe_ref{path,sha256} 绑定审计。**阻断语义**：无产物、旧 v2、哈希/集合不闭合或有未裁决候选即 fail-closed。**权威脚本**：wave_scan v3、entity_source_trace、audit_release_gate。

  对每个候选逐项反查：建仓币源及前驱链、首次/末次活动、最长静置区间、关键日激活顺序、退出去向、原生币/gas 关系、公共设施属性与冲突证据。`dormant_warehouse_audit.json` 至少记录 `scan_cutoff`、`candidate_address`、`historical_peak_raw`、`current_raw`、`source_path`、`dormancy_intervals`、`key_date_actions`、`facility_checks`、`evidence_grade`、`boundary_decision`、`decision_reason`。裁决只有三类：`strict`（私人币流/资金/gas 或控制权闭环足以纳入严格实体）、`expanded`（币源闭合且关键日独立同步，但法律所有权或跨公共设施控制仍不可证，只纳入强关联体系上限）、`excluded`（公共 CEX/路由/协议设施、仅同日行为或存在反证）。**"同源 + 同日动作"不再自动并入同一主体**；公共设施只作路径节点，不作永久成员和确权证据。

  计量强制双边界：`strict` 联集给严格下限，`strict ∪ expanded` 联集给扩展上限；两者都在各自**同一交易末快照**逐事件重放后取峰值，禁止把各地址或各分支不同时刻的个人峰值相加。报告必须列出两口径各自成员数、峰值时点、峰值构成和相差来源；若没有 expanded 候选，也要在审计文件中显式写空数组，证明扫描做过而不是没做。
- **判例指针**：本组方法级失败模式及案源见 casebook S-08。
- **净盈亏≈0 群体的归因边界（量能虚胖四件套）**：呈现高换手/疑似做量群体的标准件=①占比+多阈值稳健性（净差 2%/5%/10% 阈值下的占比区间）②构成分解（机器式高频 ≥6 笔 / 1-2 笔完整进出的普通交易者 / 已标注实体，防重复计数）③原子性检验（买卖 tx 与同 block 交集——零原子性可排除三明治/原子套利）④群体已实现盈亏（净盈利可反驳"亏损刷量"）。**"客观点火效果"与"主观点火意图"分开表述**："大买速卖客观上引来接盘"可写；"协同做量"在组外存在同模式独立地址时不可写——独立 bot 抢跑失败天然产生分钟级净零往返（NOXA 复核，07-15）
- **出货回款归集排查的表述边界**：本地池侧数据只能证明"池的直接收款人无归集"——回款经公共路由 unwrap/转发后的最终去向不在本地数据内，必须写成"池侧直接收款人无归集；经公共路由之后的去向本地不可证"，否则复核按"超出可证范围"降级（meow 复核，07-15）
- **"其他大户互不关联"阴性结论三查（写"无漏检小庄"前必做）**：①两两互转扫描 ②gas funder 分层（含 gas_in 的 self_alias 字段——L1 桥别名自充值≠独立金主）③同窗注资/同秒建仓/共同下游排查。三查后仍剩的弱信号（同分钟建仓对、若合并将破线）按**"待证关联对"列观察哨重点监控**，不并入也不无视（NOXA 复核，07-15）
- **贴线实体的双口径呈现范式**：疑似关联实体合并份额骑在判级线上时（如硬证据口径 4.99% vs 含弱行为边口径 5.17%，线=5%），**两口径并列呈现、按证据下限判级**（不判庄），同时列观察哨最高优先级+给下次更新"补溯源+重扫"明确指令；跨小组只有工具/面额/时序证据而无资金边时，表述封顶"高度疑似同一工作室"，不写"单一实体"（TRASH 更新，07-14）

**已知系统性偏差：** 互转图聚类的数字**系统性偏低**——同一实体换新钱包续持会被拆成多个独立社区，曾有延续仓被拆成两个社区各自低于报告阈值（SIREN，07）。因此："不存在 >X% 集群"永远要写成"**链上可证范围内**无 >X% 集群"。

**措辞锁定：** 聚类结论一律"高度疑似同一实体"，不写确权——共用出纳也可能是做市商/OTC 服务商同时服务多个独立客户；把证据链逐条列出（每条边标日期+金额）让读者自判（IO，07）。

**特殊场景手法库：**
- **CEX 注资失效时的隐藏关联识别——跨代币共现聚类** — **触发条件**：钱包均由 CEX 提现，funder 失去区分力。**必做动作**：抓近 1 年全代币流水（BscScan tokentxns ≤1000 行/钱包）→过滤空投→支持度 ≥3 频繁项集→Jaccard→6h 共时首买/首卖→并查集。Jaccard 0.9+、买卖双向对称且互有直转可判同体；
  同块共买 ≥85–90%、中位差 0 秒并有块内 index 恒差，只到“高度疑似同一执行端”，必须同时报告共同买入分母与样本量，升同体另需私人资金边/回款归集/目标余额驱动。持仓画像旁证仅单向支持独立跟单：先排除设施，历史截断时不得强判；杂乱可支持独立，统一/全空不得反推同体。交易 50–500 种币、CEX 开号且无差别狙击的新币职业号不等于内幕。
  **阻断语义**：公共跟单/bundle 未排除不得升同体。**权威脚本**：跨币流水、频繁项集/Jaccard 与同块位置统计。案源：CZ(BSC) 2026-07；2026-08-02 修订。
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。

**半枢纽带**：47–200 度节点必须走三段式构成检验；vanity 全零前缀 gas funder 不作 R2 种子。未完成不得成边。判例见 casebook E-02/E-16。

- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。

**冻结后实体成员卫生审计** — **触发条件**：庄级成员冻结后、算峰值或画图前。**必做动作**：①逐址以 getCode/标签/协议角色/服务面剥离公共设施，并按经济归属处理托管时段；②移出仅有尘埃、无自签/等额回流/同步处置的成员；③从执行/归集网络向外再扫一圈，只补正额流入逐笔闭合且非公共服务的 EOA。**阻断语义**：任何成员增删后必须重算 current/peak/曲线/附录/state；行为闭环不自动升级法律所有人。**权威脚本**：entity_identity_gate、标签库、合约四测与同刻余额重算。案源：QUQ、SIREN 2026-07-22～24。

**【正式·两案】公共基础设施先验四测** — **触发条件**：拟把高频合约判为归集器、专属枢纽、主仓或专用 bot。**必做动作**：按序做：①先 eth_call token0/token1/fee/factory 四 selector + Sourcify 名，命中 DEX 工厂即设施；②getCode；③部署早于代币创建只作公共信号，晚于不作专属信号；④跨币全局服务面、部署者与部署时间。同模板第一解释应是标准 factory 实例。**阻断语义**：本币内专属与同模板均不证明专属；四测未过不得入成员或惯犯库。**权威脚本**：sourcify_check.py、四 selector、跨币 Transfer 反查。翻案见 casebook E-02；案源：QUQ、SIREN 2026-07-22～24。

**成员表合约地址准入硬闸（pool-probe）** — **触发条件**：定稿前和对抗复核时，或任何合约被叙事为归集/中转/分发角色。**必做动作**：对全部合约无差别重跑四 selector、Sourcify、公共标签；vesting/锁仓协议另以验证名、跨币服务面和“多址存入→长持→等额赎回”识别。命中池/路由/EntryPoint/托管 Vault/vesting 时从成员表剥离，改以权益载体进入经济控制账。**阻断语义**：复核不得沿用主分析标签；未独立探测不得冻结成员或引用该角色。**权威脚本**：sourcify_check.py、四 selector 与经济控制账。翻案见 casebook E-02；案源：QUQ、SIREN、SPX6900 2026-07-24～25。

- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。

---
- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。

- **判例指针**：本组方法级失败模式及案源见 casebook E-14～E-17。

**归属证明：层数封顶与双向合龙** — **触发条件**：BFS 扩散用于说明成员归属。**必做动作**：4 层封顶，遇公共设施立即截断；把行为/控制指纹作为归属证据、可达域只作地图；任何指纹先过协议机械行为与合约身份四测。再用自上而下 BFS 与自下而上通道穿透的交集双向合龙，未入交集者只用独立指纹补焊。**阻断语义**：不得把“可达”当归属证据；协议机械等额返还零协同证明力。**权威脚本**：BFS、合约四测与反向通道重放。案源：SIREN 2026-07-20～24。

**同窗大额动作必须给同期分母**：扩大到数日统计同量级动作总数；同期常见则主张作废。即使稀有也只作行为指纹，维持分列并并列合并风险口径。判例见 casebook E-15。

**共同上游基准率反例**：执行规则与案源见 casebook E-17。

**EIP-7702 委托目标先过普及率检验（判例 casebook E-01）**：codelen=48 且 `0xef0100` 开头＝委托 EOA 非合约；委托目标是公共实现（MetaMask StatelessDeleGator 类，人人可用）则该边一律不成立，真实归属另找资金链证据（KOGE/IQ 两案同型）；窄面参考实现的弱信号用法见上文 bibi 条，两条判据统一于"产品分发面"。

---

## §6.5 买入序列节拍指纹（聚类三规则同时失效时的破局判据）

**契约**

- **触发条件**：cluster.py 的 R1 互转、R2 共享 gas、R3 金库血统均未触发，且出现任一信号：异常平坦中户带、短窗集中建仓、或集群覆盖率远低于大户持仓总量。
- **必做动作**：至少取两个相隔较远的批次，对同一地址组的首笔动作排序；用 **Spearman ρ + 3000 次排列检验**，不得再以位置精确匹配为主判据。scripts/evm/cadence_rank.py（候选池细扫）与 scripts/evm/cadence_fingerprint.py（全库粗扫）必须配套跑。
- **阻断语义**：只有一个批次、未剔零值事件、未做留存/换手分流、或未查网络对外流向时，不得以节拍合并或判庄。节拍完美只证明程序化协同，措辞上限按 playbook-evidence-wording.md。
- **权威脚本**：scripts/evm/cadence_rank.py、scripts/evm/cadence_fingerprint.py。

**判据与分流**

1. 完全正序/完全逆序是同一钱包数组被重复遍历的强指纹；n 个地址随机完全逆序的概率为 1/n!。部分匹配必须交给秩相关与排列检验，不用脆弱的位置精确匹配。
2. 持仓型（留存 ≥50%）才进入庄候选；累计买入 ≥1 倍总供应或收发 ≥5000 笔归过手型 bot；其余归已离场残仓。
3. 跨批次是归并核心：一个遍历窗中的地址即使分散在多个建仓批次，只要顺序仍对应首次买入的全局时序，也可归为同一主名单；仍须排除公开信号源/跟单解释。
4. 收尾必须查母层对外流向。母钱包向当时为空的钱包单向转币、零回转，且受赠方收币后才入场买同币，是比节拍更硬的直转边；否则不扩成员。
5. “一址一个不同 gas 注资方”不得直接当独立性证据；继续查各注资方 nonce、余额、代码与公共性。一批 nonce=1、余额 0、无合约代码的一次性中转地址，反而是规避 gas 同源检测的指纹。
6. 自然持仓应近似单调长尾；异常双峰只作启动节拍扫描的独立旁证，不单独成边。

案源：EGL1(BSC)，2026-07-26；初版位置匹配漏检与二次修正的判例见 casebook/entity-clustering.md E-04。

---

## §6.6 gas 同源与 vanity 指纹的两个必做否证检验

### A. 共同 funder 的出度检验

- **触发条件**：拟以共同 funder / gas 来源合并两个以上地址。
- **必做动作**：查 eth_getCode 与全历史出账对手数（普通交易 + 内部交易的不同出账对手）；明显超过 1000 可停止计数。按 §6 三段式处理：≤15 私人级；16–1000 灰区，继续做收款人构成检验；>1000 或跨币种大规模服务为设施级。
- **阻断语义**：funder 未证为私人钱包（≤15、非合约，或已有独立证据确属该实体）前，共同来源不得作合并边。
- **权威脚本**：链上 eth_getCode、普通/内部交易全史与 §6 中间节点三段式检验。

案源：GMX，2026-07-26；服务枢纽制造“离场庄”的翻案见 casebook/entity-clustering.md E-05。

### B. vanity 的逐字节与随机期望检验

- **触发条件**：拟以相同前缀、后缀或中段的 vanity 地址作为控制证据，或报告随机概率。
- **必做动作**：逐字节计数（1 字节 = 2 个 hex）；计算“搜索空间大小 / 16^匹配位数”的随机期望，并将实际命中数与期望比较。前缀 + 长后缀同时命中才可进入证据评估。
- **阻断语义**：命中数与随机期望同量级时不构成证据；短前缀不得成边，概率未复算不得写入报告。
- **权威脚本**：完整地址逐字节比较与候选地址空间统计；不得使用截断地址。

案源：GMX，2026-07-26；hex 字符/字节误算与短前缀随机碰撞见 casebook/entity-clustering.md E-06。
