# CHANGELOG — codex 分支独有迭代

本文件只记录 **codex 侧自己产生的版本**。Claude Code 侧的迭代史在 `CHANGELOG.md`
（该文件由 CC 侧单向下发，codex 侧不修改，故永不冲突）。

⚠️ **版本号历史事故**：2026-07-23 从 CC 侧 v3.25.0 复制分叉后，两边各自迭代且都从
3.26.0 续号，造成 **3.26.0 / 3.27.0 / 3.28.0 / 3.29.0 / 3.29.1 / 3.30.0 六个号在两边
含义完全不同**（另有 codex 独有的 3.29.2 / 3.29.3）。下方保留 codex 侧原貌不改写历史；
引用时务必注明「codex 侧 3.x.x」以区分。

📌 **此后新版本号规则**：codex 侧新条目一律用 `c` 前缀，与 CC 侧的 `3.x.x` 轴彻底分开，不再有撞号可能。
- **下一条从 `c1.0.0` 开始**，写在下方版本索引的**最上方**（3.30.0 那条之上）；详细章节同理写在 `## [3.30.0]` 之上
- 三维含义与 CC 侧一致：主=架构级重构 / 次=每次分析复盘迭代 +1 / 修=文档小修
- 提交流程与「不许改的文件」清单见 `SKILL.md`「Codex 运行适配 → codex 侧迭代本 skill 的规则」；机制全貌见 `SYNC.md`
- ⚠️ 本文件**不在 `changelog_lint` / `docs_lint` 覆盖范围内**（两个 lint 只扫 `CHANGELOG.md`）——版本号唯一性、粗体配对、引用断链需自查

## 版本索引（codex 侧，新在上）

- **c2.2.9 2026-09-12 SQD 外排与断点复用修复**：以磁盘排序、逐交易完整边集核对替代全史有序摘要聚合；缩小 CSV 缓冲以保留外排余量。只接受正式 ACTIVE 的旧 v4 producer，成功后按当前生产者重签，支持保留下载分片。共享边校验拒绝浮点指令索引，交易摘要排序补全稳定末级键；增加百万交易低内存、零重采续跑、冲突和撤销负测。

- **c2.2.8 2026-09-07 同步主线 7.0.2 严格封口与图一口径**：合入主线生产者四协议登记及守卫、重复专用路径硬拒绝与图一生产器格式入口；保留 Codex 性能复用、终态绑定修复、图内数字闸和兼容 helper。修订旧绑定恢复夹具，先证重复输入拒绝且不产 seal，再验证历史重复件的受限恢复。
- **c2.2.7 2026-09-06 终态绑定修复与 A4 专用路径去重**：新增严格等价证明、完整归档和原轮次重放的 `repair-bindings` 入口；既有 finalize 不再重复封装 registry/verdict 路径。补齐共享发布收据装配要求及拒收回归。

- **c2.2.6 2026-09-06 图一消费既有生产器分母语义**：Solana `sol-rows` 的累计销毁按既有 sidecar 契约独立披露；绘图、图例、A5 校验及发布检查一致，EVM、anchor 与 legacy 含义保持原契约。

- **c2.2.5 2026-09-05 装载、依赖复用与确认调度**：内容绑定的 Solana 原始行物化；确认与来源计算分离；P4 使用自己的独立重放凭证；完整读证据深验复用；修改与验收仅在 Codex 分支。

- **c2.2.4 2026-09-04 SQD repair 来源登记闭合**：在已同步的 7.0.1 上补齐 v6.52.7 修复程序四项历史登记；两个正向测试改用真实登记表，新增当前文件哈希完整性与缺项/撤销负测；main 与真实案例数据只读。
- **c2.2.3 2026-08-16 time verifier final-block 回填修复**：`shared_release_receipt` 深验 balance 锚点时，计划缺 `day_end_block` 改与正式执行器一致回填已签名 `plan.final_block`；补 producer PASS、旧 consumer RED、修后 GREEN 的回归反例，转账块和显式日终块语义不放宽。
- **c2.2.2 2026-08-10 同步门禁修复**：压缩 `SKILL.md` frontmatter 触发描述，保留链支持、正式能力与分析范围语义，使文件恢复至 8192-byte 硬上限内；不改方法契约。
- **c2.2.1 2026-08-01 同步 6.6.0–6.7.0 三提交＋G4/G5 载体职责终结**：main 下发波次扫描三硬闸（6.6.0）与双闸（6.7.0：time_spotcheck 时间抽查执行器进 READY 必备——**−1 执行者本副本直接相关**，A2 第 4 查改跑 scripts/lib/time_spotcheck.py 默认锚点直查、全史 SQD 重拉降例外；A4→A5 顺序硬闸 a4_gate 封口属 −2 侧，本副本仅停止线语义不变）。SKILL.md 冲突解法：阶段表 A4/A5/A6 取 main 新行＋A6 落盘产物保留 CHANGELOG-codex 适配；「实体冻结前三硬闸」正文节随下发进驻本文件，G4/G5 从同文载体收缩为指针（docs_lint needle 由正文节命中，消除同文双份漂移隐患）；G6 对 G4/G5 引用不变。
- **c2.2.0 2026-07-31 独有资产瘦身收尾（6.5.0 回灌落地后，用户三项拍板执行）**：G3 独有清单缩减为纯平台/机制件（openai.yaml、SYNC.md、CHANGELOG-codex.md）——三分册＋audit_release_gate 及测试随 6.5.0 转正为两边共有、main 权威单向下发，本副本停止单独维护；G4/G5 改标"与 main'实体冻结前双硬闸'节同文的本副本载体"（保留全文以满足 docs_lint 四层守卫 needles）；report-template 尾部"Codex 版补充义务"节删除（同源驱动段进 main 三账段、4c/4d 进 main checklist）；删除一次性迁移文档 c2-migration-matrix.md（用户批准；备份 `~/.codex/skills/c2-migration-matrix.md.bak_20260731_102005`，git 考古 `git show d0a2e05:c2-migration-matrix.md`）并清 SKILL 第 0 步引用；SYNC.md"永不合并"改"禁整分支反向合并、经用户裁定可定向回灌"（用户批准的约定措辞澄清）。历史遗留说明：c2.0.0 存档分歧 state-anomaly §9c 的 codex 弱化版考古坐标改记于此＝`git show 639bfd0:references/playbook-state-anomaly.md`（原记录在已删除的矩阵 §五）。
- **c2.1.1 2026-07-31 独有资产审计小修（@CX 交叉复核发现）**：修复 `playbook-evidence-wording.md` ★受益权分离条残留的"P0/P1 判级"旧措辞（→"庄级判定（大庄/小庄门槛，tiering §6a）"，系 c2.0.0 措辞迁移的漏网条目）；`SYNC.md` 约定 1 版本轴描述更新（CC 侧 `3.x.x` → 注明现已迭代至 `6.x.x`）。同轮审计的其余发现（audit 协议/gate 的 0.5% 排查线落后于 v5.0 的 0.1%/0.2% 双线、gate PASS_WORDS 宽松语义与空账本可过、G3 发布门禁对全新分析的作用域错配、c2-migration-matrix 归档、方法学分册回灌 main）属结构性裁决，待用户拍板后处理。
- **c2.1.0 2026-07-30 split-run −1 模式执行条款（G6）**：本副本正式接任 −1 机械段主轨执行者。SKILL.md 适配节新增 G6：触发语/开工探针与案级锁/范围＝A0–A2＋A3 机械子层/停止线八项（明确 G4/G5 冻结前硬闸在 −1 不执行由 −2 承担，停止线优先）/未档异常写 blocker 禁自创解法/全程盲化＋receipt/收工 generate 如实报状态完成即停/audit_input_manifest 与 handoff 契约互不替代/与 G2 禁自审兼容（A4 外部异构在 CC 侧按 split-run §3.3 收紧执行）。唯一权威源＝下发的 `references/split-run.md`。
- **c2.0.0 2026-07-30 v6 大同步（main v4.2.0→6.1.1 十提交并入）+ 适配层通则化重构**：按 `c2-migration-matrix.md` 先行矩阵执行 merge；SKILL.md 重构为 main v6 薄骨架＋「Codex 运行适配」通则节（G1 路径/工具/模型映射、G2 禁自审、G3 独有资产、G4 经济控制硬闸、G5 静置仓反扫硬闸——原逐文件改字式平台适配全部收编为通则，未来同步冲突面大幅收缩）；三问框架/去分级/狙击集团废止随 main 生效，codex 独有资产 P0/P1 措辞全部迁移（economic-control-accounting 4 处＋tiering 判据节＋docs_lint needle）；codex 复盘判据与三账义务重挂为 tiering/report-template 尾部独有小节；camp_share_series schema 修正（c1.0.0 发现）与 BITCOIN 案 miss-queue 85 行已回灌 main（6.1.1）；run_all 融合为 17 项（main 16＋audit_release_gate）全 PASS；唯一存档分歧＝state-anomaly §9c（矩阵 §五留裁决）。
- **c1.1.0 2026-07-29 删除 Codex 自审伪外审流程**：Codex 版不再调用 `codex exec`、Codex CLI 或同模型会话充当外部异构怀疑者；阶段 4 与 easy/update 模式统一改称内部对抗性自检，不宣称外部独立性。原始数据重算、机械对账、发布硬闸与内部反例自检保留。
- **c1.0.0 2026-07-29 独立分享包可运行**：`sync-from-cc.sh` 区分维护者双线 worktree 与不含 Git 历史的分享快照；后者明确提示并返回 0，不再因缺少 `.git`/`main` 阻断分析。同步规则、SKILL 第 0 步与分享说明同源更新；本机双线合并和测试门禁不变。
- **3.30.0 2026-07-26 既有报告独立复核净室协议+机器发布总闸+逢0轻整编**：把“报告是被审对象，不是证据”从措辞纪律升级为低自由度工程门禁。新增 `independent-audit-protocol.md`，强制输入哈希冻结、原报告只作命题清单、当前≥0.5%与历史/归零/静置候选重标、成员/位置/经济控制三账、逐命题证据与备择解释、历史图六查、对抗复核否决权；新增 `audit_release_gate.py` 与离线反例测试，缺任一必需资产、哈希漂移、受益权未证、阴性候选未闭合、末日封口/负钳零或未关闭否决项均 exit 2。修正旧“CEX间库存调度商=非庄”排他规则：只可确认强CEX连接通道，零DEX与流向CEX均不能证明最终受益人或必然非庄。逢0轻整编将散落的复核防线归位到一份直达协议，未删除仍有效规则。成本：交付后修正轮次数/终端调用数/用时未可靠自动计数，不补猜。质量：初稿关键结论2；复核裁决REFUTED 2（两个庄级标签）、WEAKENED 1（CEX身份）；漏检地址类型/私人层覆盖1类；传播级历史图错误1。
- **3.29.3 2026-07-24 交付后口径修正：控盘主数字升级为可证经济控制量**：纠正“公共设施不进实体成员表”被错误外推为“设施内筹码不计入实体控制”的结论级错误。新增 `economic-control-accounting.md`，强制拆分实体成员表、链上位置账、经济控制账；P0/P1 判级和 TL;DR 控盘比例统一使用“严格成员钱包自持 + 可证 LP/CEX/桥/质押/锁仓/vault/托管权益”，直接钱包余额只作位置拆分。LP 按目标时点可赎回份额、CEX 按可证明账户受益权、桥双边只计一次；设施总余额和仅充值未确权的所内余额不得强归庄。新增必需产物 `economic_control_ledger.json`、三层结论（可证下限/强关联扩展/未决暴露）、报告 checklist 与 docs_lint 四层守卫。成本：交付后修正轮次数/终端调用数/用时未可靠自动计数，不补猜。质量：新增复核裁决 REFUTED 1（控盘主口径）；漏检可归属设施权益 1 类；传播级控盘口径错误 1。
- **3.29.2 2026-07-24 交付后复盘：历史静置仓反向扫描升级为实体冻结前硬闸**：纠正“沿已发现主链正向扩展后就冻结实体”的流程错误——这会系统性漏掉长期静置、已归零、仅在关键退出日激活的平行库存仓，并把狭义行为群峰值误写成完整体系峰值。阶段 3 现强制生成 `dormant_warehouse_audit.json`，从逐事件历史峰值榜、已归零/大幅回落仓、长期静置仓、关键窗口上游和执行网络边界外一圈反向扫描；未落盘或候选未裁决不得冻结实体/判级/出图。裁决拆为 strict/expanded/excluded，严格实体下限与强关联体系上限分别在同一交易末快照重放，禁止跨时刻相加个人峰值；阶段 4 完整性角色必须独立抽样复扫，report checklist 与 docs_lint 加入四层守卫。另修正旧规则“同源+同日即并入体系”的过度确权倾向，并移除方法文档中的个案数字。成本：交付后复盘轮次数/终端调用数/用时未可靠自动计数，不补猜。质量：新增复核裁决 REFUTED 1（完整体系峰值口径）；漏检强关联静置仓网络 1；传播级峰值错误 1。
- **3.29.1 2026-07-24 SIREN(BSC) 交付后修正：临时托管双账本与图2连续性硬闸**：正式区分图1“链上位置账”和图2“经济归属账”；可逐笔闭合的锁仓/vesting/NFT托管本金在图1进入设施阵营、在图2继续计入原实体，公共设施仍不进永久成员表；`whale_series.json` 新增可选 `temporary_custody_checks`，`figures_from_facts.py check` 按托管区间和最低可归属原始本金拒绝“终值对但中途假清仓”的曲线，离线测试加入锁仓中途归零反例；report checklist 强制图注解释双口径。成本：交付后修正轮次数/终端调用数/用时未可靠自动计数，不补猜。质量：新增复核裁决 REFUTED 1（临时托管期间实体曲线错误归零）；漏检庄级实体 0；传播级曲线错误 1。
- **3.29.0 2026-07-24 SIREN(BSC) 完整版复盘：冻结后成员卫生、分段因果反证与图内数字硬闸**：实体 methods 将原“枢纽三段法”的公共合约收尾拆出并升为正式两案规则，新增冻结后成员全集三查（公共设施剥离且协议本金仅作时段经济归属 / 尘埃成员清理 / 边界外一圈自签名全额回流 EOA 反查），任何增删后强制重算 current/peak/曲线/附录/state；state-anomaly 新增“效应先于行为”分段因果闸，按首跌/主体处置/尾段分别对齐价格、实体边界净减少与执行路径，禁止用路由毛流量或同小时重叠冒充整轮因果；`figures_from_facts.py flow` 新增 `--strict-text-numbers`，拒绝 title/subtitle/卡片/边标签/footnote 中未走 facts 宏的成员数、日期、层数、金额和份额，离线测试加入旧成员数残留反例；report checklist 将浏览器验收从“图片能显示”升级为逐图语义核对。成本：轮次数/终端调用数/交付用时未可靠自动计数，不补猜。质量：初稿关键结论 8；复核裁决 CONFIRMED 5 / WEAKENED 4 / REFUTED 1；漏检庄级实体 0（成员边界一加一减并剥离 2 个公共设施）；传播级数字错误 0（视觉验收在交付前抓到并修正 1 处旧成员数图中文字）。
- **3.28.0 2026-07-24 QUQ(BSC) LP手续费专项复盘：V4四层账本、逐tick归因与“已实现”措辞硬闸**：修正旧版“V4只有净现金流、手续费算不出来”的过度悲观规则；新增 `lp-fee-accounting.md`，统一池子产生/仓位应得/当前未结算/历史已结算四分法，固化 `feesAccrued`、`feeGrowthInside`、逐Swap×逐tick重放、所有权历史与8项对账gate；channels 修正V3跨窗 `collect−burn` 条件和V4归属方法；evidence-wording 禁止把“窗口应得−期末未结算”直接写成已提现净收入；skill触发范围补庄家LP收入与V3/V4比较
- **3.27.0 2026-07-24 ASTEROID(ETH) 完整版复盘：行为组隔离、盘中峰值与可观测口径硬化**：tiering 正式新增“行为 cohort ≠ 单一实体”（同块/同额可能由协议参数机械制造；无独立控制证据不得合计判狙击集团、不得回灌惯犯库）与“官方自报角色只证角色、不证技术多签或 deployer 同控”；日终/粗粒度曲线不得替代逐事件峰值，盘中峰值须事件点补回；evidence-wording 固化“公开 CEX 标签地址余额合计”口径，禁止冒充完整托管/库存/净头寸；report-template 新增价格覆盖审计与缺口留白义务；EVM sources 新增 Blockscout internal-transactions incomplete=下限纪律；`accumulate_offenders.py --help` 改为纯只读，并 fail-closed 排除行为 cohort/非同一主体/共同控制未确证组，离线测试覆盖。**不删除存量 serial 标签**：历史行为组条目属于待人工复核遗留项，禁止自动批量清洗。成本：轮次数/终端调用数/交付用时未可靠自动计数，不补猜。质量：初稿关键结论约 8；复核裁决 CONFIRMED 11 / WEAKENED 9 / REFUTED 2；复核补出关键漏检 1；传播级数字错误 0（冻结前另修正 3 项数据/口径缺口）。
- **3.26.0 2026-07-24 FROGGIE(BSC) easy 复盘：阵营序列 schema 契约修正+惯犯回灌单案安全模式+两条聚类边界候选**：monitoring/report 两册统一 `camp_share_series={dates,series}` 唯一形态，figures_from_facts 对旧列表可读 fail+契约测试；accumulate_offenders 支持直接传单案目录、默认库增量合并防截库、无状态/零实锤组 fail-closed 不改输出，离线测试覆盖；methods 新增【候选】短窗口多来源归集只证归集后统一控制、来源钱包证据不足进 associated_source_addresses；tiering 新增【候选】大额串联只证资金 campaign、必须先机械过 P0/P1 门槛再谈自动化/刷量例外

---

## [c2.2.8] - 2026-09-07 — 同步主线 7.0.2 严格封口与图一口径

- 冲突处理采用主线 7.0.2 的重复专用路径 fail-closed 契约与 `fig1_series_format` / `fig1_excluded_series` 入口；生产者四协议登记的有效 SHA、commit 和状态保持双方一致。
- 保留 Codex `fig1_exclusion_reasons` 兼容入口、全部性能凭证与受限终态绑定恢复；三处分册继续明确 anchor 灰桶在账堆叠。主工作树未修改。
- 旧恢复测试夹具故意输入重复路径会被新门禁拒绝；先保存该 RED，再把夹具拆为拒绝断言与合法封口，后续历史重复角色恢复及十二类拒收检查完整保留。
- 独立内审发现并补回第七位置参数兼容，以及真值非对象 provenance 的明确拒绝；缺失或空值仍保留既有 legacy 行为。新增真实 PNG 等价正例与四类拒收反例。
- 合入正文后 SKILL.md 超限 60 bytes；仅压缩描述段的重复表述，两链禁止正式交接及发布、单项查询与深度分析的边界均不变。
- 验收：首轮完整套件 152/153，唯一失败为 SKILL.md 大小；接口反例先保留 RED。修后在授权环境完整 153/153（rc=0），新会话独立 Codex 内审 PASS（0 返工项），包含 16 张真实 PNG 与 base/final 各 21 组来源对象矩阵；不把内审或测试通过等同真实案例发布。一次因本地端口权限中止的受限环境重测单独保留，不计为通过。

## [c2.2.7] - 2026-09-06 — 终态绑定修复与 A4 专用路径去重

- `repair-bindings` 先独立重放既有终态，仅允许 G8 state 哈希随已封存的观察元数据新增而更新；身份行、成员、金额、快照和判断必须保持一致。
- 完整归档原 A4 及终态产物后，使用终态扫描绑定的真实前轮检查点开启同轮重放；恢复入口不直接声明终态，后续仍须原生扫描、解释与记账。
- A4 finalize 消除 registry/verdict 与普通封存文件的重复角色。登记新增凭据读写及逐文件替换语义；加入十二类拒收与原生往返回归，完整套件新增为 152 组。
- 分段装配文档补齐共享发布收据要求；没有放宽数值、身份或发布门禁。

## [c2.2.6] - 2026-09-06 — 图一消费既有生产器分母语义

- 四处消费者按 `camp_series_provenance.stack_exempt_for` 的生产器格式解释堆叠成员；`sol-rows` 的累计真实销毁独立于净供应持仓，图一坐标注明净供应分母。
- 图例、A5 图例核对和发布检查消费同一规则；保留原始序列数值，EVM、anchor 与 legacy 的锁仓及销毁存量语义不变。
- 在既有图表契约组加入实际 CLI、堆叠实参、四消费者一致与旧错误图例拒收回归；同步三处图例使用说明。本次不改变归属、来源传播或重放算法。

## [c2.2.5] - 2026-09-05 — 装载、依赖复用与确认调度

- 前移必填路径、算法内容与裁决预检；长计算前后核对实际裁决和名册，避免旧检查状态跨越输入变化。新增真实调用计数与两种竞态回归。

- 正式 `load_sol` 保存可核验原始行物化，保留整数金额、完整顺序和 Solana 来源；每次核验来源选择及数据库完整摘要。
- 来源计算排除确认和非计算登记字段；确认每次与当前结果重新绑定。P4 首次真实独立计算，之后只复用自己签发的同依赖结果。
- 深验用完整实际读证据单独证明复用，不扩大一级 frontier witness。损坏或无法证明未变时拒复用并按正式检查处理。
- 名册/确认/路径结构检查前移；不变更归属数学、图表算法或报告审阅范围。
- 运行开关、依赖边界及验收口径见 [性能复用说明](references/performance-reuse-codex.md)。独立副本验证后仅纳入 Codex；保留现有任务、失败记录及 main 原貌。

## [c2.2.4] - 2026-09-04 — SQD repair 来源登记闭合

### 修正
- 为已提交的 `sqd_gap_repair.py` 指纹 `25f04ff10bc494be977e4c5b3193c3a928c0764fa529d8d5a47563fe2a825e66` 补齐 cache、bundle、coverage-resolution、pointer 四项 ACTIVE 登记，Git 锚定 `4c5cd578a5f1a10449d128dcdb91a724c359e7a5`。该提交的 verify CLI 局部变量修正改变了文件哈希，原登记表漏跟进。
- 历史生产者及 REVOKED 优先规则保留；不修改生产者、读取器、对账算法、协议或共有版本 7.0.1，仅在 codex 分支登记本次修复。

### 回归与边界
- `test_sqd_gap_repair.py` 从现用程序文件计算哈希，逐项检查四协议登记；逐一缺项、哈希撤销及未知 bundle producer 必须拒绝，旧版生产者继续认可。
- 该测试与 `test_batch18_review_digest.py` 的正向修复/消费/对账链移除临时自动认可哈希，直接使用真实登记表；沿用现有全量 suite 的 146 个入口。
- MELANIA 仅只读核验原修复包的来源与缓存选择，不运行 A2/A3、不关闭异常、不改变案例产物。来源门禁通过不代表整个 A2 已通过。

---

## [c2.2.3] - 2026-08-16 — time verifier final-block 回填修复

### 修正
- `scripts/report/shared_release_receipt.py` 的时间回执深验，balance 锚点缺少 `day_end_block` 时，改用已通过 authority-chain 校验的 `plan.final_block`；语义与 `time_spotcheck.py` 的正式执行路径一致。
- 显式 `day_end_block`、tx `block`、签名计划、目标块和逐调用 transcript 的现有 fail-closed 校验保持不变。

### 回归
- `test_recon_deep_reverify.py` 新增无 `day_end_block` 的边缘 balance 点：生产器必须在冻结块执行并记录该块，consumer 必须接受；修复前稳定复现 `time rows do not correspond one-to-one with plan points`，修复后通过。

---

## [c2.1.0] - 2026-07-30 — split-run −1 模式执行条款（G6）

分段执行计划步 3。CC 侧 v6.1.0 已落 split-run.md（−1/−2 边界、交接契约、handoff_manifest.py 四子命令）并经 c2.0.0 同步下发；本条把"codex＝−1 主轨执行者"落成 SKILL.md 适配节 G6 正式条款，要点：触发语（"对 <币> 跑 −1/机械段"）；探针＋案级 `.stage1.lock` 双前置；停止线八项与 G4/G5 的优先级关系写死（−1 不冻结实体，冻结前硬闸由 −2 承担，不矛盾）；初步定性只落 `sealed/`；未档异常写 blocker；全程盲化＋逐步 receipt；收工 generate 如实报状态、提示用户开 CC −2 会话、完成即停；三账审计件与 handoff 契约互不替代；与 G2 禁自审兼容。验收走计划步 3.5 dry-run 与步 4 首战。

## [c2.0.0] - 2026-07-30 — v6 大同步 + 适配层通则化重构（主版本：架构级）

背景：codex 分支停在 v4.1.0 merge，落后 main 十提交（v4.2.0 复盘、v5.0.0 框架修订、v6.0.0 架构重构、v6.1.0 分段执行、v6.1.1 回灌修正）。分叉期 codex 在 16 个共有文件上有本地迭代（3.26.0–3.30.0/c1.x），直接 merge 必然大冲突且语义矛盾（如 codex 文档仍写 P0/P1 而 main v5.0 已废止分级）。按分段执行计划步 2 执行：迁移矩阵先行 → 回灌 → merge → 通则化重构。

- **c2-migration-matrix.md（merge 前定稿）**：16 双改文件逐项解法/概念迁移/gate schema 核查/测试新预期/分歧存档五节；执行中 1 处初判被全文核查推翻并公开修正（枢纽两段版实为重组强化非分歧，已保留）。
- **回灌 main 两件**（先于 merge，CC 侧 6.1.1）：camp_share_series schema 修正（文档-引擎漂移，c1.0.0/FROGGIE 发现）；BITCOIN 案 miss-queue 85 行（标签库 CC 真源约定）。
- **SKILL.md 通则化重构**：main v6 薄骨架为体＋「Codex 运行适配」独有节——G1 路径/工具/模型映射、G2 禁自审（c1.1.0 通则化：下发文档中一切外审步骤一律读作不适用）、G3 独有资产清单、G4 经济控制硬闸（3.29.3 收编）、G5 静置仓反扫硬闸（3.29.2 收编）＋第 0 步同步/worktree/迭代规则。**逐文件改字式适配全部废止**：labels/README、research-workflows、update-workflow、easy-workflow、monitoring-package 等与 main 一字不差，映射靠通则——未来同步这些文件零冲突。
- **概念迁移（P0/P1→庄级判定）**：economic-control-accounting.md 4 处、entity-cluster-methods 卫生审计段、analysis-playbook 登记行、test_cluster_quality 测试数据 2 处 tier 残留、docs_lint tiering needle——codex 独有资产内 P0/P1 全清零；audit_release_gate.py 的 whale_tier 维度名保留（协议内部枚举，语义=现行门槛判定）。
- **codex 方法学资产重挂**：tiering 尾部"Codex 侧复盘判据"节（7 条，FROGGIE/ASTEROID 案，措辞已迁 v5）；report-template 尾部"Codex 版补充义务"节（经济控制账同源驱动＋checklist 附加 4c/4d）；entity-cluster-methods 的 dormant_warehouse_audit 段与"冻结后成员卫生审计"正式条随 merge 保留。
- **测试融合**：run_all＝main 16 项＋test_audit_release_gate＝**17 项全 PASS**；test_cluster_quality T1–T5、test_figures_from_facts 双方增量并存。
- **分歧存档（唯一）**：state-anomaly §9c——codex 弱化版（强 CEX 连接通道四命题拆开）vs main 正式条（调度商定性两案转正），`git show 639bfd0:references/playbook-state-anomaly.md` 考古，留用户/复盘裁决。
- TODO（c2.x）：accumulate_offenders 单案模式增强回灌 main；枢纽两段重组+卫生审计正式条回灌 main 评估；−1 模式条文（c2.1.0，split-run 主轨执行者）。

## [c1.1.0] - 2026-07-29 — 删除 Codex 自审伪外审流程

### 修正
- 删除 `research-workflows.md` 中从分析会话调用 `codex exec`/Codex CLI 的整条外部异构怀疑者流程。
- 完整版、easy 与 update 模式不再追加同模型 Codex 外审路；Codex 子代理怀疑者和完整性角色统一定性为“内部对抗性自检”。
- 在 Codex 运行适配中加入永久边界：同一 Codex 体系的二次审阅不得宣称外部独立复核；真正外审需由不同模型或人工审计方在本 skill 自动流程之外执行。

### 保留
- 原始 Transfer/快照独立重算、输入哈希、三账、反例脚本、发布硬闸和内部 fail-closed 自检全部保留。
- “既有报告净室复核”模式继续存在：它复核的是外部既有报告，并从原始数据重建，不等同于 Codex 对自己刚生成的报告作外部审计。

### 验收
- 活跃流程文件中不再出现 `codex exec`、`GPT-5.6-sol` 外审或 `adversarial_external` 调用。
- 完整离线测试与 skill 结构校验通过后提交。

## [c1.0.0] - 2026-07-29 — 独立分享包可运行

### 写入
- `sync-from-cc.sh` 先判断当前 skill 是否拥有自己的 `.git`、有效 worktree 与本地 `main` 分支；缺任一项即视为独立固定快照，明确提示后返回 0。
- 维护者本机的 `codex ← main` 单向合并、脏工作区拦截、冲突退出码与全套测试门禁保持不变。
- `SKILL.md` 与 `SYNC.md` 补齐分享包边界：快照可直接使用，但不会自动获得后续 CC 更新，需重新分发新版 ZIP。

### 验收
- 独立无 `.git` 安装返回 0；双线 worktree 仍走原同步路径。
- 完整离线测试与官方 skill 结构校验均通过后才打包。

## [3.30.0] - 2026-07-26 — 既有报告独立复核净室协议、机器发布总闸与逢0轻整编

> 本条只沉淀跨标的审计方法、证据边界和自动化验收，不保存任何标的结论或地址。

### 写入
- 新增净室复核协议：原报告只作待审命题清单；开工冻结输入哈希和数据截止；从原始Transfer、快照、交易与设施权益重建地址类型、实体和历史序列。
- 强制当前≥0.5% owner及历史峰值、归零、回落、静置候选全覆盖；成员账、位置账、经济控制账三分离；算术闭合不得替代地址分类和最终受益权证明。
- 新增 `scripts/report/audit_release_gate.py`：检查11项必需资产、输入哈希、三项对账、候选闭合、经济账防双计、逐命题证据、CEX受益权、完整阴性结论、对抗否决项及历史图六查，任何失败 exit 2。
- 新增离线契约测试：覆盖合格放行、输入篡改、CEX通道受益权未证、阴性候选未闭合、末日封口/负钳零和未关闭发布否决项。
- 修正旧CEX调度规则：资金经中转进入CEX和核心仓零DEX只能证明强CEX连接的运营/资金通道，不能证明交易所/做市商身份，也不能一票否决庄家；最终受益人未知时必须停在经济身份未决。
- 逢0轻整编：把散落在主skill、证据措辞、状态异常、复核workflow和报告checklist的审计防线归位到 `independent-audit-protocol.md`，保留一层直达索引，未删除拿不准的存量规则。

### 指标
- 成本：本次为交付后方法修正，轮次数、终端调用数和准确用时未可靠自动计数，不补猜。
- 质量：初稿关键结论2；复核裁决 REFUTED 2 / WEAKENED 1；漏检地址类型和私人层覆盖1类；传播级历史图错误1。

## [3.29.1] - 2026-07-24 — 临时托管双账本与图 2 连续性硬闸

> 本条只沉淀通用记账语义、制图口径和自动化验收，不保存标的投资结论。

### 写入
- **双账本语义**：图 1回答“币当时在哪个链上位置”，可逐笔闭合的本金进入锁仓、vesting、NFT 仓位或其他公共设施期间放入设施阵营；图 2回答“本金经济上仍属于谁”，同一本金在托管期间继续计入原实体。公共设施不进永久成员表，转入设施不得直接解释成实体清仓。
- **连续性硬闸**：图 2 的 `whale_series.json` 可为实体线声明 `temporary_custody_checks`（label/start_date/end_date/minimum_raw）。`figures_from_facts.py check` 除终值对账外，新增区间最低持仓与可归属本金比较；终值即使正确，只要托管期间错误归零仍拒绝交付。
- **测试与报告纪律**：离线测试新增“锁仓中途归零、赎回后终值恢复”的反例和连续归属正例；report-template 的标准图定义、图层同源说明与 checklist 增加双口径对账，图注必须用大白话解释两图为何不同。

### 指标
- 成本：交付后修正轮次数、终端调用数、准确用时未可靠自动计数，不补猜。
- 质量：新增复核裁决 REFUTED 1；漏检庄级实体 0；传播级曲线错误 1。

## [3.28.0] - 2026-07-24 — QUQ(BSC) LP 手续费专项复盘：V4 四层账本与已实现措辞硬闸

> 本条只沉淀协议机制、账本方法与措辞边界，不保存具体代币收益结论。

### 写入
- **触发与路由**：skill frontmatter 增加庄家 LP 收入、V3/V4 手续费比较与LP费计算触发；新增直达参考 `lp-fee-accounting.md`。
- **四层账本**：正式分开池子窗口产生费、仓位应得 gross fee、目标区块未结算 fee、历史已结算/已提现 fee。V4 普通事件不公开本金/fee 拆分，但核心返回 `feesAccrued`，逐 Swap×逐 tick 可归因应计 fee，`feeGrowthInside` 可算未结算快照；“事件不直接给”不等于“不可计算”。
- **措辞硬闸**：`窗口应得 fee−期末未结算 fee` 仅可称 `settled_or_crystallized_estimate`；没有内部 trace 或严格对齐双端 checkpoint 时，禁止写“已提现”“已实现净收入”。gross fee 必须与 gas、无常损失、库存价格变化、协议/hook 扣费和自成交回付分开。
- **归属与对账**：官方 PositionManager 头寸按 ownerOf + NFT Transfer 历史 + position state 归属；receipt 净现金流不再冒充 fee 账本。新增8项 gate，核心门禁是全史 ModifyLiquidity 重放后逐笔与 `Swap.liquidity` 对表，任何 mismatch 均停止出数。
- **旧规则修正**：channels 中“V4只有净现金流无硬数”被替换；V3 `collect−burn=fee` 增加同仓位、期初 tokensOwed 与跨窗结转条件；V4 TVL/幽灵仓条改为所有权历史优先。

### 遗留项与指标
- 首次完成通用方法定型，但现有可运行脚本仍为案专属硬编码版本；按“稳定复现后再收编”纪律，待第二个独立 V4 LP 案验证动态费/hook/协议费与 NFT 转移分支后，再参数化为 `scripts/evm/` 通用件。当前禁止把案专属脚本直接当跨池工具。
- 成本：轮次数、终端调用数、准确交付用时未在开工时自动计数，不补猜。
- 质量：专项初稿关键结论 4；机制复核 CONFIRMED 3 / WEAKENED 1 / REFUTED 0；漏检实体 0；传播级数字错误 0，传播级口径命名错误 1（“已实现”降级为“已结算/晶化估算”）。

## [3.27.0] - 2026-07-24 — ASTEROID(ETH) 完整版复盘：行为组隔离、盘中峰值与可观测口径

> 本条只沉淀机制、口径与工具修复，不写标的结论。

### 写入
- **实体判级**：正式区分“行为 cohort”与“单一实体”。同块、同额、同上限可由协议参数机械制造；没有独立控制证据，不得把 cohort 合计峰值用于狙击集团判级，也不得回灌惯犯库。公开自报的 community/multisig 项目角色按 P0 单列，但不自动证明技术多签有效或与 deployer 共同控制。
- **时间粒度**：日终/粗粒度曲线不能替代逐事件峰值；同日往返的盘中峰值须作为事件点补回图表，所有庄级判级和“从未超过 X%”断言仍以逐事件重放为准。
- **可观测口径**：公开 CEX 标签求和统一称“公开标签地址余额合计”，不得冒充完整客户托管量、做市库存或净头寸。价格图新增覆盖审计；缺失区间留白，稀疏日线不得支撑分钟/小时级精确事件收益率。Blockscout internal-transactions 若自报 incomplete/仍有分页，只能作为下限。
- **脚本安全**：`accumulate_offenders.py --help` 在任何默认路径解析与写文件前返回；新增行为 cohort/非同一主体/共同控制未确证排除式，零合格组继续 fail-closed。`test_cluster_quality.py` 增加只读帮助与行为组隔离回归。

### 遗留项与指标
- 不自动删除现有 serial 标签；历史上可能由行为组写入的条目需要逐案人工复核，批量清洗会有误删风险。
- 成本：轮次数、终端调用数、准确交付用时未可靠自动计数，不补猜。
- 质量：初稿关键结论约 8；复核裁决 CONFIRMED 11 / WEAKENED 9 / REFUTED 2；关键漏检 1；传播级数字错误 0（冻结前另修正 3 项数据/口径缺口）。

## [3.26.0] - 2026-07-24 — FROGGIE(BSC) easy 复盘：schema 契约、单案安全回灌与聚类边界

> 本次只沉淀工具与方法，不携带标的结论。两条方法规则均为首次观察，按制度保留【候选·单案】。

### 写入
- **报告/监控 schema**：`monitoring-package.md` 与 `report-template.md` 统一规定 `camp_share_series={"dates":[...],"series":{"阵营":[...]}}`；逐日对象列表不再作为合法示例。`figures_from_facts.py fig1` 对错误列表形态给可读 FAIL，不再泄漏 `AttributeError`，测试新增反契约样本。
- **惯犯回灌安全模式**：`accumulate_offenders.py` 同时接受分析根目录与单案目录。单案写默认库时先加载现有 `serial_actors.csv` 增量合并，避免把全库截成一个案；找不到状态文件或筛出 0 个实锤组时 fail-closed，输出文件保持原样。`test_cluster_quality.py` 新增单案成功与空目录不改写两路。
- **实体聚类候选**：短窗口多来源整包归集只证明归集后统一控制；缺少共同 funder/部署者/跨链同控/机械连通等独立证据时，来源钱包进入 `associated_source_addresses`，不进入确定成员。
- **判级候选**：直接大额串联边只证明资金活动链，不自动证明最终受益人同一；先按可证实体份额机械过当前/历史 P0/P1 门槛，再讨论自动化交易、集中清算或刷量例外。

### 指标
- 成本：轮次数 / 终端调用数 / 准确交付用时均未在开工时自动计数，不补猜。
- 质量：初稿关键结论 4；复核判定 CONFIRMED 0 / WEAKENED 2 / REFUTED 1；漏检 P0/P1 0；传播级数字或判级错误 1。
